# Fix plan — the audit findings of 2026-09-22

Written from the lawyer audit of commit `6792f0c256e81e1473908308bd73881004853be4` and the comparison
of the colleagues' business papers against the same tree. It is a **plan, not a backlog**: nothing is
filed as a ticket. The owner ruled on 2026-09-22 that the whole plan runs on **one branch with one
PR**, phases in order, one commit per item — so this document is the only index the work has.

## The rule this plan follows

**Only defects.** A finding earns a row here when the code is wrong, dead, or contradicts itself —
something a reader can check without asking anybody what the business wants. Everything that needs a
ruling is in [§ Blocked](#blocked--needs-a-ruling-before-any-code) with the decision that blocks it,
and is **not** planned, scoped or estimated. Two consequences worth stating plainly:

- The largest money finding — a crew of three paying three full pay rows against one flat price — is
  **deferred by the owner to R26** and is not in any phase. Fixing the package-pay defect (item 2.1)
  without it makes package jobs cost more, not less: the packages that were paying nothing start
  paying, per cleaner. That is the intended reading of "fix the defect, leave the model alone", and it
  is called out again in phase 2.
- The guest access key is **re-keyed, not redacted** (owner ruling). That makes phase 1 the largest
  piece of work in the plan, and the only phase that touches every tree at once.

## Phases at a glance

| # | Phase | Why it is in this position | Trees touched |
|---|---|---|---|
| 1 | Guest access re-key | The only finding reachable by a real person today: an assigned cleaner can cancel the guest's order and charge them the fee | backend, web, both customer mobile apps, e-mail |
| 2 | Money the backend gets wrong | Money owed, money not moved, a webhook that 500s | backend |
| 3 | Receipts that a tax adviser would reject | Every order produces one; the defects are in the document itself | backend, PDF layouts |
| 4 | Personal data written over the wrong person | One erasure reaches other customers' rows | backend |
| 5 | Copy that contradicts the code | Promises to customers the code does not keep | web, mobile i18n |
| 6 | Dead code, duplicated truth, guards | Cheap, and it stops the next audit re-finding them | backend, web, Android |

Phases are ordered by exposure, not by size. Any phase can be dropped without breaking a later one,
with one exception: phase 3 assumes phase 2's refund ordering is in (a receipt is regenerated after a
refund).

---

## Phase 1 — Guest access re-key

**Finding:** F69. An assigned cleaner is served the unredacted order detail, which carries
`DisplayOrderNumber`, `CustomerEmail` and `ConfirmationCode` — exactly the triple the anonymous
`POST /api/Order/CancelGuest` accepts. Cancelling that way charges the guest the 25 % / 50 % tier.

**Original shape (owner ruling; proposal history):** replace the triple with a one-time token the guest receives by e-mail. The
confirmation code stops being an access credential, which closes the class rather than one leak.

| # | Item | What changes |
|---|---|---|
| 1.1 | Guest access token | A token entity (order, hash, issued, expires, used), issued on order creation and on request; the anonymous endpoints take the token, not the code |
| 1.2 | Guest endpoints on both customer hosts | `LookupOrder`, `LookupOrderBatch`, `CancelGuest`, `GuestCancellationPreview` re-keyed; old parameters removed, not deprecated |
| 1.3 | E-mails | Order confirmation and cancellation e-mails carry the link; five locales; the code is no longer printed |
| 1.4 | Cleaner-facing redaction | `ConfirmationCode` leaves every cleaner-facing DTO and its redaction test asserts it |
| 1.5 | Clients | Customer web guest screens, both customer mobile apps, NSwag regeneration and the mobile spec re-dump |
| 1.6 | Schema | Migration regenerated (one `Initial`), DEV drop owed at the next DEV deploy |

**Shipped semantics:** the table above preserves the original proposal. The implemented token is a
reusable 256-bit credential, valid until expiry or revocation, expiring 30 days after the cleaning.
Each message issues an independent credential; issuance alone does not consume or revoke earlier
credentials. Only the hash is persisted; there is no used/consumed state. The short confirmation code
remains a display reference, never an access credential.

**Done:** a cleaner cannot produce a working guest cancellation from anything the API gives them; a
guest can still find and cancel their order from the e-mail; the integration suite covers both.

**Not in this phase:** expiring or re-issuing tokens from the customer app, guest order history,
attaching a guest order to an account later.

---

## Phase 2 — Money the backend gets wrong

| # | Item | Finding | What changes |
|---|---|---|---|
| 2.1 | Package pay is never written | F68 | `CalculateOrderPay`'s handler and validator include `SelectedPackages`; an integration test books a package-only job and asserts the pay row |
| 2.2 | An upheld complaint can close without the money moving | F70 | `ResolveDispute` attempts the refund first and writes the terminal state and `RefundAmount` only on success; a failed refund returns a business error instead of success |
| 2.3 | A guest chargeback 500s the webhook | F71 | `Dispute.UserId` nullable (or a guest branch); the webhook records the dispute and acknowledges; schema change, so migration regen + DEV drop |
| 2.4 | Fiscalisation could never match | F78 | The resolver keys on the same code the service declares (`CZE` against `"CZ"`); a test pins the pair |
| 2.5 | Revenue report mixes gross and net | new | `TotalRevenue` and `DailyRevenues` agree — both net of both refund branches |
| 2.6 | Subscription renewals are not recorded | F80 (part) | The Stripe webhook handles `invoice.paid`; `order.confirmed`, a catalogue key with no producer, is removed |
| 2.7 | A pay overload that disagrees with the shipped path | new | `EmployeePayConfig.CalculatePay(int,int,decimal)` is deleted (no production caller; pays `× rooms` where the real path pays `× (rooms − 1)`) |

**Interaction to keep in view:** 2.1 makes package jobs pay. With R26 deferred, each assigned cleaner
gets a full pay row, so a three-cleaner package job pays three times the configured rate. That is the
existing model applied to a case that previously paid nothing — not a new defect, but it is money.

**Done:** every completed order produces exactly the pay rows the configuration describes; no Stripe
event we receive can 500; the revenue report reconciles with the refund ledger.

**Shipped (`799b2c635`):** 2.1, 2.2, 2.3, 2.4, 2.7 and the `order.confirmed` half of 2.6. **Struck with
evidence:** 2.5 — the headline and by-tender figures are gross on purpose (they reconcile against a
gateway statement; `NetRevenue`, the daily series and the service/package splits are net of both
refund legs); the `invoice.paid` half of 2.6 — renewals are already recorded by
`customer.subscription.updated`, and `invoice.paid` would be a second writer of the same fact.

**Found after phase 2, reported, not fixed:**

- **A chargeback on any web card booking is dropped.** The webhook finds the order only by its stored
  payment intent; `AssignStripePaymentIntentId` is called only by the mobile `CreatePaymentIntent`
  (`[Authorize]`) and `ConfirmRecurringOrder`, while web Checkout stores the session id alone. So a
  chargeback on a web booking, guest or account, answers 200, records no dispute and tells no
  administrator, and its `updated` / `closed` events find nothing. 2.3's fix is correct but guarded a
  state no guest can reach — `GuestChargebackWebhookTests` seeds a payment intent the guest flow never
  writes, and the `Dispute.cs` comment describes that path as live. Money; needs a go-ahead.
- `ResolveDispute` records the refund amount the administrator asked for, not the amount the refund
  seam moved (it clamps the card share and returns the credit share separately), and a retry re-drives
  the first attempt's row whatever amount it names.

---

## Phase 3 — Receipts

**Finding:** F77 — the document the customer keeps.

| # | Item | What changes |
|---|---|---|
| 3.1 | Lines sum to the total | The discount, the express surcharge and the extras' prices each get a line; a test asserts `Σ lines == Total` for a booking that carries all three |
| 3.2 | A paid cash order stops printing `Pending` | The receipt is regenerated (or amended) when the cash is marked collected |
| 3.3 | The receipt is in the customer's language | The layout's labels come from the order's language, not a hardcoded English set; the payment method prints a translated word, not the enum name |
| 3.4 | A non-payer's receipt stops printing a VAT number | „Nejsme plátci DPH" and a `DIČ` line do not appear together |

**Not in this phase:** a credit note (a new document type — R1/R24 territory), fiscal registration.

---

## Phase 4 — Personal data written over the wrong person

| # | Item | Finding | What changes |
|---|---|---|---|
| 4.1 | One customer's erasure rewrites a neighbour's address | F72 | Before anonymising, the erased customer's orders get their own copy of the address row; the shared row is left alone. The same guard in the retention sweep |
| 4.2 | A phone change rewrites other customers' orders | F82 | The update is scoped to the caller's own orders |
| 4.3 | The cleaner's read of the access instructions is not recorded | F82 | An audit entry is written when an assigned cleaner first reads them — the same evidence an administrator's reveal already leaves. No change to what the cleaner may see |
| 4.4 | Order photos have no retention window | F82 | **7 days** after completion (owner, 2026-09-22), as a `RetentionDefaults` entry the sweep reads. An order with an unresolved dispute keeps its photos until the dispute closes — they are its evidence |
| 4.5 | The admin and cleaner audit tables have no window | F82 | **3 years** (owner, 2026-09-22), the same figure the customer audit already carries |
| 4.6 | Guest access tokens are never deleted | phase 1 | The retention sweep removes a `GuestOrderAccessTokens` row once it has expired or been revoked. Struck if the tree already does it |
| 4.7 | A live guest token outliving the data it opens | phase 1 | If any erasure or retention path removes a guest order's personal data while its token is still live, that path revokes the token. Struck if no such path exists |

**Not in this phase:** time-boxing the cleaner's access after completion — that is R8.

**Review outcome (2026-09-23):** data integrity, GDPR/audit, jobs/tenancy and
test coverage reviewed. The cleaner's own shared address is included in 4.1; saved-address-only
references, every dispute status and distinct company photo windows gain Postgres cases. The admin
catalogue gains the three retention descriptions and the cleaner read label in all five locales.

Two concurrency findings survived adversarial refutation (three independent confirmations each):
concurrent first reads could duplicate the audit row, and a new address reference could arrive after
the sharing census. The read audit now serializes its existence check and insert under an order-row
lock. Address anonymisation moves each affected order/employee to a new blank row and deletes an
unshared original; the order-address FK must restrict deletion, rather than cascade-delete a racing
new order. The single `Initial` migration is regenerated for that FK. A concurrent new reference can
refuse the erasure commit, leaving the other person's data intact.

**Explicit CQRS exception for 4.3:** the authorized assigned-cleaner detail query awaits an
out-of-band audit commit before returning nonempty access instructions. The separate context cannot
flush the query's tracked business graph, and a query has no UnitOfWork commit to carry the evidence.
This exception is confined to recording the disclosure; it grants no additional access.

**Local verification:** solution build (`-m:4`) has zero errors; `Cleansia.Tests` passes 6,339 tests.
The full single-threaded integration run executes 600 tests: 598 pass and two new race-test fixtures
fail on their strict mock's disposal setup. After adding that setup and correcting the expected
Postgres restrict-deletion SQLSTATE, the entire 11-case shared-address class passes. No production
code changes follow the full run. Single-threaded `Cleansia.HostTests`: 347 passed. With
`NX_DAEMON=false`, admin-app Jest: 267 passed; audit-log Jest: 66 passed. Every `agents/tools/check-*.mjs`
script and self-test exits zero; browser checks use the local customer frontend with the DEV proxy.
The spacing reporter alone prints two advisory mismatches (hero padding 12px vs 52px, title margin
12px vs 30px); the home-page SCSS is unchanged from the audited base. Reported, not changed. The final run repeats
the full backend suites after phase 6. Migration `20260923064534_Initial` changes only the address FK.

**Owed:** MS-2 DEV-only database drop immediately before the next manual DEV deploy (also reseeds
phase-5 perks); one owner Mac sitting for customer API generation and both iOS schemes' builds/tests,
batched with the later phase-6 mobile changes. No production action taken.

**Found in phase 4, reported, not fixed:**

- `GdprDeletionService.ExtractBlobNameFromUrl` keeps the container segment, so order-photo erasure
  addresses the wrong blob; `DeleteOrderPhoto` uses `Segments.Skip(2)`, wrong with Azurite.
- `Order.AnonymizeCustomerData` leaves `CustomerFloor` and `CustomerApartment`.
- Erasure deletes saved-address links but leaves their Address rows when no erased order or cleaner
  profile reaches them.
- The saved-address erasure loader selects only active links; an inactive saved-address link and its
  street survive. The address guard excludes only links actually staged for removal, so this existing
  residue does not turn erasure into an FK failure.
- `AdminCancelOrder`, `CancelUnfilledOrders`, `CleanupStalePendingOrders` and
  `CompanyWindDownService` do not revoke guest links; the booking flow prose overclaims this.
- `FiscalRetryService` and `GenerateReceiptHandler` issue guest links without the anonymisation guard.
- A phone change in a frozen company throws `CompanyArchivedException`, contrary to ADR-0064 D3.

**Found in the phases 2/3/5 docs pass, reported, not fixed:** the express-waiver role card's record
shape is stale; the cancellation/refund flow's ceiling omits `CreditAppliedAmount`; the partner status
legend has no New/OnTheWay rows.

---

## Phase 5 — Copy that contradicts the code

**Finding:** F81. Each is one key in five locales; none needs a code change.

| # | Item | What changes |
|---|---|---|
| 5.1 | „stupeň už vám nikdy neklesne" | The loyalty page stops promising it, because revoked points lower the tier |
| 5.2 | The Plus free trial | The FAQ and the CTA stop offering a trial no plan carries |
| 5.3 | Tier perks with no reader | `priority_support` and `dedicated_pool` are removed from the seeded perks, or the page stops listing them |
| 5.4 | The dead `Pending` status in the partner help | The legend drops the row |
| 5.5 | The home page's Plus band offers the same trial | Found running 5.2 — the band's CTA and lead stop offering it |
| 5.6 | The recurring-bookings gate sends the customer to a trial | Found running 5.2 — the gate's CTA says what joining Plus actually does |
| 5.7 | Trial keys left with no reader | The keys 5.2 and 5.5 orphaned are deleted in all five locales |

**Done (uncommitted → committed with this phase):** five-locale copy in both web apps, the seed no longer
carries `priority_support` / `dedicated_pool` on Gold or Platinum, and three claim specs
(`loyalty-tier-claim`, `plus-trial-claim`, the widened `membership-express-claim`) fail if the copy
promises again what the code does not do. **A DEV database seeded before this phase keeps the old perk
rows** and shows their raw keys on the loyalty page — the DEV drop already owed for the phase-3
migration reseeds it.

**Found in phase 5, reported, not fixed:**

- The Android and iOS customer apps carry the same trial copy (`home_upsell_plus_cta` "Try Plus free", and their FAQ).
- `lead_no_trial` in Czech declines "4 hodin" where it should read "4 hodiny".
- The loyalty `history_lead` says points only go up; refunds and admin revokes take them away.
- A Platinum member never sees the note that a tier can drop.
- No backend test pins tier demotion on revoke — the copy now depends on it.
- About 18 `pages.membership` keys have no reader.
- The admin console writes `PerksJson` free-form, so an administrator can re-seed a perk nothing reads.
- The Gold `INSERT` seeds `MinimumOrderAmountForDiscount` as NULL while the later `UPDATE` sets 1000.
- The home band's `ctaLink()` points at a route that only redirects.
- `TECHNICKE-MEZERY.md` cites `trial_disclosure` as a live promise — corrected at the re-pin.

---

## Phase 6 — Dead code, duplicated truth, guards

| # | Item | Finding | What changes |
|---|---|---|---|
| 6.1 | Enum members with no producer | F83 | `OrderStatus.Pending`, `PaymentStatus.Disputed`, `RefundStatus.Failed`, `ContractStatus.Active` / `Terminated` — removed, or pinned by a test that says why they stay |
| 6.2 | `IsFirstTimeCustomer` is `const false` | F83 | Either the 60-minute first-order window is reachable or the constant and the window go; today it is a rule nobody can hit |
| 6.3 | Server booking-window constants nobody reads | F83 | `FirstWindowHour` / `LastWindowHour` / `WindowDurationMinutes` either gate `CreateOrder` or leave the server |
| 6.4 | The VAT rate is stored twice | new | `CountryConfigurations.StandardVatRate` and `CountryInvoiceConfigs.VatRate` are tied, or one wins; `ReducedVatRate` (ten markets, no reader) goes with it |
| 6.5 | `Rooms` / `Bathrooms` have no upper bound | new | A sane maximum in the validator; today the price grows linearly and uncapped |
| 6.6 | `DocumentType.InsuranceDocument` and `TaxDocument` | new | No seeded requirement and no reader — removed, or a requirement seeded (the *policy* question is R7; removing dead enum members is not) |
| 6.7 | `SetTravelDistance` has no caller | new | `DistanceRatePerKm` pays nothing because nothing sets the distance; the dead setter goes, the rate stays as data |
| 6.8 | Web and Android leftovers | new, F83 | A market selector with no reader; a test fixture with the insurance amount on the wrong country; `allowBackup="true"` on the partner Android app; `Package`'s orphaned `[Required]`; `PackageService`'s comment naming a `Package.Price` that does not exist |
| 6.9 | Monthly recurrence drifts | new | A 30-day step walks off the chosen weekday; either it is calendar-monthly or the copy says "every 30 days" |
| 6.10 | A partial refund leaves the extras out of its split | found in phase 3 | `IssuePartialRefund` loads the order through `WithDetailGraph`, which never includes `SelectedExtras`, so the extras loop runs over nothing in production. Money — load what the split reads, prove it on Postgres |
| 6.11 | The receipt prints the cleaning time in UTC | found in phase 3 | A 10:00 Prague booking prints 08:00; convert with the market zone the receipt already resolves |
| 6.12 | A VAT payer can be saved with no VAT number | found in phase 3 | `UpdateCompanyInfo` does not require one when `IsVatPayer` is true, so that company's VAT receipts carry no DIČ |
| 6.13 | The recurring occurrence's surcharge contradicts its comment | found in phase 3 | `MaterializeRecurringBookingTemplate` prices the occurrence with no cleaning date and says the surcharge never applies, while the factory applies it by occurrence date — make the two agree |
| 6.14 | Dead and stale members found along the way | found in phases 1–3 | `order.on_the_way` case label in the partner Android deep link (no template can reach it); `order.seat_open` missing from both iOS rosters and deep links; the loyalty perk strings `priority_support` / `dedicated_pool` on Android and iOS; the `Order.MembershipDiscountAmount` comment calling it exclusive of the tier discount; `OrderConfirmedHonestProducerTests` named for a key that no longer exists; the fiscal test fixtures built on alpha-2 country codes production never stores |

### Phase 6A disposition — 2026-09-23

- **6.1 retained with evidence:** the persisted/wire enum numbers do not move. Pending is read for
  historical orders and refused for new admin transitions; Disputed remains paid-history input and
  blocks checkout retry; Failed refunds do not count as moved money; Active remains a legacy
  eligible cleaner contract and Terminated is excluded from offers. Existing tests and the new
  paid-history/hold cases pin those uses.
- **6.2 and 6.9 await the exact owner questions below.** No cancellation or recurrence policy changes.
  Monthly currently adds 30 days and then realigns to the selected weekday, normally 35 days; the
  proposed "every 30 days" wording would not describe the implementation.
- **6.3 partial:** removed only the unread `WindowDurationMinutes`. The first/last-hour constants
  are read by the client-parity checker and remain its authority. API time-grid enforcement awaits a ruling.
- **6.4 partial:** removed unread `ReducedVatRate` from domain, EF and seed columns. The sales and
  retained invoice-rate configurations remain separate pending a ruling; current cleaner invoices
  carry zero VAT under the non-payer ruling R16. Initial is regenerated for the removed column.
- **6.5:** maximum eight rooms and four bathrooms, matching the web picker, enforced in create,
  quote, Plus savings, and both recurring-template validators. Existing lower-bound behavior stays.
  The new error has customer web/Android/iOS translations in all five locales.
- **6.6 premise struck:** TaxDocument and InsuranceDocument are supported by the generic validated
  requirement writer and document-upload reader. New tests save/read approved optional documents;
  no mandatory onboarding policy is invented and no enum value is removed.
- **6.7 and backend 6.8/6.14:** removed the unused travel setter, preserving stored distance and pay
  data; removed the orphan Package attribute and corrected PackageService/membership comments;
  renamed the payment-confirmed test and used production alpha-3 codes in ordinary fiscal fixtures.
- **6.10–6.12:** include persisted extras in refund detail loading; print market-local cleaning time
  on initial and regenerated receipts; require a VAT number for VAT payers on both create and update.
- **6.13:** corrected the comment only. The materializer computes a raw subtotal; the factory applies
  the occurrence-date express surcharge exactly once. No recurring waiver is reserved (ADR-0035).

**Regression proof:** before fixes, size tests were 15 failed / 10 passed; conditional VAT tests
6 failed / 4 passed; PostgreSQL refund/time tests 4 failed. The refund requested 1200 rather than
1000 for a service beside a 200 extra, and receipt cases failed in summer, winter and across midnight.
Green verification: solution build zero errors; unit suite 6,377 passed; full PostgreSQL suite
604 passed against regenerated `20260923071814_Initial`, including the phase-4 concurrency cases.
The generated schema delta removes only `ReducedVatRate`. No endpoint/DTO/enum shape changed;
NSwag and the mobile spec do not need regeneration for phase 6A. Single-threaded host suite:
347 passed. Customer-app Jest (including five-locale error parity): 81 passed. Source review clean;
Android resource tests and the owner Mac pass remain part of the batched client verification.

**Found in phase 6A, reported, not fixed:** `PackagePricing.cs` also has a stale reference to the
removed `Package.Price`; some mode-only fiscal fixtures use `standardVatRate: 19m` while production
stores a fractional rate; CreateOrder has no explicit negative room/bathroom guard, unlike quotes
and recurring templates. These are outside the selected mechanical removals/upper-bound fix.

### Phase 6B — client follow-through

The unused market insurance selector is removed and its fixture uses the Czech seeded coverage,
with Slovakia unset. Android partner backups are disabled while the existing extraction exclusions
stay; its unreachable `order.on_the_way` route is removed. iOS keeps that APNs event, adds
`order.seat_open` to both loc-key test rosters and the partner feed/order route, and continues to
reject partner-only navigation in the customer app. The two retired loyalty perk strings are removed
from Android/iOS customer resources and their stale previews/fixtures use supported perks.

**Found in phase 6B, reported, not fixed:** iOS customer resources lack the seeded discount perk
labels (`discount_5_above_1000`, `discount_10`, `discount_12`); the generic renderer falls back to a raw
label. Retiring the other two keys does not add those missing translations. Android/iOS booking
steppers also remain open-ended; values above the phase-6A API size limit receive its translated
refusal. Clamping their controls was outside the validator-only row and is reported for follow-up.

**Local verification:** customer-stores Jest 57 passed; Android customer 1,161 and partner 627
passed, zero skipped/failures, with `--no-daemon --no-parallel --max-workers=2 --rerun-tasks
--no-build-cache` (all 88 actionable tasks executed). The iOS symbol checker and independent source
review are clean; iOS runtime/build checks require the Mac.

**One owner Mac sitting, after the branch is complete:**

1. In `src/cleansia_ios`, run `scripts/generate-api-clients.sh customer`.
2. Generate/build/test the `CleansiaCustomer` and `CleansiaPartner` schemes on an available simulator.
3. Cover the guest access-token flow, removed `order.confirmed` key, booking display language,
   eight-room/four-bathroom error translations, seat-open partner feed/title/body/job link (and missing
   order ID), customer rejection of that partner destination, both push loc-key rosters, and the
   cleaned loyalty previews/rewards fixtures. Preserve `order.on_the_way` behavior on iOS.

**Owed DEV action:** one DEV-only drop for `20260923071814_Initial` immediately before the next
manual DEV deploy, also reseeding phase-5 perks. No production action.

---

## Phase 4/6 documentation — 2026-09-23

Nineteen pages align the thirteen retention settings/fourteen sweep tasks, address-copy erasure,
guest-token revocation and cleanup, first cleaner disclosure audit, three-year dated ADR rulings,
size limits, partial-refund extras, market-local receipts and VAT-number validation. They keep
owner decisions and reported residues explicit. Independent source review corrected photo-erasure
row retention, cancellation-path scope, competing FK commit order and strict photo-cutoff cadence.
The docs reference checker reports 267 references / zero unresolved; catalog claims report zero
blocking violations (154 advisories). VitePress build passes.

## Final local verification — 2026-09-23

Checks run serially against the completed source tree, with no production access:

| Check | Result |
|---|---|
| Backend unit / full PostgreSQL integration / single-threaded host suites | 6,377 / 604 / 347 passed, zero failed or skipped |
| Full Nx lint | All 77 projects passed, cache bypassed, one project at a time |
| Full Nx test | All 74 targets passed: 3,446 tests in 328 suites; four targets have no tests; four Jest workers, one project at a time, cache bypassed |
| Customer / partner / admin production web builds | All passed; customer and admin compiled, partner reused its matching Nx cache result; no production server was started |
| Android customer / partner | 1,161 / 627 passed in 116 / 82 suites, zero failures/errors/skips; all 88 Gradle tasks executed with task/build caches bypassed and at most two workers |
| All `agents/tools/check-*.mjs` | 25 scripts exited zero; 17 static/self-test checks, eight local browser probes against a DEV-only proxy |
| Documentation build | VitePress passed; bundle-size warnings only |
| Graphify code rebuild | Completed for 6,184 files: 55,553 nodes, 80,517 edges, 4,526 communities; both graph output files refreshed |

Graphify runs in the existing WSL virtual environment; Windows `python3` has no Graphify package.
The stock run reached full extraction but spent prolonged CPU time on exact all-node betweenness
for optional suggested questions. A runtime-only wrapper repeated the full rebuild with that
ranking sampled at 64 nodes (seed 42), leaving extraction, graph construction and reporting intact.
No repository code or installed package was changed. The extractor reported 484 schema warnings.

**Verification limits and findings, reported, not fixed:** the homepage spacing probe reports two
advisory mismatches (hero padding 12 rather than 52 pixels; title margin 12 rather than 30), already
seen in phase 4. The quote-stability probe reports zero size chips, so its zero-motion result does
not establish size-change coverage. Catalog claims retain 154 advisories; consistency and web/mobile
builds also emit non-blocking advisories or warnings. These do not expand this audit-fix scope.

**Owed manual work:** DEV-only drop for `20260923071814_Initial` immediately before the next manual
DEV deployment, also reseeding the phase-5 perks; one Mac sitting for customer API regeneration and
both iOS schemes, covering the consolidated phase-6B list above. No production action.

## Audit repin findings — 2026-09-23

The six audit documents are pinned to `9e970917e1c7270a0baa0ea1ab07693990340914`, after the last
application commit and the clean audit-branch merge. Citation check: 1,706 citations, 199 without
an identifier, zero unresolved. GNU `wc -w`: PROVOZ 8,983/9,000; ROZPORY 7,888/8,000; TECHNICKE
3,701/5,000; OBCHODNI 4,032/5,000; AUDIT-LOG 1,203/1,500; PODKLADY 2,824/3,500. All six HTML/PDF
pairs and the inventory HTML were rendered; all 126 PDF pages passed visual review.

The Czech business summary is delivered as `SHRNUTI.md/html/pdf`: eight pages and 2,209 visible
words, with inline diagrams, tables and business questions. It has no visible code filenames,
audit decision IDs or commit identifiers. Its source claims and all eight rendered pages were
reviewed; the complete seven-document citation check remains at zero unresolved. Regenerating
the HTML navigation left every reviewed PDF page-content stream unchanged.

Reported only; no application change is included in the documentation repin:

- **Recurring cash can be marked paid before collection.** `ConfirmRecurringOrder.HandleCashAsync`
  sets `PaymentStatus.Paid` and queues the receipt when the customer confirms the occurrence
  (`ConfirmRecurringOrder.cs:145–167`). `MarkCashCollected` rejects an already-paid order
  (`MarkCashCollected.cs:60–61,85–91`), while the cash completion guard checks `Paid`, not
  `CashCollectedAt` (`CompleteOrder.cs:193–202`). A recurring cash order can therefore complete
  without a recorded collection, and the normal collection action cannot record the collector or
  time. Decide whether to separate customer confirmation from payment settlement in follow-up work;
  the current audit describes this exception explicitly.

The audit also narrows unsupported document claims (shared Stripe configuration versus legal
account ownership, operational legal duties versus code contradictions, historical membership
state versus dead code, and approval guards versus whole-row immutability). These are documentation
corrections, not additional application changes.

Owed manual work remains the DEV-only drop for `20260923071814_Initial` immediately before the next
manual DEV deployment, and the consolidated customer/partner iOS generation and checks on a Mac.

## End-of-run CI correction — 2026-09-23

PR #265 is the single delivery PR to `master`; #264 remains open. The first iOS CI run stopped at
SwiftFormat's `blankLinesAtEndOfScope` in `BookingSuccessViewModelTests`. The correction removes one
blank line only; `git diff -w --ignore-blank-lines -- src` is empty. On a temporary LF copy of the
tracked iOS sources, pinned SwiftFormat 0.60.1 passes all 878 checked files. Pinned Linux SwiftLint
0.65.0 exits zero but skips the two SourceKit-dependent rules (`custom_rules`, `statement_position`);
the macOS CI gate remains authoritative. The iOS-symbol and booking-policy checkers pass again.

The full graph was rebuilt after the formatting correction: 6,185 files, 55,569 nodes, 80,543 edges,
4,535 communities; the same 484 schema warnings and optional 64-node question-ranking sample apply.
The report also now distinguishes the original one-time-token proposal from shipped reusable links,
subscription-update handling from an `invoice.paid` writer, and retained VAT configuration from the
current non-payer cleaner invoices. No application behavior changed in this correction.

The audit is re-pinned to `865549b026b02602d11648a3a7e9a12aa2878198` after this final source edit.
The unchanged citation checker reports 1,706 citations, 199 without an identifier and zero unresolved;
all word budgets pass, with the refreshed AUDIT-LOG at 1,234 words. Final CI status is recorded on PR #265.
Owed work remains the DEV-only drop for `20260923071814_Initial` immediately before the next manual
DEV deployment and the combined owner Mac generation/build/test checklist above.

## Blocked — needs a ruling before any code

Nothing below is planned. Each names the decision that blocks it; the decisions live in the audit set
(`lawyers-docs/analyza-2026-09-22/ROZPORY-DOKUMENTY-VS-KOD.md` §3 and `PODKLADY-KOLEGU-VS-KOD.md` §9).

| Finding | Blocked on | The question in one line |
|---|---|---|
| Crew pays N times one flat price | **R26** | Does the pay config describe the job or one cleaner? |
| Cash settlement: no amount ledger, set-off or saldo; cancellation fee never collected | **R6**, R27 | Eligibility restriction decided 2026-09-24 and planned below; who owns/settles the remaining eligible cash is still open |
| The stored card as a guarantee for storno, no-open, unpaid cash | **R27** | May we charge a card the customer saved for a different purpose? |
| The draft legal texts seed into production | **R3** | Gate the seeder and ship with no texts (orders refuse without an in-force work contract), or ship the drafts? |
| `Tenants` has no writer; the default-market company cannot be deactivated | **owner action** (R2's regional-company model is already ruled) | Who creates an operating company, with what identity data? |
| Lifecycle notices have channel and audience gaps | **R18** | Which missing notices become events, for whom and through which channel? Successful admin-cancellation and dispute refunds already notify registered customers; there is no verified ten-event total |
| The cleaner's access to the address after completion | **R8** | Does it end, and when? |
| `AdminReassignOrder` checks neither approval nor currency | **R11.6** | May an administrator place a cleaner who could not have taken the job? |
| The cookie banner writes `DataProcessing` / `MarketingEmails` | **R9** | What is the banner actually recording, and does marketing exist? |
| Fiscalisation: finish or delete | **R21** | Is it required for any market we are opening? |
| The price model: m², hours, hourly pay, VAT registration, price-list structure | **R22**–**R25** | The colleagues' model against the catalogue in the code |
| Placeholder company identity (IČO, DIČ, bank, name ×3) | owner action | Real values before the first production receipt |
| Mapbox token in the APK and in git history | owner action | Rotate and restrict (already `MS-3`) |
| PRO Postgres public endpoint, private networking commented out | infra ruling | Before PRO carries data |

---

## Owner questions raised while fixing (not blocked rows — each came out of a phase)

| From | Question | What the code does until you answer |
|---|---|---|
| 3.2 | The paid cash receipt reaches a signed-in customer through the app's download, but a **guest** cannot download receipts and nothing **e-mails** the paid copy — so a guest and every inbox still hold the "awaiting payment" PDF. Send a second receipt e-mail at collection (a new customer message, R18 territory), give guests a receipt download keyed on their access token, or accept it? | The stored receipt is rewritten to Paid under the same number; nothing is sent |
| 3.2 | Restating the receipt adds a second key formula to the receipt queue (`receipt-reissue:{order}`); ADR-0002 declares that queue's key table frozen ("changing one is a superseding ADR"). Amend ADR-0002, or route restates through a queue of their own? | The second key is in place; ADR-0002 is not amended |
| 3.2 | Which date does a cash receipt restated as Paid carry — the booking-time issue date it keeps today, or the date the cash was collected? A tax-adviser question | Keeps the booking-time issue date |
| 3.3 | Every receipt header prints `CompanyInfo.Tagline`, seeded in English only ("Professional…"). Drop it from the receipt, give it per-language values, or seed Czech copy with the real company identity? | Prints the English tagline |
| 3.1 | The booking wizard's quote reports discounts unrounded, so on a fractional currency the discount shown can differ by one cent from the one charged. Rounding it changes the booking contract (the client echoes the quote back) | Quote unrounded, charge rounded |
| 4.5 | Must `retention.admin_audit.years` and `retention.employee_audit.years` have a minimum of three years, rather than allowing a company to shorten them to one? | Default three years; catalogue permits one |
| 4.4 | Accept deletion on the first weekly run after the seven-day window, with no photos for a later dispute or a chargeback 120+ days later, and no photo sweep for never-completed orders? | First weekly run strictly after the seven-day cutoff (usually 7–13 days, exact cutoff equality can reach 14); recorded unresolved disputes hold photos; never-completed orders are outside the rule |
| 2.3 | Web card chargebacks are silently dropped because Checkout does not store the payment intent used by the webhook lookup. Fix this now or in later work? | Current web Checkout and chargeback lookup unchanged |
| 6.2 | Resolved 2026-09-24: standard grace 15 minutes; entitled Plus grace 60 minutes, not a first-customer benefit. Implementation plan below. | Not implemented yet: assessor still selects 15 minutes for everyone |
| 6.3 | Must API bookings enforce the apps' 08:00–19:45 local start times on a 15-minute grid, or should these remain client-picker limits? | No new API restriction; `FirstWindowHour` / `LastWindowHour` remain the live client-parity authority; only the unused duration is a mechanical deletion |
| 6.4 | Should the retained invoice-rate configuration be unified with the market rate for customer sales? R16's cleaner non-payer model remains decided; VAT-paying cleaners are hypothetical. | Sales use `CountryConfiguration.StandardVatRate`; invoice architecture retains `CountryInvoiceConfig.VatRate`, but current cleaner invoices carry zero VAT because `IsVatPayer` is false. The unused reduced rate was removed. |
| 6.9 | Does monthly mean the same calendar date (with what rule for the 29th–31st), a chosen weekday occurrence each month, or a fixed interval? | The materializer adds 30 days then realigns to the chosen weekday, usually yielding 35-day intervals; relabelling it as every 30 days would also be false |

## How a phase runs, when it runs

One branch (`fix/audit-findings-2026-09-22`) and one PR to `master` for this whole run, with locally
verified phase commits (owner ruling supersedes one branch per phase). Per phase:

- `dotnet test` on **all three** backend projects (`Cleansia.Tests`, `Cleansia.IntegrationTests`,
  `Cleansia.HostTests`) — one at a time, not in parallel.
- A schema change means the single `Initial` migration is regenerated and the DEV drop is owed at the
  next DEV deploy, not at regen time.
- A DTO or endpoint change means NSwag regeneration **before** the frontend work, committed in the
  same change, and a mobile spec re-dump when a mobile host is touched.
- iOS work is batched so it compiles in one sitting on the Mac.
- `docs/` is updated at the end of the phase, once it is green — not alongside the code.
- CI is **not** a gate between phases (owner, 2026-09-22): each phase is verified locally, committed, and
  the next one starts; CI is read once at the end and anything red is fixed in one pass.

## What this plan does not do

The defect phases do not invent business policy or implement the colleagues' proposed business
model. The separately authorized post-phase work below re-pins the lawyer documents and adds the
visual summary. Unanswered choices remain open.

## After the phases (owner rulings, 2026-09-22)

1. **The audit folder comes onto this branch.** `lawyers-docs/analyza-2026-09-22/` lives only on
   `docs/lawyers-audit-2026-09-22` (PR #264, unmerged); that branch is merged into this one, and this
   PR carries both. #264 is then redundant.
2. **The five audit documents are re-pinned** to the head of this branch once every phase is in: the
   same folder and names, every fact and citation re-verified against the post-fix tree, the citation
   checker run against the new commit, and the audit log gains a second delta table (`6792f0c2` → the
   new head) so the lawyers can see what the fixes changed.
3. **A new business summary** for the lawyers and the partners: Czech, PDF + HTML in the same folder,
   written for a non-technical reader from the business side — what the platform is, who does what,
   how money and data move, with diagrams and tables and **no code references** — ending in the
   crucial points and the open questions to take through with colleagues.

## Owner policy changes — 2026-09-24 (analysis and execution plan only)

**Baseline:** `e4b46a486`, branch `fix/audit-findings-2026-09-22`. No implementation, tests, commit,
push or deployment is part of this planning pass. Preserve the completed audit as the description of
that baseline until the new behavior ships. This section is the only execution index; no new tickets.

**Execution authorized (owner, 2026-09-24).** The four changes are implemented in numbered order on the
same branch and PR #265; progress, deviations and findings are recorded under
[§ Execution log](#execution-log--2026-09-24). Legacy-booking treatment for cash is settled by the
owner: existing confirmed bookings are preserved; an ineligible recurring template must be corrected
by the customer; nothing is switched to card silently and no saved card is charged.

### Decisions and interpretation

| Change | Planned behavior |
|---|---|
| Cash eligibility | Cash only for a signed-in customer when the server determines exactly one cleaner is required. Guest OR required crew > 1 means card only. |
| Cancellation grace | 15 minutes after booking for standard customers, including guests and first-time customers; 60 minutes for entitled Plus members. Owner corrected the original “60 hours” to **60 minutes**. |
| Deleted-account credit | Interpret as forfeiting unused credit on completed account deletion, without payout, and preventing subsequent credit restoration to that erased account. Actual card refunds remain separate. |
| Cleaner travel pay | Remove the distance-based component from future cleaner-pay calculations. Cleaners are not paid by stored travel kilometres. Other rates, bounds and the existing per-cleaner crew calculation remain unchanged. |

Preserve cancellation's other rules: no fee without an assigned cleaner, ordinary 24-hour/4-hour
notice thresholds and 25%/50% fees, the existing Plus notice benefit, and refusal once cleaning has
started. The new 15/60-minute rule changes the grace period, not those notice thresholds.

### 1. Restrict cash across booking entry points

**Current:** `CreateOrder.cs:92` checks the payment enum, without this eligibility rule.
`OrderFactory.cs:287` computes staffing; `Order.cs:868` sets required crew to
`ceil(EstimatedTime / 120 minutes)` (at least one for the zero-duration fallback).
`QuoteOrder.cs:455` exposes the same required count. Use **RequiredEmployees**, not assigned crew or
MaxEmployees: spare seats do not mean a second cleaner is required.

- Enforce the authenticated/required-crew rule server-side in one-off creation, recurring-template
  create/update, occurrence materialization and `ConfirmRecurringOrder`. Reuse the authoritative
  duration calculation; do not trust a client count or introduce a second pricing calculation.
- Return a translated business error before persisting/debiting credit/dispatching payment. Add the
  factory backstop where needed so recurring/internal creation cannot bypass the rule.
- Customer web, Android and iOS: disable cash with a clear reason, re-evaluate after login/logout or
  any size/service/package/extra change, and invalidate an already-selected cash option. Cover
  recurring authoring/editing too. Existing quote count can supply the UI; add DTO fields only if
  needed, then regenerate affected clients/specs.
- Rollout default: preserve existing confirmed bookings. Do not silently convert old recurring cash
  templates or charge a saved card; require the customer to change an ineligible template before
  another occurrence is created/confirmed. Account for already-materialized unconfirmed occurrences
  with a recoverable edit/rebook path. Confirm this legacy-booking treatment before implementation
  if the intention is to prohibit already-booked cash jobs too.
- Tests: guest/account × required crew 1/2; 120/121-minute boundary; one required cleaner with spare
  capacity; forged requests; selection becoming invalid; recurring entry points and legacy templates;
  no partial order, credit debit or payment side effect on rejection.

**Scope boundary:** this does not fix cash settlement, card guarantees, or the separately reported
recurring-cash premature-Paid defect. The latter still affects eligible one-cleaner cash occurrences.

### 2. Make cancellation grace membership-based

**Current:** `BookingPolicy.cs:80–83,325–354` has 15-minute standard and unused 60-minute first-time
grace. `CancellationAssessor.cs:33,49–62` always passes first-time=false. Separately,
`CancellationPolicyResolver.cs:17–44` resolves membership's **hours before cleaning**; current Plus
copy describes four hours instead of the ordinary 24 hours.

- Replace the dead first-time branch with an explicit resolved grace duration: 15 or 60 minutes.
  Reuse the paid, current membership entitlement predicate; guest, expired, paused and PastDue
  memberships receive 15. Keep grace minutes distinct from `FreeCancellationWindowHours`.
- Use the same resolved policy in cancellation execution and previews, including guest routes;
  update booking/cancellation audit evidence and policy figures so the applied grace is recorded.
  Keep existing live-entitlement resolution unless a separate booking-time snapshot policy is chosen.
- Update web/mobile cancellation explanations, Plus benefits, FAQs, all five locales, policy-parity
  checks and tests. Remove first-customer claims/arguments; preserve persisted enum numeric values
  and historical audit readability. Regenerate contracts only if a public shape changes.
- Tests: exactly 15/60 minutes and immediately after; first-time versus returning account; guest;
  entitled versus non-entitled Plus; membership changing between preview and cancellation; identical
  preview/charge at a fixed time; unchanged unassigned, notice-tier and in-progress behavior.

### 3. Forfeit credit when deletion completes; prevent its return

**Current:** `GdprDeletionService.cs:152–163` blocks deletion for positive credit. It does not
automatically pay that balance out. `CreditUnwind.cs:35–54` returns applied credit and
`CreditAccountRepository.cs:57–99` can recreate an account during a return, without an erased-user
guard. `RefundService` returns card and credit shares separately. Erasure anonymizes and deactivates
the User (`GdprDeletionService.cs:556–557`); ordinary inactivity alone must not mean erasure.

- Replace only the positive-credit deletion refusal with an explicit, idempotent write-off of all
  the subject's currency balances when erasure succeeds. Reuse existing ledger/write-off machinery
  where it fits; keep balance and ledger reconciled, with a recorded deletion reason. Preserve all
  other live-order, assignment, invoice and unsettled-cleaner-pay blockers.
- Apply to self-delete, admin-delete and retry through their shared erasure service, including the
  existing frozen-company path. Failed or refused deletion must not forfeit credit.
- Guard the shared return path before account creation and make it safe against concurrent erasure.
  Trace refund retries, checkout failure/expiry compensation, cancellation, disputes and credit-grant
  writers so none restores spendable credit after erasure. Use the existing erased identity semantics
  if sufficient; do not add a new schema flag without proving it necessary.
- Preserve refunds of money actually charged to a card. Suppressed credit must not be converted to
  extra cash refund, reported as successfully returned, or produce endless retries. Preserve retry
  idempotency and accurate refund/accounting evidence for this new case.
- Add a clear forfeiture warning to the existing account-deletion confirmation on customer clients;
  update admin error/help copy, five locales and the previous debt-blocking documentation/ruling.
- PostgreSQL tests: zero/positive balances, multiple currencies, self/admin/retry, blocked/failed
  deletion, repeated requests, ledger reconciliation, late/repeated mixed card-credit refunds,
  concurrent return versus deletion in both orders, and no forfeiture merely for an inactive user.

### 4. Remove distance-based cleaner pay

Owner ruling, 2026-09-24: remove `(TravelDistance or 0) * DistanceRatePerKm` from the calculation.
The planned formula for each cleaner is:

```text
raw = SUM over selected service/package configurations of:
      BasePay
    + max(Rooms - 1, 0) * ExtraPerRoom
    + Bathrooms * ExtraPerBathroom

pay = raw, raised to the highest positive MinimumPay,
           then capped at the lowest positive MaximumPay
```

- Update both `PayCalculatorExtensions.CalculatePay` paths and `CalculateOrderPay`; newly calculated
  automatic travel/expense pay is zero regardless of stored distance or an old kilometre rate.
  Trace recalculation and pay breakdowns so they cannot reintroduce a distance component.
- Retire active kilometre-rate configuration from admin inputs, commands and seed data as appropriate;
  update affected DTOs/generated clients and translations if their contract changes. Preserve stored
  historical pay rows, approved invoices and required readers; do not retroactively recalculate them.
  Retaining unused legacy columns is preferable to assuming a destructive schema cleanup is required.
- Keep service/package rate selection, room/bathroom additions, minimum/maximum bounds, currency and
  the full calculation for each crew member unchanged. This ruling does not authorize unrelated
  changes to manual pay adjustments or other expense types.
- Tests: identical new pay for null/zero/nonzero distance with a nonzero legacy kilometre rate;
  both calculator paths; services plus packages; cleaner-specific overrides; floor/ceiling cases;
  persisted new pay has no distance component and historical rows/invoices remain unchanged.
- Update cleaner-pay documentation and customer/partner/admin copy that promises travel pay; refresh
  the audit and Czech summary after implementation. No retrospective backfill is planned.

### Current cleaner-pay formula (baseline, before the planned removal)

For **each assigned cleaner**, choose the order-currency rate for each distinct selected service and
package; a cleaner-specific rate overrides its shared rate. For the resulting configurations C:

```text
raw = SUM over C of:
      BasePay
    + max(Rooms - 1, 0) * ExtraPerRoom
    + Bathrooms * ExtraPerBathroom
    + (TravelDistance or 0) * DistanceRatePerKm

pay = raw, raised to the highest positive MinimumPay,
           then capped at the lowest positive MaximumPay
```

Zero bounds mean no floor/ceiling; contradictory bounds are rejected. Room, bathroom and distance
additions apply for every selected configuration. Ordered extras, customer discounts, express
surcharge and actual time worked do not enter this formula. Each cleaner gets the **full formula**
at their own rates; it is not divided by crew size. That existing crew-pay decision remains R26.

Source anchors: `PayCalculatorExtensions.cs:30–107`, `EmployeePayConfigRepository.cs:63`,
`CalculateOrderPay.cs:141`, `CompleteOrder.cs:346`, `OrderEmployeePayEntityConfiguration.cs:28`.
The calculator uses decimal arithmetic without an explicit final round; stored amounts use 18,2
precision. Display-only F2 formatting is not the calculation's rounding rule.

### Execution and verification

Implement in the numbered order above, with focused backend/PG tests before each client follow-through.
Then update business rules, booking/cancellation/credit/GDPR/payroll flows, affected ADR notes and legal/help
copy; after the last code change, re-pin the audit and refresh the Czech summary's cash, cancellation
deletion and cleaner-pay sections. Do not present proposed rules as already live in the current PDFs.

Run checks serially: three backend suites; full affected client/parity coverage and web builds;
Android tests; iOS generation/build/tests on Mac; repository checkers, docs build and graph rebuild.
Use capped workers, explicit-path commits and one final CI pass. No production action.

Existing deployment prerequisites remain: the DEV-only drop for `20260923071814_Initial` immediately
before the next manual DEV deployment (also reseeds perks), and the combined owner Mac checklist.
These policy edits need no assumed migration; if implementation actually requires schema changes,
regenerate Initial and update that DEV prerequisite to the resulting migration ID. This planning pass
leaves the currently green PR #265 unchanged and does not close #264.

### Execution log — 2026-09-24

| # | Change | State | Commit |
|---|---|---|---|
| 1 | Cash eligibility — backend and contracts | done | `a261cc226` |
| 2 | Cancellation grace — backend and contracts | done | `0ed55136f` |
| 3 | Credit forfeiture on deletion — backend | done | (this commit) |
| 4 | Distance pay removal — backend and admin contract | pending | — |
| C | Client follow-through (web, Android, iOS) | pending | — |
| D | Documentation, audit re-pin, SHRNUTI | pending | — |

**Change 1 as shipped.** `BookingPolicy.AllowsCash(signedIn, requiredEmployees)` is the one rule;
`OrderDuration.RequiredEmployees(minutes)` is the one crew formula (the copies in `Order` and
`QuoteOrder` now call it). Enforced in the `CreateOrder` validator's price chain (before the express
slot, credit debit, referral accept or any dispatch), recurring create/update validators, a factory
backstop, the materializer (an ineligible legacy cash template is skipped, not switched, and the list
DTO gains `RequiresPaymentMethodChange`) and `ConfirmRecurringOrder` (refuses an ineligible cash
occurrence; recovery: cancel the unconfirmed occurrence — free while nobody has taken it — and move the
template to card or a one-cleaner selection). Error key `order.cash_not_available`. The quote already
carries `RequiredEmployees`; the customer web client and the customer mobile spec were regenerated for
the one new DTO field. No schema change.

**Change 2 as shipped.** The backend keeps its "oops window" name for the grace (because "grace"
already names the cleaner's `StartGraceWindowMinutes`): `OopsWindowMinutesStandard = 15`,
`OopsWindowMinutesPlus = 60`; the dead first-time constant, parameter and assessor constant are gone.
`CancellationPolicy` carries the resolved `OopsWindowMinutes`: 15 for guests and every non-entitled
account, 60 for an entitled Plus member (the paid-entitlement query; independent of the plan's
`FreeCancellationWindowHours`, which still overrides only the notice window). Every route — member and
guest cancel, both previews, booking evidence — resolves it through `ICancellationPolicyResolver`, live
at each call. Evidence: cancellation rows gain `oopsMinutesApplied`, `policyFigures.oopsMinutesFirstTime`
is renamed `oopsMinutesPlus` (old rows keep the old key), booking evidence gains
`cancellationPolicyShown.oopsMinutesForThisCustomer`. One additive contract field,
`GetCancellationFeePreview.Response.OopsWindowMinutes`, regenerated into the customer web client and the
customer mobile spec. No schema change.

**Owed to lane D from change 2 (docs):** ADR-0062 D3 table (~:298 CancelOrder row lists
`oopsMinutesFirstTime`, lacks `oopsMinutesApplied`; ~:299 CreateOrder row lacks
`oopsMinutesForThisCustomer`) — a dated note recording the key rename; `business-rules.md` "The oops
window" (~:104-111) and `flows/cancellation-refund-dispute.md:24` still describe a dormant 60-minute
first-time window; the `business-rules.md` audit table (~:1070) should name the applied minutes.

**Change 3 as shipped.** The positive-credit deletion refusal and its error key are gone. A successful
shared erasure (self, admin, retry, frozen company) drains every positive currency account and records
one `Expired` ledger movement under `account-deletion:<account id>` with the deletion reason; refused,
deferred or failed deletion keeps the balance. The owner lock (`Users … FOR NO KEY UPDATE` plus the
subject's `CreditAccounts` rows) is taken only by `ForfeitCreditAsync`, after the Stripe cancel and the
blob deletes, and held to the pipeline commit; the unit of work rolls an owned lock transaction back on
failure. Credit returns, grants and expiry check the owner under the same lock: an erased (anonymized and
deactivated) subject gets no return, grant or new account; plain inactivity still receives credit.
Suppressed credit records no returned amount, changes no card share and does not fail a card refund. The
expiry sweep locks one owner at a time. No schema or public contract change. Reviewed adversarially;
Cleansia.Tests green apart from the then-unfinished change 4, IntegrationTests 655, HostTests 347; twelve
revert mutations each caught by a test. The dead `gdpr.deletion_blocked_by_credit_balance` copy left all
three web apps with the constant.

**Found while executing, reported, not fixed:**

- Change 3, **money, pre-existing**: `CancelUnfilledOrders` returns a Paid card order's applied credit
  twice — once as the refund's credit leg (`RefundService`, key `credit-return:refund:{id}:admin`), then
  again through `ReturnUnpaidOrderCreditAsync` (`order-ended-unpaid:{id}`) — so the customer gains
  2 × `CreditAppliedAmount`. The sibling paths guard it (`PaymentStatus != Paid && !refundInitiated` in
  `PlatformOrderCancellation` and `CustomerOrderCancellation`); pin test: a 2000 order with 500 credit
  ends with exactly one 500 return. Needs a go-ahead.
- Change 3, pre-existing: in one unit of work a tracked grant/drain followed by a raw `TryReturnAsync` on
  the same account can be overwritten by the tracked absolute balance at commit (`CancelUnfilledOrders`
  mixes both for one customer).
- Change 3: credit suppressed because the owner was erased is indistinguishable from an idempotent replay
  (`TryReturnAsync` returns false in both); money and ledger are correct, the evidence is not explicit.

- Change 2: `GetMyMembership` (what clients read to show Plus state) uses the ACTIVE predicate
  (tenant-filtered, counts a running trial), not the ENTITLED one the resolver uses; they agree today
  (one tenant, trials refused) but can diverge. Web `order-detail.component.ts:368-369` shows
  `freeCancellationWindowHours ?? 24` from `hasMembership` with no entitlement check.
- Change 2: `UserMembership.cs` comments (~:145-150 on `IsActive`, and the `TrialEndsAtUtc` doc saying the
  cancellation window is not withheld during a trial) contradict the entitlement query every cancellation
  benefit reads. Pre-existing.
- Change 2: a materialized recurring occurrence's oops window runs from materialization (60 minutes for a
  member), usually masked by FreeNotAccepted; `RecurringOccurrenceConfirmationEvidence` records no
  cancellation policy.
- Change 2: `CreateOrderAuditEvidenceTests` still has a handler-level guest-cash booking, a state change 1
  forbids (the validator is skipped at that level).
- Change 2: `BookingPolicy.CalculateCancellationFeeRate` has no production caller (tests only).

- Change 1: no client's recurring *update* sends `preferredEmployeeId` (web facade, iOS
  `RecurringBookingClient`, Android `RecurringBookingDtos`), and `UpdateSchedule` then clears it; web
  also sends `endsOn` undefined. The very edit the cash rollout asks for — moving an ineligible template
  to card — silently drops the preferred cleaner (and on web the end date). Pre-existing.
- Change 1: `SendRecurringOrderReminders` still asks the customer to confirm an ineligible legacy cash
  occurrence that the confirm now refuses; and a customer who never opens the recurring list is not
  told the template stopped producing occurrences (no notification added — R18).
- Change 1: `MarkCashCollected` lets a cleaner take cash on an unsettled card order, so cash can still
  land on a guest or multi-cleaner order whatever the booking-time rule says (R6/R27).
- Change 1: the seeded customer terms of service (all five locales, line 19) still offer cash on
  delivery unconditionally (R3 — draft legal texts).
- Change 1: if `Stripe:Enabled` were ever false, guests and multi-cleaner bookings could not pay at all.
- Change 1: stale comments — `ConfirmRecurringOrder` and both customer `OrderController` summaries say
  cash "flips to Confirmed" (tied to the out-of-scope premature-Paid defect); `OrderDuration.cs:10`
  and `GetMyServingCleaners.cs` cite a non-existent `OrderDurationAgreementTests`; a stray doc comment
  at `QuoteOrder.cs:61-66`.
