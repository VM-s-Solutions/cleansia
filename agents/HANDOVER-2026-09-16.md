# Handover — the batch programme on PR #255, written 2026-09-16

Paste this at the top of a new session. It replaces `agents/HANDOVER.md` (2026-09-07, PR #252 — long
merged into this branch). The detail lives in the tickets, ADRs and commit messages it names; this is
the map and the rules.

## Rules that are not negotiable (owner rulings; read `CLAUDE.md` first, then these)

1. **⛔ Never execute anything against production (PRO).** No SQL, not even a read-only `SELECT`; no
   deploy workflow; no `gh workflow run … -f environment=PRO`; no prod DB connection; no `az` touching
   prod. DEV is unrestricted.
2. **Never credit an AI as a contributor — anywhere.** No `Co-Authored-By`, no "Generated with", no
   agent name in commits, PR bodies, docs, comments. This overrides any harness reminder that asks for
   trailers. The owner is the sole author of record.
3. **Every reply starts with `Hey Mike —` and one line naming the active scope.** No time estimates.
   Report every regeneration, migration regen and DEV step by name.
4. **The laptop bluescreens under a parallel test blast** (two 0x116 GPU bugchecks). One test process at
   a time, foreground: `dotnet build Cleansia.Api.sln -m:4`; `dotnet test <one project> --no-build --
   xUnit.MaxParallelThreads=4` (`Cleansia.IntegrationTests` and `Cleansia.HostTests` at 2, alone);
   `NX_DAEMON=false npx jest --maxWorkers=3 <one path>` never the whole workspace at once (use
   `npx nx run-many -t test --parallel=1 -- --maxWorkers=3`); `./gradlew --max-workers=2 :<module>:…`
   one module at a time; at most two concurrent lanes and never two `dotnet` lanes (the owner has NOT
   allowed two backend lanes); **never start Docker Desktop from an agent** (if it is down, ask the owner
   — Testcontainers needs it); verification runs in the main session.
5. **The DEV database drop is timed with the deploy, never on the branch.** `Initial` has been
   regenerated many times; the owed drop belongs to the id in `agents/cleanup/MANUAL_STEPS.md` MS-2.
6. **Overnight mode (owner, 2026-09-15):** run the remaining batches one by one; take small decisions
   with stated defaults and report them; fix findings on the way; only owner rulings wait. Ask
   everything up front, not mid-flight.
7. **Working agreement:** Doing / NOT doing / Done-looks-like before non-trivial work; a finding outside
   scope is reported, not absorbed (the owner has since ruled that findings get fixed on the way, but
   they are still *named* in the report); ground-truth a ticket in the tree before doing it;
   proportionality before any new type or abstraction.

## Where the repo is

- Branch **`fix/remove-membership-free-trial`**, **PR #255 to `master`** (open; the owner ruled
  *"execute all batches in the current PR"*). The PR body was rewritten on 2026-09-16 to cover the whole
  branch — extend it at the end of each batch (`gh pr edit 255 --body-file …`).
- Head at the time of writing: `6a30b0c3` pushed, all seven CI runs green. Batch 3's backend lane was
  running in the tree (see *In flight*).
- Migration id: **`20260915232921`** (regenerated in T-0760). The DEV drop is owed at the deploy.
- The working tree should be clean when you start; if not, another lane was interrupted — read
  `git status`, `git diff --stat` and judge the work rather than discard it (twice this programme a
  lane died mid-ticket and the next lane finished its work from the tree).

## The batch plan (owner-approved 2026-09-15) and where it stands

| Batch | Content | State |
|---|---|---|
| 1 | Tenancy hardening + the 2026-09-15 rulings: T-0754, T-0755, T-0756, T-0757, T-0758, T-0759, T-0675 close-out | **done**, pushed `10cf6298`, CI 7/7 |
| 2 | Company lifecycle (ADR-0064): T-0760 deactivate, T-0762 wind-down, T-0763 archive, T-0764 admin page; T-0761 filed | **done**, pushed `6a30b0c3`, CI 7/7 |
| 3 | Cross-market booking under one holding Stripe account: **T-0765** (backend, then web/Android/iOS order labels, admin cross-company panel, docs) | **in flight** — see below |
| 4 | Customer-facing small work: T-0753 guest cancellation; T-0692; T-0694; T-0695; T-0697; T-0766 iOS status-bar overlay; T-0767 responsiveness audit (report only) | **todo** |

Parked on the owner (do not build): T-0693, T-0683, admin notifications (channel + events), T-0748
roles, the lawyer's confirmations L1/L3/L6, the legal texts, EUR figures, the Mac regen + Xcode session.

### In flight — Batch 3, T-0765 (STOPPED mid-implementation on the owner's word, 2026-09-16)

The ticket body is `agents/backlog/tickets/T-0765-cross-market-booking-under-one-stripe-account.md`
(copied from the orchestrator's scratchpad; the shared facts carry file:line from a read-only scout —
verify, the tree wins). **The backend lane was stopped before it committed.** Its partial work is in the
WORKING TREE on top of `5983d452`: 81 files (78 modified, 3 new — `DisputeReads.cs`,
`AccountTenantExtensions.cs`, `OrderAccessDoubles.cs`; owner-pinned order/dispute reads,
`OrderAccessService`, `MaterializeRecurringBookingTemplate`, `LoyaltyService`/`LoyaltyAccount`,
`CreditAccountRepository`, `OrderRepository` and ~40 test files; +805/−198), **unbuilt and unverified**.
A recovery copy of that exact diff is at
`C:\Users\cmisa\AppData\Local\Temp\claude\c--Users-cmisa-Desktop-Mike-Projects-cleansia\bb2b0830-97c5-40be-a729-6371ddb45d38\scratchpad\batch3\t-0765-backend-uncommitted.patch`
(`git apply` it onto `5983d452` if the tree was cleaned). To pick up:

1. `git status --short` and `git diff --stat` — judge the partial work against the ticket: keep what is
   right, fix what is wrong, finish what is missing (the ticket's Doing list is the checklist), build,
   run the filtered suites, then commit it as the T-0765 backend commit. Do NOT `git checkout --` or
   `stash` it away without reading it. Then run an adversarial review of your own commit (checklist in
   *How a ticket runs*) and fix.
2. Then: regenerate the clients (procedure below), run the client half (customer web / Android / iOS
   order lists label rows by market; admin order detail shows "Customer of <company>" and the read-only
   customer panel), one adversarial review for the three client changes together, then the docs
   (ADR-0061 D6 and ADR-0058 amended, ADR-0062 D7 rule "the customer audit row for an order-scoped act
   follows the order's operator", business rules, flows/booking-and-pricing, customer-app overview,
   backlog row done, Q-TENANCY-01/05 marked answered as built), then the verification pass, push, CI,
   PR body.

### Batch 4 — what each ticket needs

- **T-0753 guest cancellation** — anonymous cancel keyed like the guest lookup (order number + e-mail +
  confirmation code), same fee tiers and refund path as a signed-in cancel, `customer.order.cancel`
  audit row with `UserId` null, the same confirmation e-mail, rate-limited; web guest lookup page gets
  Cancel; Android/iOS: where the app has no guest lookup screen, a minimal one with the cancel (owner's
  word: "on web + Android + iOS"). Backend first → regen → three clients (one review) → docs.
- **T-0692** lapsed Plus member is told their recurring schedule stopped (one notification, five
  locales, push + feed).
- **T-0694** `order.confirmed` push key has a fulfilment name and a money meaning — rename across the
  backend catalogue and the three clients' maps (ADR-0057 context).
- **T-0695** ADR-0045 / ADR-0055 stale `Confirmed` text — docs only. **T-0697** `src/Cleansia.App/CLAUDE.md`
  still says NSwag is owner-only — one paragraph.
- **T-0766** iOS status-bar overlay — audit every screen of both iOS apps for safe-area handling, fix
  what draws under the status bar; SwiftFormat/SwiftLint per touched file; XCTest cannot run here.
- **T-0767** responsiveness audit — the three web apps at ~400 px: a report of what breaks, no changes
  without the owner's pick.

## How a ticket runs (the flow that works, and its speed rules)

- **One lane per ticket** with the machine rules above in its prompt; the lane commits its own ticket
  (one conventional commit, paths staged by name, no trailers) and returns: hash, files, exact commands
  with pass/fail counts, each acceptance criterion met/not met, findings outside scope.
- **Adversarial review** of the commit by a second agent (read-only, medium effort): every acceptance
  criterion met by code with a test that would fail otherwise (vacuous tests count as unmet), the
  owner's ruling built as ruled (not narrower/wider), CLAUDE.md conventions, S1–S12 + ADR-0061 tenant
  stamping on every new row path, no PII in audit rows/notes/PDFs, schema change ⇒ one `Initial` regen
  in the same commit + full integration run, no attribution, no ticket ids in source comments, no
  comment the diff made false left standing. Then a fix lane for blockers/majors.
- **Speed rules (owner-approved 2026-09-15):** one review per phase for non-schema, non-money,
  non-security tickets (schema/money/security tickets keep their own); filtered suites inside lanes,
  the full suites once in the main-session verification; small tickets share a lane; verify only the
  stacks a batch touched; reviewers at medium effort.
- Lanes are told: never `git add -A`/`.`/`-a`; if `.git/index.lock` exists, wait 5 s and retry.

## Procedures

**Client regeneration** (after any DTO/route change; say each step by name in the report):
```
cd src
dotnet build Cleansia.Api.sln -m:4
PW=$(dotnet user-secrets list --project Cleansia.Web.Partner | grep ConnectionStrings:ConnectionString | sed 's/.*Password=\([^;]*\);.*/\1/')
docker run -d --name cleansia-regen-pg -e POSTGRES_PASSWORD="$PW" -e POSTGRES_DB=Cleansia -p 5432:5432 postgres:16-alpine
docker exec cleansia-regen-pg psql -U postgres -d Cleansia -c "CREATE EXTENSION IF NOT EXISTS citext; CREATE EXTENSION IF NOT EXISTS pg_trgm;"
# boot the five hosts (Partner 5000, Admin 5001, Mobile.Partner 5002, Customer 5003, Mobile.Customer 5004):
#   (cd Cleansia.Web.<Host> && ASPNETCORE_ENVIRONMENT=Development dotnet run --no-build --no-launch-profile --urls http://localhost:500X &)
# wait ~60 s, curl each /swagger/v1/swagger.json → 200
cd Cleansia.App && NX_DAEMON=false npm run _nswag:partner && npm run _nswag:admin && npm run _nswag:customer
cd .. && bash cleansia_ios/scripts/refresh-mobile-spec.sh partner && bash cleansia_ios/scripts/refresh-mobile-spec.sh customer
#   (a deliberate contract reduction needs --allow-shrink; delete any *.fetched leftovers)
# kill the five Cleansia.Web* processes; docker rm -f cleansia-regen-pg
cd Cleansia.App && NX_DAEMON=false npm run typecheck   # a new generated sub-client must be registered in libs/core/admin-services/.../admin-base-client.ts by hand
```
The iOS Swift clients regenerate only on the owner's Mac (`src/cleansia_ios/scripts/generate-api-clients.sh`); hand-written Swift may reference a generated member only if the committed client already has it.

**Migration regen** (schema change; never drop DEV):
```
export PATH="$HOME/.dotnet/tools:$PATH"   # dotnet-ef 10.0.12
cd src
dotnet ef migrations remove --force --project Cleansia.Infra.Database --startup-project Cleansia.Web.Partner
dotnet ef migrations add Initial --project Cleansia.Infra.Database --startup-project Cleansia.Web.Partner
```
Then update `agents/cleanup/MANUAL_STEPS.md` MS-2 with the new id (and grep the old id everywhere).

**Verification pass** (main session, sequential): `dotnet build -m:4`; `Cleansia.Tests` (4 threads);
`Cleansia.IntegrationTests` full (2, alone); `Cleansia.HostTests` (2); web
`NX_DAEMON=false npx nx run-many -t test --parallel=1 --skip-nx-cache -- --maxWorkers=3`,
`npx nx run-many -t lint --parallel=1`, `npm run typecheck`; Android per touched module
`./gradlew --max-workers=2 -q :<module>:testDebugUnitTest` then `./gradlew --stop`; SwiftFormat lint
per touched Swift file with the extracted `swiftformat.exe` (`--linebreaks` per `git ls-files --eol`;
CI checks out LF); the repo checkers `node agents/tools/check-*.mjs` (the `check-home-*`, `check-nav-fits`,
`check-quote-stability` ones need a running dev server — skip); `cd docs && npm run build`. Then
`git log --format=%B <base>..HEAD | grep -ciE "co-authored|claude"` must be 0 before pushing.

**Push and CI:** `git push origin fix/remove-membership-free-trial`; watch the seven runs with
`gh run list --branch fix/remove-membership-free-trial --json name,status,conclusion,headSha` (`jq` is
not on this shell — use `--jq`). Read a red run with `gh run view <id> --log-failed`.

**Docs lanes have no shell**: the orchestrator builds the docs and commits their work; run
`check-catalog-claims`, `check-docs-refs`, `check-backlog-consistency` after them (the backlog reader
parses only numeric ticket ids — `T-0760a` does not parse).

## Where the truth about each programme is

- ADR-0058 (market), ADR-0061 (tenancy, amended 2026-09-15), ADR-0062 (customer audit log, amended
  2026-09-14/15), ADR-0063 (legal documents by effective date), ADR-0064 (company lifecycle),
  ADR-0046 (payout numbering, amended) — all under `docs/decisions/`.
- Open owner questions: `agents/backlog/questions/open.md` (Q-GDPR-03, the lifecycle O-1/O-3/Q-LC-03,
  the export's trail scope, the admin sign-out label — answered; the credit/promo `UserId` in the
  archive bundle — architect ruling pending).
- The batch scratchpads (this machine only; readable by any session):
  `C:\Users\cmisa\AppData\Local\Temp\claude\c--Users-cmisa-Desktop-Mike-Projects-cleansia\bb2b0830-97c5-40be-a729-6371ddb45d38\scratchpad\{batch1,batch2,batch3,audit,audit2}\` —
  tickets, the ADR drafts with their challenge rounds, and the read-only scout reports. The
  `swiftformat\msi\PFiles64\nicklockwood\SwiftFormat\swiftformat.exe` there is the SwiftFormat 0.60.1
  binary extracted from the MSI (`msiexec /a`), the only way to lint Swift on this Windows box.

## Memory that matters (the harness memory dir has these; if it is gone, they are here)

- Refund/money/authz tickets need adversarial review — never accept a dev's own green suite.
- `OrderMockFactory`'s `SetCurrency` overrides `CurrencyId` — pass `currency:`.
- Anonymous webhooks have no tenant claim: repo reads must `IgnoreQueryFilters()` in both the validator
  and the handler and set the override before writing.
- `FromSql` non-composable on an `ITenantEntity` needs `.IgnoreQueryFilters()`.
- The error-contract parity specs resolve a controller dispatch to a file NAMED for the class — one
  feature class per file.
- `nx` runs from Bash poison the Nx daemon and kill the owner's `nx serve` — always `NX_DAEMON=false`.
- Docs and business rules are written at the END of a feature, once green, by a docs lane.
