# Owner plate — what only Mike can decide or do (written 2026-09-16 from a full-branch sweep; updated 2026-09-19 after the batch-6 rulings, the same night when batch 6 shipped, 2026-09-20 when batch 9 — the contract for work — shipped, 2026-09-20 evening when the UI-polish and dead-code batch was filed, and 2026-09-22 when it shipped)

Everything an agent could build without a ruling is either shipped or filed as a `todo` row in
`agents/backlog/INDEX.md`. This file holds the rest: decisions with their default in force, and
actions outside the repository. A ruled decision or finding **stays on the plate, unstruck, marked
"Ruled ‹date›"** with the ADR or ticket that carries the ruling until that ticket ships — its "default in
force" column is then the ruling, and the panel's open defaults inside it hang off the row as **O-**
entries, which outlive the strike as the defaults in force. **Strike a line when it ships** (D2, D3, D4,
D5, D8, D12, F1–F8, F10, Q-UI-01, Q-UI-02), naming the commits. The record of *why* is the ADR or ticket, never this file — the same rule as
`agents/backlog/questions/open.md` (a queue, not a record); nothing is moved there.

> **2026-09-19.** The owner ruled D2, D3, D5, D8 and F6 (batch 6 — the rulings are quoted in their rows
> and carried by ADR-0065 / ADR-0066 / ADR-0067 and T-0768–T-0775); batch 5 shipped F1–F5, F7, F8 and D4
> the same day (struck below, commits named). The panel's defaults for the open questions inside those rulings are
> the new **O-** rows; the panel's findings are **F10–F16**; the batch-5 lanes' findings are **F17–F20**;
> one new action, **A10**.
>
> **2026-09-19, night.** Batch 6 shipped in full — T-0768–T-0775 and T-0748 / T-0773 — and the three ADRs
> are `accepted`; D2, D3, D5, D8 and F6 are struck below with their commits. **The O- rows stay as the
> defaults in force** (every one shipped as its default; overruling any of them is now a change to shipped
> behaviour, not a build choice). A1's runbook id moved to `20260919142517`.
>
> **2026-09-20.** Two rulings of the day shipped in full. **The contract for work** (*"I want to
> implement 'smlouva o dílo' (lawyer suggested it)"*) → ADR-0068, `accepted` and shipped as
> T-0777–T-0784 (the commits are in the INDEX rows); its seven panel defaults are the new **Q-WC-01…07**
> rows below, every one shipped as its default. **Company registration** (a cleaner contracts as a
> natural person) shipped without a ticket — **D12** below. The batch's findings are **F23–F28**. A1's
> runbook id moved to **`20260920204705`** (T-0791; was `20260919231739` from T-0777); A2 gains the work-contract draft.
>
> **2026-09-20, evening.** The UI-polish and dead-code batch (*"polish alignments in admin app … remove
> redundant and deprecated things that are no longer used (like availability module, the same for
> backend) … make overall check on both apps to make them consistent"*) is filed as **T-0785–T-0799**
> on `chore/ui-polish-and-dead-code` (stacked on #255); the discovery's thirteen questions are the
> **Q-UI-01…13** rows below. **Your *go* accepted the list as put** — Q-UI-01 and Q-UI-02 are marked
> ruled (they change T-0791's scope: the wire members drop, the four schema things fold into the
> batch's one `Initial` regen); the other eleven stand as defaults in force. **A1 will move again**:
> T-0791 regenerates `Initial` once more — the runbook id is updated when its lane reports, and the
> DEV drop stays owed at the deploy, never run on the branch.
>
> **2026-09-22.** The UI-polish and dead-code batch shipped in full — T-0785–T-0799 on
> `chore/ui-polish-and-dead-code` (**PR #260**, stacked on #255; the commits are in the INDEX rows).
> Q-UI-01 and Q-UI-02 are struck below (T-0791, `Initial` = `20260920204705`, 84 tables); Q-UI-03 … 13
> stand as **defaults in force since 2026-09-20, unruled** — you ruled none of them explicitly — and
> each row now says what shipped against its default. **One shipped against it:** Q-UI-08's
> breadcrumb was not built (the shared tree was frozen through phase 3), so the admin details carry
> the back control beside the title and the partner keeps its two hand-rolled bars. The lanes'
> findings are **F29–F65** below, reported not absorbed; the shared-tree defects among them
> (**F29–F43**, the phase-3 freeze kept them out — F29 and F30 are platform-wide) are the ones a
> follow-up ticket would take first. **Owed:** the DEV drop at the next DEV deploy (A1, MS-2); the
> merge order is **#255, then #260**.

## Decisions (default in force until ruled)

| # | Decision | Context | Default in force |
|---|---|---|---|
| D1 | **Q-GDPR-03** — a live guest booking under an erased e-mail: block the erasure, or leave the booking out? | The question's premise moved: it said blocking is defensible only once a guest can cancel — T-0753 shipped that on web, Android and iOS (`e5d28767`, `0f4d301a`, `28ccf8b1`). | Left out; the booking's contact data goes when the job ends and the order-PII sweep reaches it (`GdprDeletionService.cs` `ErasureBlockingStatuses` on the account's own orders only). |
| ~~D2~~ | ~~**T-0693** — when the last cleaner drops a job, does the order return to `New`?~~ | ~~Today it stays `Confirmed` with zero assignees (`DropOrder.cs`; `RequestCover.cs` removes nobody — the premise was one third wrong); six sweeps read the crew.~~ | **Ruled 2026-09-19** (*"I want that it could go back to New since 'Confirmed' means that cleaner(s) are assigned. Also I want admin know about it as well."*) → ADR-0067, **shipped the same day as T-0770 (`c30e2580` + `667de9f7`)**: `Confirmed → New` when a drop or an admin rejection empties the crew, `admin.order.crew_lost` at any non-terminal status, the override refuses `Confirmed` on an unstaffed order. O-D2-1 below stays the default in force (the customer is told nothing). |
| ~~D3~~ | ~~**Revenue report semantics** (T-0702 AC3 unmet though the row reads done)~~ | ~~`GetRevenueReport.cs` sums `TotalPrice` of every order incl. cancelled ones and subtracts no refunds.~~ | **Ruled 2026-09-19** (*"paid and completed orders, minus refunds, per currency, by completion date."*) → **shipped the same day as T-0771 (backend `c45a4af4` + `1fa06aa8`, web `56884360` + `df7c9ba9`)**: completed and paid by `CompletedAt`, net of both refund legs, the override stamps `CompletedAt`, the page names the gaps. O-D3-2 below stays the default in force (lost chargebacks are a named gap). |
| ~~D4~~ | ~~**Which statuses may a customer cancel?**~~ | ~~Guest cancel offers it through *OnTheWay*; signed-in stops at *Confirmed*; the server allows both.~~ | **Shipped 2026-09-19, batch 5** — web `32c5fe18` + `fe21eada`, Android `2bde3c85` + `5fb2f96f`, iOS `caecad6d` + `aeabcc15`: both flows cancel through *OnTheWay* (the server's set less the dead `Pending`), the reason is capped at the server's 500 UTF-16 units on every client, and the confirmation shows `actualRefundAmount`. |
| ~~D5~~ | ~~**Admin notifications** — channel + events~~ | ~~Nothing exists; the failed-erasure retry logs at Error for want of it (`RetryFailedUserDeletions`).~~ | **Ruled 2026-09-19** (*"both in-app and email"*) → ADR-0065, **shipped the same day as T-0768 (`7078e617` + `8e4219b2`), T-0769 (`8086c376` + `fcfdb81d`), T-0774 (`c099d23d` + `77531caf`, web `90d3800e`) and T-0775 (`b27fc069` + `4d6d6f11`)**: nine events, one feed row per administrator in the event's audience, one e-mail per recipient or to the company's mailbox, the bell and the page. O-5 and O-6 below stay the defaults in force. |
| ~~D6~~ | ~~**T-0767 responsiveness picks**~~ | ~~`agents/analysts/web-responsiveness-2026-09-16.md`: seven layout groups and two runtime groups across the three web apps. Pick which become tickets.~~ | **Ruled 2026-09-19** *"go with recommended"* → all nine groups built as T-0776, shipped the same day (55661dea, d7a54a59, f2562d67, 24299a99, 0299e127, 355ee133). |
| D7 | **Company lifecycle defaults** (ADR-0064) | Q-LC-01 administrators of a deactivated company stay admitted (ADR-0066 D7 keeps it as ruled; since T-0748 only an Administrator-role administrator may reactivate); Q-LC-02 the archive bundle holds the books only, not users/consents/customer audit rows; Q-LC-03 `UserId` stays on `credit-accounts.jsonl` / `promo-code-redemptions.jsonl` (architect may rule); O-6 no immutability lock on `company-archives`; O-2 every Plus is cancelled at wind-down — its rationale ("the customer can book nowhere else") weakened now that cross-market booking exists (T-0765). | As stated. |
| ~~D8~~ | ~~**T-0748 roles** — Support / Accountant / Manager~~ | ~~Q-LC-01 and the holding view hang off it.~~ | **Ruled 2026-09-19** (*"let's do those 3 for now"*) → ADR-0066, **shipped the same day as T-0748 (backend `56fa5aa2` + `45462202`) and T-0773 (admin web `14c833bf` + `bef0838e`)**: `AdminRole` on the account, the `admin_role` claim, five administrator sets, the map per D3, eight admin-host read twins, `SetAdminRole` under the company lock, the role on the audit row, the console hiding what the role lacks; `Initial` → `20260919142517`. O-R1..O-R10 below stay the defaults in force — every one shipped as listed. Q-LC-01 kept as ruled (D7); the holding view is still ADR-0061 D14's open item. |
| D9 | **Q-VS-01** — does the accountant accept the ten-digit variable symbol (`YYYY` + 6-digit ordinal, now per company)? | ADR-0046 cites it as filed; it was lost in the backlog reset and is re-filed in `questions/open.md` on 2026-09-16. | Ten digits. |
| D10 | **Q-ART-01(b)** — narrow the dispute-evidence file types? | ADR-0043 / security-rules cite it as filed; re-filed on 2026-09-16. | Accept set unchanged (`SniffedContentType.Signatures`). |
| D11 | Smaller defaults, fine unless you object | "The subject's disputes" in the export reads both the account and the order term; the incident file stays narrow (no bystander rows; the file's own act stays in the trail); a withdrawn consent keeps IP/UA/version after erasure; `CreateOrder` records the tick but grants no consent rows (A6); one legal entity vs one per country stays undecided (nothing blocks). | As stated in ADR-0062/0063. |
| ~~D12~~ | ~~**Company registration** — may a cleaner register or turn themselves into a legal entity (s.r.o.) from the partner apps?~~ | ~~`UpdateEmployee` / `UpdateIdentificationInfo` accepted `EmployeeEntityType.LegalEntity` from the cleaner; the three partner clients offered the choice on the identification form.~~ | **Ruled and shipped 2026-09-20** (a cleaner contracts with the platform as a natural person; only an operator may onboard a company): backend **`c400d47f`** + `20e78001` — the two cleaner-facing writes refuse a *change to* `LegalEntity` with `employee.legal_entity_not_accepted`, the admin write still accepts it; partner web **`828622d6`**, Android **`2ef0401b`**, iOS **`f3cc4927`** — the identification form no longer offers the choice; **the preservation fix `1a06fad0` + `3980201c`** — a row an operator already set to a company keeps its type and name whatever the command carries, so a cleaner's own save cannot silently downgrade an operator-onboarded company. No ticket, no ADR; the record is these commits and F23 below. |

### Defaults inside the 2026-09-19 rulings (the panel's; they proceed unless you overrule)

| # | Question | Default in force | Overruling changes |
|---|---|---|---|
| O-D2-1 | **The customer on a walk-back** (ADR-0067 D9). After the last cleaner leaves a `Confirmed` order and it returns to `New`, the customer is told nothing; the next take is `New → Confirmed` again, so `TakeOrder` sends a **second "your order is confirmed" e-mail** (+ the per-assignment push). The sequence: *Confirmed (e-mail + push) → silence → Confirmed again (e-mail + push)*, with the timeline's search step current again in between. | Nothing sent; the second e-mail accepted (a new cleaner *is* a new confirmation). | (a) suppress the second e-mail when the history already holds a `Confirmed` row; (b) a "your cleaner left, we are looking for another" message — five locales, three clients, a new ticket. |
| O-5 | **`admin.order.new` by e-mail, by default** (ADR-0065 D3). Until a company sets `notifications.admin_email`, every administrator is e-mailed every offerable order: 50 orders/day × 3 admins = 150 e-mails/day. | Every administrator (the ruling says both channels). | Feed-only for `admin.order.new` until a mailbox is set — a `Channels` flag on that one catalogue entry. |
| O-6 | **Payment-failure cadence** (ADR-0065 D4). Stripe fires `payment_intent.payment_failed` per attempt; the platform retries or cancels the order itself within the hour. | Once per order (the first decline), guarded at the site by `AnyForEventAsync`. | Per attempt, or not at all. |
| O-D3-2 | **Lost chargebacks on the revenue report** (T-0771). The `lost` webhook closes the `Dispute` and writes no money row; the platform does not store the reversed amount. | A named gap on the page (*"Lost chargebacks are not subtracted: the platform records the dispute's outcome, not the amount the bank reversed"*). | Store `Dispute.ChargebackAmount` from the `lost` webhook and subtract it — one column, one stamp, one `GroupBy` (finding F14). |
| O-R1 | `CanViewUserCredit` | Any admin — a credit balance is a liability on the books; the accountant reads the ledger. | To Support: the accountant loses the credit balance page. |
| O-R2 | `CanExpireCustomerCredit` | Manager — discharging a balance is a write-off, not customer care. | To Support: a support agent can write off a balance. |
| O-R3 | `CanInterveneReferral` | Support — reverse / force-qualify is a per-customer intervention like credit. | To Manager: support cannot reverse a referral. |
| O-R4 | `CanViewPagedEmployee` | Any admin — the accountant reaches a cleaner's masked payout details through the list. | To Support: the accountant loses the cleaner list and with it the masked payout details. |
| O-R5 | Pay-config reads / writes | Reads Accountant, writes Manager — a pay rate is company policy, not bookkeeping. | Reads Manager (the accountant cannot see rates) or writes Accountant (the accountant sets rates). |
| O-R6 | Catalogue reads (`CanViewServices` … `CanViewCountryConfigurations`) | Any admin — a support agent explaining a price needs the price list. | To Manager: support cannot see the price list. |
| O-R7 | Country-configuration writes (VAT, fiscal mode, time zone) | Manager — market configuration is operations; *company* settings are the Administrator's. | To Administrator. |
| O-R8 | Admin-user CRUD (`CanCreateAdminUser` …) | Administrator — creating an account is granting access and the create form carries the role. | To Manager: a manager can create accounts and, with the role on the form, choose their role. |
| O-R9 | Default role on the create-administrator form | Support (least privilege). | Any other member. |
| O-R10 | `admin.dispute.chargeback` audience | Every role (`AdminOnly`) — Support answers the bank, the Accountant reconciles the money that left. | Support only. |

### Defaults inside the 2026-09-20 ruling — the contract for work (ADR-0068; the full text of each is in `agents/backlog/questions/open.md`)

| # | Question | Default in force | Overruling changes |
|---|---|---|---|
| Q-WC-01 | **Which figure is the *cena díla* on the record** — the customer's price or the cleaner's pay estimate? (Lawyer + accountant; the invoicing model is A8.) | The customer's price and currency (`Order.TotalPrice`) — the price of the customer ↔ cleaner contract; the pay is the platform ↔ cleaner relation and is not stored on the row. | One member on `WorkContractFacts` (no schema) + a line on the sheet. |
| Q-WC-02 | **The web gesture.** | A tick + *Accept and take the job* on the partner web (the platform's legal-act control on the web); the swipe on Android and iOS. | A dragged slider on the web dialog — one ticket. |
| Q-WC-03 | **How the parties are named on the rendering.** A consumer contract with a trader normally identifies the trader. | Given name only, as the crew list does. The likeliest ruling to arrive. | A DTO member on the read (the cleaner's registration number / name), no schema. |
| Q-WC-04 | **Does an in-app swipe form a B2C contract for work under Czech law, or is a qualified signature (Signi) needed?** (Lawyer.) | The swipe, recorded as ADR-0068 D2 records it — the exact text row and its hash, the instant, the actor, the client, IP / device, the signed device id. | `SignatureEnvelopeId` + `SignedOn` + a webhook; nothing above changes. |
| Q-WC-05 | **The VOP wording** — when the contract forms (VOP 3.2 says the confirmation e-mail; the framework 2.3 and VOP 1.3 say the cleaner's acceptance) and incorporating the template by reference. (Lawyer.) | The code follows the framework: the cleaner's acceptance forms it, the order is stamped with the template at booking, the wizard's P074 sentence links `/work-contract`. | New sentences in the VOP — a dated seed folder + a deploy. |
| Q-WC-06 | **May an admin force a crew member, or should an admin placement be an offer the cleaner takes** (ADR-0036's hold), so every seat is formed by the cleaner's own act? | (a) — the admin keeps the force; `AcceptWorkContract`, the Start + Complete gates, the banner and the admin's *contract pending* line close what they can; the residual (a second crew member who neither starts nor completes) is stated. | (b) deletes the standalone act, both gates, one client key and the sheet's *accept* mode, and reshapes the hold for a partly crewed order — a product decision. |
| Q-WC-07 | **The coarse location on a permanent row** (*"Praha · 120"* frozen for ever as a term of the contract, while erasure blanks city and zip at the source). (Lawyer / DPO.) | Keep it, under the row's ground (Art. 6(1)(b), then 17(3)(e)). | Blank one member of the facts at erasure. |

### Defaults inside the 2026-09-20 *go* — UI polish and dead-code removal (T-0785–T-0799, shipped 2026-09-22 on PR #260; the full text of each is in `agents/backlog/questions/open.md`)

| # | Question | Default in force | Overruling changes |
|---|---|---|---|
| ~~Q-UI-01~~ | ~~**The two availability wire members** (`EmployeeItem.Availability`, `RegistrationCompletionStatus.HasSetAvailability`) — drop now or after one mobile release?~~ | **Ruled 2026-09-20 (*go*): drop now — shipped in T-0791** (backend `38312c8d9`, web `5ef4c292f`, mobile `c76523efa`; the three web clients and both mobile specs regenerated in the same commits). Deleted from `questions/open.md`; the ticket is the record. | — |
| ~~Q-UI-02~~ | ~~**Four schema-level dead things** — `Cart` ×3, `EmailTranslations`, `Employee.PreferredCurrencyCode`, `MembershipPlan.TrialPeriodDays` — fold into T-0791's regen or park?~~ | **Ruled 2026-09-20 (*go*): fold — shipped in T-0791** (`38312c8d9`; `Initial` = `20260920204705`, 84 tables, 48 with the `Tenants` FK; the DEV drop owed at the deploy — A1, MS-2). Deleted from `questions/open.md`; the ticket is the record. | — |
| Q-UI-03 | **Partner host `POST api/Payment/webhook` and `api/v1/Health`** — does anything external (the Stripe dashboard, a monitor) point at them? The docs register Stripe on the customer hosts only; the deploy warms `/health`, not this controller. | **Default in force since 2026-09-20, unruled: keep both.** Both controllers are in the tree; T-0792 removed nothing of theirs. | Delete both — 175 lines and one route, once you confirm nothing external points at them. |
| Q-UI-04 | **Admin endpoints with no UI** — `AdminPayPeriodController` `create/update/delete/open` (the *Create pay period* button ends in `console.warn`, the client method exists), `AdminEmployeeDocumentController` `versions`, `AdminPayrollController` `generate-invoice`. | **Default in force since 2026-09-20, unruled: leave all three.** T-0790 put the dialog on the shared shape and left its `console.warn` (`pay-period-management.component.ts`). The pay-period one is the dead button you will notice first. | (a) wire the UI — a story each; (b) remove the endpoint and, for pay periods, the button. |
| Q-UI-05 | **ADR-designed states with no producer** — `PayoutDetailsStatus.NeedsReconfirmation` (ADR-0034 D5), `RefundStatus.Failed` (no `refund.failed` handler). | **Default in force since 2026-09-20, unruled: leave both.** Both members are still in `Core.Domain/Enums`; the dated *what shipped* lines on ADR-0034 and ADR-0006 are the docs lane's (T-0799). | Build the producer, or strike the state from the ADR and the wire enum (NSwag + both mobile specs). |
| Q-UI-06 | **Back-office headings are Nunito** although the design language says Poppins (neither back-office `index.html` requests it). | **Default in force since 2026-09-20, unruled: the doc is amended, not the apps** — `design-language.md` §2.3 says the back-office web is Nunito-only. `$font-heading` still names Poppins first in `common/typography.scss` and three shared dialog / drawer partials read it (the Nunito fallback in the back office, Poppins in the customer bundle) — recorded, not a defect. | Load `Poppins:wght@600;700` in both `index.html` + `$font-heading` on `%cleansia-title` — two lines, every heading changes face. |
| Q-UI-07 | **Neutral palette** — `common/variables.scss` is Tailwind gray where the design language prescribes slate; `--cleansia-red` is Material, not DL. One edit cascades into all three web apps. | **Default in force since 2026-09-20, unruled: not in this batch.** T-0785 defined only the missing tokens; `--cleansia-text-primary: #111827`, `--cleansia-text-secondary: #6b7280`, `--cleansia-red: #f44336` stand (`design-language.md` §2.1). | The one-line swap, whenever you say — it touches the customer app too. |
| Q-UI-08 | **The detail page's back control** — a breadcrumb above the title (frees the title row for title-left / actions-right) or the back button on the title row (the admin majority today). | **Default in force since 2026-09-20, unruled: breadcrumb — and it did not ship.** The shared tree was frozen through phase 3, so T-0797 built no `cleansia-breadcrumb`; T-0788 put the back control **beside the `h1`** on all eleven admin details (`.cleansia-detail-title`, `_detail-page.scss`) and the partner's two details keep their hand-rolled bar (`pages/cleansia-partner/_breadcrumb.scss`). The tree is the *overruled* shape, uniform on the admin side. | Either word is now one shared component plus one adoption sweep: *breadcrumb* builds `cleansia-breadcrumb` for both apps; *header row* turns the two partner bars into the admin row. |
| Q-UI-09 | **Partner order-detail vocabulary** — read-only filled inputs, one card per section, vs the admin's label/value `.detail-grid`. | **Default in force since 2026-09-20, unruled: keep the partner's shape.** T-0797 aligned shell, header, badges, dates, money, dialogs and empty states; the crew rows and the completion summary took the shared `.detail-grid`; the read-only-input sections stay. | Align to `.detail-grid` — a rewrite of the partner `order-details.component.html` sections. |
| Q-UI-10 | **Spacing tokens / 8-pt grid** — `html { font-size: 14px }` makes every rem gap miss it; no tokens exist. | **Default in force since 2026-09-20, unruled: leave.** No `--cleansia-space-*` exists; `$base-font-size: 14px` stands (`design-language.md` §2.5). | Define `--cleansia-space-{1..8}` and sweep — a rhythm pass, not an alignment one. |
| Q-UI-11 | **Partner web lags mobile** — never calls `notifyOnTheWay`, the preferred-offer pair, `requestCover`, `dropOrder`, `getMyDocumentRequirements`. Not dead code. | **Default in force since 2026-09-20, unruled: no change.** Untouched by the batch. | Each is a story. |
| Q-UI-12 | **`partner-auth.service.ts` `authenticateWithGoogle`** — only a spec calls it; the partner login has no Google button. Is Google sign-in coming to the partner web? | **Default in force since 2026-09-20, unruled: keep.** Still in the tree. | Delete — cheap, any time. |
| Q-UI-13 | **Country form: `ISO kód` and `Dvoupísmenný kód`** read as duplicates. Two facts or one? | **Default in force since 2026-09-20, unruled: layout only.** T-0789 put the form on the twelve-column grid; both fields stay on `country-form.component.html`. | Merge the two fields — a content decision, then one form edit. |

## Actions (outside the repo)

| # | Action | Detail |
|---|---|---|
| A1 | **The DEV deploy-day runbook** (PR #255 and PR #260 are **merged** — 2026-09-22, `96f38aa9` then `cd73566b`, both branches deleted; merging did not deploy, `deploy-dev.yml` is manual) | Order on deploy day: (1) `Execute SQL Script → DEV → reset-database.sql` — the DEV database holds master's migration `20260908075819`; the tree's is **`20260920204705`** (T-0791 regenerated it on 2026-09-20 after T-0777's `20260919231739` of the same day: the availability column, `PreferredCurrencyCode`, the three `Cart*` tables, `EmailTranslations` and `TrialPeriodDays` dropped — 84 tables; T-0777 had added `WorkContractAcceptances` and `Orders.WorkContractDocumentId`; it was `20260919142517` from T-0748 until then) and the migrator fails on a mismatch; (2) `Deploy to DEV → mode=deploy` — Bicep provisions itself (the `company-archives` container, the `company-wind-down` and `company-archive` queues with poison twins); (3) `Execute SQL Script → DEV → insert_seed_data.sql` **after** the deploy (deployed hosts run as Production and never auto-seed; the seeded administrator carries `AdminRole = 1`); (4) verify `__EFMigrationsHistory` = **`20260920204705_Initial`**, `Tenants` has `cleansia-cz`, CZE `OperatorTenantId` set, **three** `LegalDocuments` seeded (terms, privacy, work contract — a booking is refused without the third), the seeded admin row reads `Profile = 100, AdminRole = 1`, and no `Carts` / `EmailTranslations` table exists. Local Aspire Postgres needs the same reset. `agents/cleanup/MANUAL_STEPS.md` MS-2. |
| A2 | **Legal texts** (lawyer) | DEV customers consent to the seeded **draft** (banner in five locales, `src/Cleansia.Infra.Database/Seed/Legal/customer/*/any/2026-09-14/`), and since 2026-09-20 every booking is concluded under the seeded **draft contract for work** (`…/customer/work-contract/any/2026-09-20/`, a template — it names the job by what the acceptance shows, carries no figure, name, address or date, and `{{currency}}` where a price is mentioned). The final wording of each is a new dated folder + deploy; the seeder refuses to alter an in-force version; a new contract version applies to orders booked from its date only. Plus the lawyer's confirmations of L1 (three-year retention, Art. 6(1)(f) / 17(3)(e)), L3 (dispute text three years after erasure), L6 (the PDF incident file), and Q-WC-01 / 03 / 04 / 05 / 07 above. |
| A3 | **Seed placeholders** | `CompanyInfo` IČO / DIČ / bank details read `REPLACE WITH ACTUAL` (`sql-scripts/insert_seed_data.sql`) and print on every receipt, payout invoice and the wind-down e-mails; `Tenant.Name` "Cleansia CZ s.r.o." ≠ `CompanyInfo.LegalName` "Cleansia s.r.o.". Replace before DEV is shown to anyone. |
| A4 | **Market figures at activation** (admin forms) | EUR/PLN/GBP/USD no-show credits are DEV placeholders; EUR's loyalty divisor is null and `ActivateCurrency` refuses until it is set; SK's insurance figure is null; Plus prices exist for CZK only; SVK VAT is seeded 20 % twice and POL's payout scheme is NULL (both out of scope until those markets open). |
| A5 | **MS-13** — send one of each e-mail from DEV and eyeball it | Nine templates now (the two wind-down notices are new); T-0774 adds a tenth (`admin-notification.html`). Related: the order-status e-mail (type 6) has no seeded translations — cs/sk/uk/ru customers get hard-coded English for take/start/complete unless a seed script is added (say the word). |
| A6 | **MS-3** — rotate the exposed Mapbox token | Mapbox dashboard + Key Vault `Mapbox--GeocodingAccessToken`; two comments in the partner env files reference the old one. |
| A7 | **Mac session (optional)** | iOS CI builds and runs XCTest for all three targets (721 + 860 + 1 223 green on `82cfb546`); a Mac is needed only to run the apps and to eyeball T-0766 (status bar) on a device. |
| A8 | **Accountant** | The multicurrency plan names the účetní review as the one overdue external item; nothing blocks code until ~1 M Kč YTD bookings. |
| A9 | **MS-9 at the first PRO deploy** | Invite yourself to the prod admin console (DEV verified 2026-09-11). PRO stays untouched by agents. |
| A10 | **`lawyers-docs/**` — tracked on this branch, or kept beside it?** | You staged the 28 files (ten DOCX contracts under `lawyers-docs/SMLOUVY CleanSia/SMLOUVY/`, the 2026-09-11 analysis and its `podklady/`) in the index on 2026-09-16; twice on 2026-09-19 a lane's `git commit` without paths swept them into a code commit (`0b9246d2`, then `07551c83`). Both times the local history was rewritten before any push (reflog *"drop lawyers-docs from local history"* ×2; messages, authors and dates preserved) — **nothing left the machine**, `git log -- lawyers-docs` is empty. State now: the 28 files plus `analyza-2026-09-16/` are on disk, **untracked and locally excluded** (`.git/info/exclude`, not `.gitignore` — a lane had added a `.gitignore` line inside a web commit and `99eec5fb` takes it out again, because whether legal drafts live in the repository is your call). Decide: (a) keep them out of git for good — say so and I move the exclude into `.gitignore` with a note; (b) track them — say so and I commit the 28 files with the new analysis as one `docs(lawyers):` commit. Until you rule, every lane stages by path and the exclude holds. |

## Findings (reported, not built — say which become tickets)

### From the 2026-09-16 lanes

| # | Finding | Where | Why it is yours |
|---|---|---|---|
| ~~F1~~ | ~~Partner and admin web still load **Font Awesome 6.0.0-beta3 from cdnjs**; all three apps load `primeicons` and `flag-icons` from jsdelivr and Google Fonts.~~ | `apps/*/src/index.html`; `apps/cleansia.app/src/app/theme/primeflex-subset.spec.ts` | **Shipped 2026-09-19, batch 5 (`a19cb9b3`):** `primeicons` and `flag-icons` 6.15.0 self-hosted in all three apps, Font Awesome gone, the allow-list is Google Fonts only. |
| ~~F2~~ | ~~The admin **disputes** list and detail render `field`/`dispute-status-badge` markup with **no stylesheet**.~~ | `libs/cleansia-admin-features/disputes-management` | **Shipped 2026-09-19, batch 5 (`51c228a3`).** |
| ~~F3~~ | ~~The wizard's **Plus-savings quote ignores the market fallback** the two other quotes use.~~ | `order-wizard.component.ts` `refreshPlusSavings` | **Shipped 2026-09-19, batch 5 (`499bb632`).** |
| ~~F4~~ | ~~The Android **customer** app still **parks** the signup tick beside sending it (`AuthViewModel.kt` → `SignupConsentRepository`).~~ | `src/cleansia_android/customer-app`, `core/consent` | **Shipped 2026-09-19, batch 5 (`2261e241`):** the Android customer app no longer parks the tick; `cz.cleansia.core.consent` deleted, `SignupConsentType` moved into the customer app. Recorded in ADR-0062 D4 as amended 2026-09-19. |
| ~~F5~~ | ~~`RegisterRequest.termsAccepted: Bool?` (iOS) carries a "form without the box" rationale that no iOS form matches any more.~~ | `CleansiaCore/Auth/AuthRequests.swift:47-48` | **Shipped 2026-09-19, batch 5 (`7984c648`):** the tick is a stated `Bool`; both sign-up forms carry the box; the core client pins that both values reach the wire. |
| ~~F6~~ | ~~Partner web **My Pay currency switch** appears only once a period has invoices and offers a cancelled invoice's currency too.~~ | ~~`period-pay.facade.ts`; `GetPeriodPays.cs:79-95`~~ | **Ruled 2026-09-19** (the switch exists *"for any period holding pays in more than one currency"*) → **shipped the same day as T-0772 (backend `8fb1e549` + `b9f18e50`, partner web `2407b701`)**: `PeriodPaySummaryDto.availableCurrencies` from the period's pay rows, a cancelled invoice's currency not offered, one call per period load. |
| ~~F7~~ | ~~`invoices.facade.ts` (partner web) still hand-maps `EmployeeInvoiceDto` to a local interface with string-literal statuses.~~ | `libs/cleansia-partner-features/invoices/src/lib/invoices/` | **Shipped 2026-09-19, batch 5 (`cf77d68a` + `31477b97`).** |
| ~~F8~~ | ~~`cleansia-select`'s `writeValue` sets a plain field under OnPush without `markForCheck`; `TODO(W6.2)` and an `any` output remain.~~ | `libs/shared/components/.../cleansia-select.component.ts` | **Shipped 2026-09-19, batch 5 (`58b8e69f` + `3767e36e`).** |
| F9 | The **T-0746 order-detail** *Incident file* action still passes its own order id (by design); the customer page has the picker. | `order-detail` (admin) | Nothing to do unless you want the picker there too. |

### From the 2026-09-19 panel (ADR-0065 / ADR-0066 / ADR-0067 and the two designs)

| # | Finding | Where | Why it is yours |
|---|---|---|---|
| ~~F10~~ | ~~`AdminReassignOrder.Handler` appends a `Confirmed` status row without loading `OrderStatusHistory`, so the appended row collides with the creation row's `Sequence`.~~ | `AdminReassignOrder.cs:58-64` | **Fixed 2026-09-19 in T-0770** (`c30e2580`): the handler loads the history with `AsSplitQuery` and a test goes red on the collision (ADR-0067 §What shipped). |
| F11 | `GetDashboardStats` and `RefundPolicy` read `CompletedAt`, which historic override-completed DEV rows lack (the override never stamped it); the refund window is closed-by-default on those orders. T-0771 stamps it going forward only. | `GetDashboardStats`, `RefundPolicy`, `AdminOverrideOrderStatus.cs:125-126` | DEV rows only, and DEV is dropped at the next deploy — nothing to backfill unless you want the dashboard right before then. |
| F12 | No admin-host **receipt** or **refund** read surface exists; the orchestrator's Accountant list ("receipts / refund reads") resolves to the revenue report after T-0771. | `Cleansia.Web.Admin/Controllers` | Whether the Accountant needs a refund list or a receipt list of their own is a product call. |
| F13 | Two shared-policy smells on the admin host that ADR-0066 D3 inherits rather than splits: the document-requirement CRUD gates on `CanAdminUpdateEmployee` (so Support gets it), and the admin-user read `{userId}` gates on `CanViewOrderCustomer` (so Support gets it). | `AdminEmployeeDocumentController.cs:125, 147`; `AdminUserController.cs:117` | Two one-line rulings (Manager or Support?) for a later split; each is one constant + one attribute. |
| F14 | `Dispute` stores no chargeback amount; the `lost` webhook carries it. O-D3-2's fix is one column + one stamp + one `GroupBy`. | `Dispute.cs:32`; `HandlePaymentNotification.cs:519-521, 544-549` | Follows O-D3-2. |
| F15 | A `DeadLetter` row tells nobody — a mistyped admin mailbox or a SendGrid outage dead-letters every admin e-mail silently (ADR-0065 §Residuals). A candidate tenth admin event. | `DeadLetters`; the poison consumers | Say whether `admin.queue.dead_letter` is wanted; it is one catalogue entry + one site once ADR-0065 lands. |
| F16 | `docs/domain/order-lifecycle.md:67-71`, T-0693's context and CLAUDE.md landmine 4 say *"a drop, a cover request or an admin rejection removes the assignment"* — `RequestCover` removes nobody (`RequestCover.cs:14-22`). Wrong independent of D2. | `docs/domain/order-lifecycle.md`, `CLAUDE.md` | Fixed by T-0770's doc edit (ADR-0067 §Consequences); nothing for you unless T-0770 stalls. |

### From the 2026-09-19 batch-5 lanes

| # | Finding | Where | Why it is yours |
|---|---|---|---|
| F17 | The Android customer order detail still derives `canReportIssue` / `canRebook` **inside the composable** from the status — the shape the cancel gate just left (`canCancel` now comes from the ViewModel, `OrderDetailScreen.kt:279`, D4's Android commits). | `OrderDetailScreen.kt:283-290` | Consistency, no behaviour; a small refactor ticket if you want the three gates in one place. |
| F18 | Installed Android customer builds keep an **orphan `signup_consent` DataStore file** that nothing reads any more (the parking was deleted in `2261e241`; DataStore files are not removed with the code). | Android customer app, on-device storage | No production installs exist; a one-off delete on first launch is machinery for nothing unless a build has shipped to a tester's device you care about. |
| F19 | iOS `OrderDetailViewModel.cancel(reason:)` does not guard the reason length at the VM layer — the sheet caps it (`CancelOrderSheet.swift:66`, `CancelReasonLimit.maxUtf16Length = 500`) and the guest VM guards (`GuestOrderViewModel.swift:124`); parity with Android was kept deliberately. | `OrderDetailViewModel.swift:222-227` | Defence in depth vs. parity; a one-line guard if you want the signed-in VM to refuse like the guest one does. |
| F20 | `.swiftlint.yml`'s `included` list omits `CleansiaCustomer/Tests` and `CleansiaPartner/Tests`, so the 400-line `type_body_length` ceiling is not checked there — two test classes already exceed it (`CustomerWireContractTests`, `OrderDetailViewModelTests`). | `src/cleansia_ios/.swiftlint.yml:1-5` | Adding the two folders turns those two files red in CI; either split them in the same change or raise the ceiling for tests. |
| F21 | The integration harness's arrange wrapper (`BaseIntegrationTest.TestMethod`) saves with a bare `SaveChangesAsync` and stamps only `TenantId`, so every hand-built `Auditable` seed must remember `StampUnstampedAdded` + `CommitAsync` or dies on `CreatedBy` NOT NULL — three classes forgot it on 2026-09-19 (fixed in `0e0f786d`, `badb03cc`); CI caught each because Docker was down here. | `src/Cleansia.IntegrationTests/BaseIntegrationTest.cs:135-143` | Stamping `Created` in the wrapper makes the class unrepeatable; a harness change to make with Docker up, not blind. |
| F22 | A `DateTime` bound from a bare date on the query string arrives Kind=Unspecified and Npgsql refuses it against `timestamp with time zone`; the revenue report 500'd that way until `5cb86e9a` normalised the report bounds (`ReportFilter.AsUtc()`). Other date-bound query endpoints may carry the same shape (dashboard analytics, pay periods, audit filters) — an audit, not a guess. | `AdminReportController.cs`; `GetOrderAnalytics.cs`; the audit/pay-period filters | One sweep + a host test per endpoint that sends a bare date. |

### From the 2026-09-20 batch (ADR-0068 and the company-registration ruling)

| # | Finding | Where | Why it is yours |
|---|---|---|---|
| F23 | `Employee.GetMissingProfileFields()` still names `profile.fields.legalEntityName` for a **legal-entity row with no name** — but since D12 no cleaner-facing form offers the entity choice or the name field, so an operator-onboarded company whose name was left blank is told to complete a field it cannot reach; only the admin write can fill it. | `src/Cleansia.Core.Domain/Users/Employee.cs:443-444`; the admin employee write | Either the admin form is the answer (document it) or the check moves to the admin's approval gate — a one-line ruling. |
| F24 | **The contract PDF** is a follow-up, not built (ADR-0068 D4): both parties read the accepted text with the frozen facts in-app; a dispute bundle today is the incident file's *Contracts for work* section plus `AdminGetLegalDocument`'s SHA-256 (the admin dialog prints it). Shape: `IPdfService.GenerateWorkContractPdf(WorkContractDto)` → `(Bytes, DataSha256)`, a markdown-to-QuestPDF block renderer over Markdig's AST, one download route per audience beside `DownloadOrderReceipt`. | `IPdfService`, `QuestPdfService` (process-wide render lock) | A product call — whether a downloadable contract is wanted before a dispute needs one. |
| F25 | **A guest has no contract read.** `LookupOrder` is its own projection with no crew and gains no acceptance line; a guest read keyed on the confirmation secret would be a new route. | `LookupOrder.cs`; the customer `OrderController` | Whether a guest should see who accepted the contract for their booking. |
| F26 | **`TermsDocumentId` on the order for the VOP themselves.** The gap ADR-0068 closed for the contract exists for the customer terms: the booking's terms version lives only in a three-year audit payload (`OrderBookingEvidence.TermsVersionAccepted`); the order row names no terms document. The platform ↔ customer relation, not the contract's. | `CreateOrder.cs`, `Order.cs` | One nullable FK + one stamp in `OrderFactory`, if the customer's half should be as durable as the cleaner's. |
| F27 | **The subject export has no section for `EmployeeActionAudit` rows** (took / dropped / cover requested / contract accepted) — the cleaner's own acts are on the timeline and in the incident file but not in the JSON they can download. | `GdprExportService.cs`, `GdprExportDto.cs` | Art. 15 completeness for cleaners — one list on the DTO. |
| F28 | `GetOrderDetails.Handler` gained the acceptance repository as its **eleventh** collaborator — past the charter's smell line before this batch, further past it now. | `GetOrderDetails.cs:35-46` | A refactor ticket (a read model or a mapper service), no behaviour. |

### Findings from the UI-polish lanes (T-0785–T-0798, phases 2–4, and the nine close-out reviews), reported not absorbed — 2026-09-22

Every item the lanes found and did **not** fix, once each, de-duplicated across the lane reports and
the close-out reviews; the items later fixed inside the batch are not listed (the reject dialog's
primary label and the expire-credit lede — `d741296ca`; the invoices chunk pulling the orders barrel
and the completion summary's phone collapse — `e523f6888`; the partner `severity="success|warn"`
fills and the disputes-list `.filter-panel` blocks — gone with the phase-3 sweeps). Paths under
`src/Cleansia.App/`. **F29–F43 are shared-tree defects the phase-3 freeze kept out** — the natural
first ticket; the rest are page-local, tooling, or fixture artefacts.

| # | Finding | Where | Why it is yours |
|---|---|---|---|
| F29 | **Every form control re-renders nothing on `markAllAsTouched()`** — `CleansiaBaseFormInputComponent` is OnPush and never subscribes to its control's `events` / `statusChanges`, so a page's *submit on an empty form* prints no *required* message on any admin form (13), and the partner and customer forms share the base (31 non-spec files call `markAllAsTouched` against it). One no-op `input` event on the control then shows it. Fix is ~3 lines in `ngOnInit` (`inject(ChangeDetectorRef)` + `formControl.events.pipe(takeUntil(destroyed$)).subscribe(() => cdr.markForCheck())`). Pre-existing in general; `cab1cf559` made the membership `code` row worse (its page-level message used to fire from the parent's CD). | `libs/shared/components/src/lib/cleansia-base-form/cleansia-base-form-input.component.ts` | Platform-wide; the one shared fix a follow-up should take first. |
| F30 | **Every `cleansia-button` autofocuses.** The template binds no `autofocus`, so PrimeNG's `[pAutoFocus]="autofocus \|\| buttonProps?.autofocus"` is `undefined` and its directive sets the HTML `autofocus` attribute for anything `!== false`; one button per page load takes focus (the dark ring on *Upravit* / *Přidat překlad* in every capture; probe: `button[autofocus]` = 8 on `/customers/:userId`). | `libs/shared/components/src/lib/cleansia-button/cleansia-button.component.html` — `[autofocus]="false"` on `<p-button>` | One line, three apps. |
| F31 | **The shared dialog skin never renders in the admin or partner app.** `cleansia-dialog.component.scss` reads PrimeNG v17 tokens (`--surface-border`, `--surface-card`, `--text-color`, `--text-color-secondary`, `--surface-ground`, `--surface-hover`) that only `apps/cleansia.app/src/styles.scss` defines, so its header / footer rules and title colour are void in both back-office bundles; the title should be `--cleansia-primary-700`. `_dialog.scss`'s `.dialog-actions` deliberately reads the same `--surface-border` so both footer kinds light up together once the skin is fixed. | `libs/shared/assets/src/styles/components/cleansia-dialog.component.scss` | Swap the six tokens for `--cleansia-*` ones. |
| F32 | **The `cleansia-button` legacy API stayed** (T-0797 Doing, not shipped): `title` and `clickFn` remain on the component; `(clickFn)` is bound 46× in the customer features, once on the admin login and once in the shared sidebar. Checker F6 holds the governed count at 3. | `cleansia-button.component.ts`; `admin-login.component.html`; `cleansia-sidebar-menu.component.html`; `libs/cleansia-customer-features/**` | A sweep across the customer app, then two deletions. |
| F33 | **The select / multiselect label rests on the border in the admin app only.** `0a3994159` put the resting rule in the admin form partial as an unscoped global selector; the partner's `cleansia-select` / `cleansia-multiselect` keep the label inside the empty box beside text inputs that carry theirs on the border. The rule belongs on the two shared component stylesheets. | `pages/cleansia-admin/_form-page.scss` (`cleansia-select .p-floatlabel-on label`); `components/cleansia-select.component.scss` | Admin / partner parity not closed. |
| F34 | **`cleansia-multiselect` has no component stylesheet** (no 44 px, no width, no invalid ring) and its `maxSelectedLabels` default of 3 collapses the chips into PrimeNG's `selectionMessage`, which `primeng-translation.ts` does not translate ("{0} items selected" in every locale); patched inside `.form-grid` only (`chipLimit`). | `libs/shared/components/src/lib/cleansia-multiselect/`; `_form-page.scss` | A `components/cleansia-multiselect.component.scss` beside the select's + a translated `selectionMessage`. |
| F35 | **`cleansia-telephone`'s box is 35 px**, not the text input's 44; patched to 44 under `.form-grid` only, so the partner profile still shows it short. | `components/cleansia-telephone.component.scss` | One rule on the shared stylesheet. |
| F36 | **`cleansia-help-card` draws its legend pill with its own sizing** (`&__status-badge` padding / radius / font-size win over `.status-badge` by import order) — the partner passes the shared tone classes so the colours match, but the legend pill is a few px smaller than the table pill. | `components/cleansia-help-card.component.scss`; `cleansia-help-card.component.html` | Render `<cleansia-status-badge kind value>` or adopt `.status-badge`'s sizing. |
| F37 | **`formatFileSize` prints `1 Bytes`** (no plural rule; `['Bytes', 'KB', 'MB', 'GB']`); read by the admin `employee-documents.facade.ts` and `cleansia-file`. The partner profile's `Intl` byte formatter belongs in the shared util. | `libs/shared/utils/src/file-transformation.utils.ts:208-211` | One shared helper. |
| F38 | **`cleansia-calendar`**: the input displays `21.09.2026` (the partner profile) while every read-only date prints `21. 9. 2026`; and it has no `showOnFocus` input, so the pay-period create and wind-down dialogs auto-open their first date picker when PrimeNG focuses the first field (the captures dismiss it). | `libs/shared/components/src/lib/cleansia-calendar/`; `pay-period-create-dialog`, `wind-down-dialog` | A display-format input and a `showOnFocus` pass-through. |
| F39 | **Two orphan shared stylesheets still ship in every bundle**: `reject-dialog.component.scss` (`.reject-dialog` has no template reader) and `cleansia-complete-order-dialog.component.scss` (after `ab8d60437` its selectors no longer match the template, which keeps only `complete-order-dialog__times`). Both are still `@use`d from `components/index.scss`. | `libs/shared/assets/src/styles/components/` | Delete both and their `@use` lines. |
| F40 | **`cleansia-table`**: `th { white-space: nowrap }` on an auto-layout table overflows wide tables — membership plans and company settings carry page overrides (`th` wrap, `overflow-wrap: anywhere` on text cells) that a `wrap` column flag or normal-wrapping headers would remove; and its template still uses `*ngTemplateOutlet` (outside F7's reach). The audit-log table wider than its scroll container on `/invoice-management` and `/company-settings` (`actions-column` at x≈1343) predates the batch. | `components/cleansia-table.component.scss:131`; `cleansia-table.component.html` | One shared flag; one template edit. |
| F41 | **The badge catalogue lacks three kinds** — audit outcome, fiscal error, GDPR request — so `audit-log.models.ts` and `fiscal-failures-list.models.ts` render the shared `.status-badge` classes through page-local tone maps, the GDPR request status stays plain text, and the audit timeline's `audit-source-badge` (`timeline.models.ts`) is still hand-rolled. | `libs/shared/components/src/lib/cleansia-status-badge/cleansia-status-badge.models.ts` | Three `STATUS_BADGE_KINDS` entries + the spec rows. |
| F42 | **Shape partials living in the admin pages folder that belong in `common/`** once the shared tree opens: `tab-strip.scss` (the partner's tab strips are uncovered), `_form-page.scss` (the partner index reads it across folders), `_dialog.scss`, and `_detail-page.scss`'s `.cleansia-detail-title` / `.detail-identity` / `.detail-actions` (`.detail-ledger` duplicates the partner's `.amount-breakdown`). `pages/cleansia-partner/index.scss:5-7` carries that phase schedule as a source comment (*"once the shared tree is open again"*) — a dangling pointer of the kind `conventions.md` moves to the commit message. | `libs/shared/assets/src/styles/pages/cleansia-admin/`, `pages/cleansia-partner/index.scss` | One move + the guards' `ADMIN_PAGES_DIR` with it. |
| F43 | **The partner GDPR page's `p-toggleSwitch`** sits raw (a shared `cleansia-toggle-switch` wrapper does not exist), and its delete block lost the danger emphasis when the blocks moved onto `cleansia-section` (`ab8d60437`, outside T-0797's Doing list; no rule requires either). | `libs/cleansia-partner-features/gdpr/src/lib/gdpr/gdpr.component.html` | A wrapper component, then the block's emphasis. |
| F44 | **The admin disputes list prints `Vratka` without a currency** — `DisputeListItem` carries no `CurrencyCode`, so the cell can only print the bare amount (the detail labels it). Ruled out of T-0786 and T-0787 in review: a backend DTO member + `npm run generate-admin-client` (the DTO is served by the customer hosts too, so a mobile spec re-dump) + `formatMoney(row.refundAmount, row.currencyCode, …)`. | `src/Cleansia.Core.AppServices/Features/Disputes/DTOs/DisputeListItem.cs`; `disputes-management.models.ts` | A small backend + web ticket. |
| F45 | **The admin order detail prints two raw enums beside a translated badge**: *Typ platby* renders `paymentType.name` (`Paid`) and the status-history node renders `New`, while the payment badge reads *Zaplaceno*. The T-0788 row named only the badge. | `libs/cleansia-admin-features/order-management/src/lib/order-detail/order-detail.component.html` | Two `enums.*` lookups. |
| F46 | **Three page-local percent formatters, none through `Intl`**: `${x}%` (`membership-plan-list.models.ts:110`), `${Math.round(pct * 100)}%` (`promo-codes-list.models.ts:20`), `formatPercentage` → `+12.5%` (`reports.facade.ts:221`). Czech typography is `25 %` (with the space); there is no shared percent helper beside `formatMoney` / `formatDate`. | the three files | One `formatPercent(value, lang)` in `@cleansia/utils`. |
| F47 | **Membership plans still print `299.00 CZK`** via the local `formatPlanPrice` (`toFixed(2)` + the code) — the one list money figure left off `formatMoney`; on T-0786's NOT list as a consolidation (DC §3.9), not a defect. | `libs/cleansia-admin-features/membership-plan-management/src/lib/membership-plan-list/membership-plan-list.models.ts:34` | One call swap. |
| F48 | **The boolean-column idiom, as decided in T-0787's review** (recorded so the next list follows it): a *marker* column that exactly one row carries (countries `isDefaultMarket`, currencies `isDefault`) reads `Ano` / `—`; a boolean *fact* (legal documents *V platnosti*, membership *Expres*, company-settings values) reads `Ano` / `Ne`; the currencies' `isActive` keeps its deliberate `Ano` / `Zatím ne` (a WHY comment at the column). | `currency-management.models.ts`, `country-management.models.ts` | Nothing unless you want one word for all three. |
| F49 | **Two section titles repeat the label under them** against the "titled by content" standard `0e9174356` set: `document-requirements` titles its section *Země* over a select labelled *Země*; `promo-code-detail` titles its section with the generic `details_title` (*Podrobnosti*). | `document-requirements.component.html:15-18`; `promo-code-detail.component.html` | Two key edits, five locales each. |
| F50 | **Admin checkbox / radio option rows keep a `div[tabindex][click]` wrapper** around `cleansia-checkbox` / `cleansia-radio` — two focusable controls per option, and a click on the box bubbles to the wrapper's toggle. | the admin filter forms and option lists (T-0794's report) | An a11y sweep. |
| F51 | **Not-found / error branches missing** on the promo detail, the customer (loyalty) detail and the email-type detail (T-0796 covered order and employee); and the order detail renders the `load_error` copy for a 400 `order.not_found` (the facade maps 404 only). | `promo-code-detail`, `user-loyalty-detail`, `email-type-detail`; `order-detail.facade.ts` | Three `@else` blocks, one map. |
| F52 | **Small admin leftovers**: `employee-detail.component.html` compares `employee.entityType === 2` twice instead of the enum, and two filled primaries can still render at once on that page (the "one filled primary" line in `d56e48364` overclaims there); unused keys `pay_periods.detail.{overview,period_label,status,start_date,end_date}` and `pages.promo_codes.form.validation.{code_pattern,code_required,percent_range,amount_positive}`; `pages/cleansia-admin/template-form.component.scss` holds only a `.hint` rule with no consumer (checker F12's one advisory site); the data-protection user-id form prints its *required* message in a `<small class="field-error">` that no stylesheet declares (`data-protection.component.html:71` — `cleansia-text-input` already renders its own `.cleansia-error-message-container`, so the page-level message is a second, unstyled one); and `pages/cleansia-admin/data-protection.component.scss:3` writes `max-width: 24rem` on `__user-id` a second time beside the `.cleansia-list-filter` rule `ea70c8c7e` added for exactly that width (`list-filter.scss:4`). | as named | A prune. |
| F53 | **The customer credit ledger's sign can wrap**: `formatLedgerAmount` joins the sign and the amount with a plain space (`+ 1 250 Kč`) into a table cell with no nowrap; the invoice ledger it copies sits under `.detail-ledger__value` nowrap. | `user-loyalty-detail.facade.ts:547-551` | A non-breaking space or the `numeric` flag. |
| F54 | **Two validation / money gaps on forms**: the package form's derived gross prints via `number:'1.2-2'` with no currency (the one admin form money figure not on `formatMoney`); the partner bundles lack `validation.common.min` / `max` in all five locales, so a partner control with `Validators.min` / `max` prints the raw key (the admin bundles gained them in T-0789). | `package-form.component.html`; `apps/cleansia-partner.app/src/assets/i18n/*.json` | One `formatMoney`, ten keys. |
| F55 | **Presentation leftovers on the admin details**: disabled filled primaries (*Odeslat zprávu*, *Použít stav* on the dispute page) render pastel — PrimeNG's disabled style, shared; `admin-order-photos` shows a broken-image tile in the fixture; the audit-entry identity sits on a bare grid rather than `.detail-identity` (outside T-0788's Doing list). | `dispute-detail`, `admin-order-photos`, `audit-entry` | Cosmetic; say if any matters. |
| F56 | **Two bespoke validation copies T-0790 dropped** are worth a product look as hints: `credit.dialog.validation.amount_max` (*above the limit for a single credit — issue it in smaller amounts*) and `note_required` (*Say what happened.*). | the issue-credit dialog | Copy decision. |
| F57 | **The partner order timeline colours the wrong statuses**: `STATUS_CLASS_MAP` / `STATUS_ICON_MAP` are keyed on stale enum numbers (they assume Pending = 1 … OnTheWay = 6; the client is New = 0 … Cancelled = 6), so the icon and colour for OnTheWay, InProgress, Completed and Cancelled are wrong. Left as is by T-0797 (not a shell / badge / date / money item). | `libs/cleansia-partner-features/orders/src/lib/order-details/order-details.helpers.ts:63-82` | Re-key on the enum, or render the node through `cleansia-status-badge`. |
| F58 | **The partner's one irreversible dialog keeps a filled primary** — `mark-cash-collected-dialog` calls the act irreversible in its own summary and its primary is plain blue, while T-0790 put every admin irreversible primary on the red outline; the partner `dialogs.spec.ts` has no such rule, and its call-block delimiter does not match how the partner facades close `dialogService.open(` (each block runs into the following `onClose` handler, so the test is looser than it reads). | `mark-cash-collected-dialog.component.html:25-29`; `apps/cleansia-partner.app/src/app/theme/dialogs.spec.ts:41-64` | Parity ruling + a tighter spec. |
| F59 | **Partner order-detail residue**: `complete-order-dialog` prints the delay figure inside a `status-badge` pill (ruled against in T-0797's first review — no badge on a figure); the history connector now starts at the first node's centre but runs ~16 px past the last one's (a two-line item is taller than its icon). | `complete-order-dialog.component.html:32-36`; `pages/cleansia-partner/order-details.component.scss:512-570` | Two small rules. |
| F60 | **The partner help legends still list the dead `Pending` order status** — rewritten by T-0793 to *"a legacy state no new order enters"*, five locales, but still a row a cleaner reads. | `apps/cleansia-partner.app/src/assets/i18n/*.json` `help.orders.status.pending_desc` | Keep the honest row or drop it — copy, not layout. |
| F61 | **Customer app items seen from the batch (outside every F-rule and every ticket)**: no PrimeNG translation is provided (`app.config.ts` — English month names in its calendars; the same `providePrimeNgTranslation()` + `primeng.*` block ×5 as the back office); the `auth` / `guest` guards still return `router.navigate` (the shape T-0795 fixed for admin and partner); `customer-navbar.component.html:74` `aria-label="User menu"` is a literal; `disputes.models.ts` keeps a local error-key map. | `apps/cleansia.app/src/app/`, `libs/core/customer-services/src/lib/guards/`, `libs/cleansia-customer-features/disputes/` | Four small customer-app tickets, or one sweep. |
| F62 | **Guard and tooling gaps**: `agents/cleanup/consistency-baseline.md` does not list the six F advisory baselines that `quality-gates.md` names it as the place for; the checker's F2 tag list is the camelCase spelling only (a lowercase `<p-multiselect>` / `<p-inputnumber>` slips); the admin `page-shell.spec.ts` card-width test matches the raw stylesheet while the partner twin strips `@media` headers first (an admin `@media (max-width: 1200px)` would fail as a card width); the checker's header still says the F-rules are *"stated in this file alone until consistency.md carries their rows"* (it does now); the `!important` count T-0798 was filed with has no guard; `unused-keys.mjs` cannot see template-string reads (`primeng.*`, `api.*`); `company-info.models.spec.ts` ×3 and the partner `ProfileFormFactory` spec fail only under a whole-workspace parallel jest run (timing). | `agents/tools/check-consistency.mjs`, `agents/cleanup/consistency-baseline.md`, the two `page-shell.spec.ts` | One tools ticket. |
| F63 | **Pre-existing build warnings**: NG8107 at `order-details.component.html:201` (`currency?.code`) and `cleansia-market-switcher.component.html:9`; `cleansia-sidebar-menu.component.scss` is 142 B over the 8 kB `anyComponentStyle` budget — the file is loaded globally *and* via `styleUrls`, so the component copy is redundant weight; the `productivity-gauges` SVG keeps `<stop style="stop-color…">` (SVG presentation, not layout). | as named | Three one-liners. |
| F64 | **Idiom leftovers**: the loyalty promo feature navigates with string routes (`'/loyalty/promos'` ×13 across `promo-code-detail.{component,facade}.ts`, `promo-code-form.{component,facade}.ts`, `promo-codes-list.facade.ts`) while `CleansiaAdminRoute.LOYALTY_PROMOS` exists; `size="'full-width'"` survives at 12 sites (10 customer, 2 partner forgot-password) through the mapping rather than `[block]`; `cleansia-button` `appearance="brand \| brand-outline"` has zero template consumers. | as named | A ratchet each. |
| F65 | **Fixture artefacts in the captures, not defects** (so nobody re-scouts them): the pay-period fixture's status `Approved` is not a `PayPeriodStatus` member (real data is Open / Closed / Paid), so the badge shows its neutral raw-name fallback and the detail has no actions section; the order fixture's `orderStatus { name: 'New', value: 2 }` makes the badge read *Potvrzeno* while the read-only select reads *Nová*; `paymentType.name = "Paid"`; the promo code `AUDIT`; a customer audit entry with no payload rows. | the scratchpad fixtures | Nothing. |
| F66 | **Local web verification must run per project, as CI does.** `libs/core/partner-services/jest.global-setup.js` (2026-08-11) pins `TZ=America/Los_Angeles` for that project's date-only wire tests; a root `npx jest` runs every project's `globalSetup` in the parent before forking workers, so the pin reaches every other project and four date-only asserts (`company-info.models.spec.ts` ×3, `profile.models.spec.ts` ×1) fail by a day while each passes alone and `nx affected -t test` (frontend-ci) never sees it. Either scope the pin (a per-project `testEnvironment` setup, or `process.env.TZ` in that project's `test-setup.ts` before any `Date` is built) or write `nx run-many -t test` into the verification runbook — a ruling, not a lane. |
| F67 | **A latent test-order leak in the Android partner suite, fixed on the way to the merge** (`34ba1233`, test only): `IdentificationSectionViewModelTest` returned at `Loaded` while the ViewModel's field-labels read was still on `Dispatchers.IO`; the read resumed on a `Main` the rule had already reset and the next test in the JVM failed with `UncaughtExceptionsBeforeTest` (twice in a row on CI, 2026-09-22). The helper now joins the ViewModel's children. `IdentificationSectionViewModel` is the only partner ViewModel that reads field labels (`countryGetFieldLabels`, one caller in the tree), so the shape — `Loaded` published before a second `viewModelScope.launch` that hops to IO — has no other instance to check today; a new section ViewModel that copies it wants the same join in its test. |
