# Backlog — INDEX

**One row per ticket. One status. This table is the only place a status lives.** See
[`README.md`](README.md) for why that sentence is the whole design.

**Status:** `todo` · `in_progress` · `blocked` · `done` · `dropped`
**Size:** S (< half a day) · M · L

> Filed 2026-08-25 from a transcript sweep of the whole session, then ground-truthed against
> master one item at a time. 17 further items were checked and found **already shipped** — they
> are not listed, because a done ticket in a queue is how four lanes got dispatched onto
> already-finished work on 2026-08-11. Everything below was verified still open.

> **Re-ground-truthed 2026-08-27, after #226, #227 and #228 merged.** 24 rows were re-checked
> against master with file-level evidence. Four were already shipped by #228 and are now `done`
> (T-0611, T-0612, T-0614, T-0616); three have a fix on master that nobody has confirmed on a
> device and are `blocked` on the owner rather than on an agent (T-0617, T-0618, T-0621); five
> had a premise the tree contradicted and are retitled (T-0613, T-0615, T-0626, T-0628, T-0631,
> T-0643). **A row filed as open is not evidence that it is open** — that is the whole reason this
> pass exists, and the reason to run one again before dispatching any lane.
> **The blocked rows were re-grounded the same day.** Every question a `blocked` row named was
> researched against the tree. Two rows named a question about something else entirely — `Q-INFRA-01`
> is the custom-domain decision and `Q-INFRA-02` is one-subscription-vs-two, neither of which gates
> the row that cited it. Several rows were not blocked at all: the Azure cost cut was designed,
> measured and committed on 2026-08-11, the docs getting-started page was written on 2026-08-24, and
> the role-name hyphen bug was fixed on 2026-08-25. **A `blocked` tag is a claim about the past too.**

> `blocked` rows name a question in [`questions/open.md`](questions/open.md). A blocked row is
> waiting on the owner, not on an agent.

> **Batches 1 and 2 closed 2026-08-27** (#230, #231). Ten rows above went `done` in one 
> pass because the INDEX was not updated as each merged — the same drift this file's 
> second provenance note was written about. Close a row when its PR merges, not later.

| ID | Title | Size | Status | Owner | PR |
|---|---|---|---|---|---|
| T-0607 | Onboarding: tappable step dots let a cleaner go back (iOS + Android) | M | `done` | — | #227 |
| T-0608 | Onboarding: the 5 iOS/Android stepper divergences reconciled | M | `done` | — | #227 |
| T-0609 | Partner login: the brand hold no longer replays after sign-in (iOS) | M | `done` | — | #227 |
| T-0610 | Partner web: dead availability module removed, with a backend null-guard | S | `done` | — | #227 |
| T-0611 | Partner web: registration-number label from country config, drop hardcoded IČO | S | `done` | — | #228 |
| T-0612 | Partner web: replace-document UI (mobile has it, web does not) | S | `done` | — | #228 |
| T-0613 | Admin web: document-requirements CRUD and the deletion-request queue — new feature lib, two screens, three permissions, five locales | M | `done` | — | #234 |
| T-0614 | Documents checklist: iOS shows retry on load failure, Android renders the list | S | `done` | — | #228 |
| T-0615 | Admin: the registration-number label comes from the country config, not a hardcoded IČO — backend route, regenerated client, and neutral fallbacks in five locales | M | `done` | — | #233 |
| T-0616 | Backend: missingFields emits camelCase keys, every locale defines snake_case | S | `done` | — | #228 |
| T-0617 | Customer iOS: swipe-to-return on order detail | M | `done` | — | 7ad5bdfc |
| T-0618 | Cancelled orders show the real map again — the status guard is out of all four mobile apps | M | `done` | — | #230 |
| T-0619 | Customer apps: the disabled cancel button mutes to a neutral instead of fading a red fill under a white label | S | `done` | — | #232 |
| T-0620 | Android core: the consent checkbox glyph sits at the form's leading edge | S | `done` | — | #232 |
| T-0621 | Partner iOS: new-order address placeholder centring | S | `done` | — | c4acb926 |
| T-0622 | Partner iOS: the profile photo reaches the onboarding Personal step | S | `done` | — | #231 |
| T-0623 | Partner Android: unwanted whitespace at the top | S | `done` | — | 5c70922f |
| T-0624 | Customer apps: the Logout / Delete-account rows follow the partner app | M | `done` | — | #231 |
| T-0625 | Partner iOS onboarding: the two slides use different mascots, matching Android | S | `done` | — | #232 |
| T-0626 | Sign out confirms everywhere it should — admin unauthorized page, and both partner splash escapes | S | `done` | — | #233 |
| T-0627 | Customer web: the serviceability banner uses the server's CityNameMatch rule, not a string compare | S | `done` | — | #233 |
| T-0628 | Customer mobile: serviceability warned at address selection — booking review pane (#233) and the saved-address lists (#235) | M | `done` | — | #235 |
| T-0629 | i18n: a label outgrowing a single-line control truncates — ported to iOS and web, written into conventions.md | M | `done` | — | #232 |
| T-0630 | Admin console gated behind an Azure invitation (`admin_console`) — **MS-9 before the next admin deploy** | M | `done` | — | #233 |
| T-0631 | Partner Android: camera capture for job photos and the avatar, CAMERA restored with its uses-feature | M | `done` | — | #231 |
| T-0632 | `SendNewJobsDigest` fires hourly as scheduled — App Insights `sum(itemCount)` reads 30/20/20/30 across 2026-08-24..27, i.e. ~25 a day. The Invocations blade showed 8 because DEV samples telemetry at 10% and that blade reports raw sampled rows without correcting for it | S | `done` | — | — |
| T-0633 | Bicep audit — three axes read 2026-08-28 against a non-mutating `what-if` (run 33198443746). **Drift**: 23 reported, 21 are what-if artefacts (ARM's site GET omits `siteConfig`, plus service-defaulted Postgres / SWA / blob-container properties) — 2 are real and are filed as T-0655 and T-0656. **Secrets/identity**: RBAC vault, no value in source, managed-identity reads, Secrets *User* for hosts and Officer only for CI — no finding. **Cost**: B2 plan, SWA Free ×3, Postgres B1ms Burstable, Standard_LRS, ACR Basic, private networking off, telemetry sampled to 10% under a 500 MB breaker — no finding. Method limit worth carrying: `what-if` returns NestedDeploymentShortCircuited on the roleAssignment nested deployment, so a clean drift report never covers RBAC. One dismissed entry was spot-checked in the portal 2026-08-28 — health check is **Enabled** with `/alive` on the API sites, confirming the `+ healthCheckPath` line was a what-if artefact and not drift | M | `done` | — | — |
| T-0634 | Log Analytics is required by workspace-based App Insights — closed, not a defect | S | `done` | — | d5b020bf |
| T-0635 | Azure cost: the dev cut was designed, measured and committed on 2026-08-11 — owner closed it without resolving further | S | `done` | — | d5b020bf |
| T-0636 | Partner DEV API 500 from /health — **root cause found, closed 2026-08-28 with no code change**. `System.BadImageFormatException`: *"…The format of the file '/home/site/wwwroot/Cleansia.Infra.Database.dll' is invalid."*, from `Microsoft.Extensions.DependencyInjection` → `ResolveService`, 2026-08-25 18:43:57 UTC. A deploy race, not a defect: `azure/webapps-deploy@v3` extracts the zip file-by-file over the live `wwwroot` and dev has no staging slot (a B-series plan cannot host one), so the restart raced extraction and DI loaded a half-written assembly — clearing when extraction finished, which is the four minutes the report measured. **Deliberately not fixed.** Prod is immune by construction (slot → warm → prove → swap). Dev is already mitigated: `warm-dev-sites` retries 30×10s and only warns, and its comment already names this ticket. `WEBSITE_RUN_FROM_PACKAGE=1` does not apply — these are `kind: 'app,linux'` hosts and the value `1` is a Windows mechanism; the Linux form takes a blob URL, i.e. a SAS-rotating upload across five deploy jobs to remove a warning from a dev log | S | `done` | — | — |
| T-0637 | Warm-up probe: classifies the status and surfaces the body; prod fails fast, dev keeps its budget | S | `done` | — | #232 |
| T-0638 | Backend: `context.TraceIdentifier` joins the request log lines to the exception line, keeping the 413 | S | `done` | — | #230 |
| T-0639 | refresh-mobile-spec.sh: compares CR-normalised content, counts refusals separately, exits non-zero on one | S | `done` | — | #232 |
| T-0640 | Line endings pinned to LF repo-wide — the index was already 100% LF, so the ~5,700-file renormalize the row predicted was a no-op; this is the preventive attribute | S | `done` | — | #234 |
| T-0641 | Repo stays public — automating the flip was refused deliberately: private bills against an already-exhausted Actions quota, so it would stop CI, and the right moment depends on a billing reset nothing in the repo can observe. One owner command: `gh repo edit --visibility private` | M | `done` | — | — |
| T-0642 | docs: a getting-started page for running Cleansia locally | S | `done` | — | 7651ee69 |
| T-0643 | Root README: six stale facts corrected | S | `done` | — | #232 |
| T-0644 | iOS: enum L10n.Profile back to 342 against the 400 cap | S | `done` | — | #232 |
| T-0645 | Partner iOS: the address step checks the city and shows all four service-area verdicts | M | `done` | — | #231 |
| T-0646 | Customer apps: the status-coloured left stripe is off the orders-list card | S | `done` | — | #230 |
| T-0647 | Customer Android: pull-to-refresh on Home and Recurring bookings | S | `done` | — | #232 |
| T-0648 | iOS: a disabled field looks disabled and stays disabled — four editability escapes closed | M | `done` | — | #231 |
| T-0649 | Partner Identification: Self-employed / Legal entity slides on a spring, both platforms | M | `done` | — | #231 |
| T-0650 | CI: cancel superseded iOS runs, ceiling every CI job | S | `done` | — | 00f4f729 |
| T-0651 | Customer iOS: the profile tab reaches recurring bookings, so the Plus upsell is reachable | S | `done` | — | #230 |
| T-0652 | Partner mobile: the splash Logout signs out for real, on both platforms, and confirms first | M | `done` | — | #233 |
| T-0653 | Customer apps: a saved address in an unserved city carries a warning glyph before it is picked — 3 surfaces; the recurring wizard's list cannot, its model lacks the fields | M | `done` | — | #235 |
| T-0654 | iOS: `testRefreshedTokenReRegisters` waits for the TOKENS it asserts on, not a register count — the session-start token-less register races the first forwarded token, so "two have arrived" never meant "these two have arrived". New `waitForTokens` helper; the assertion drops to a set plus a call count, which is what the test is about | S | `done` | — | #236 |

| T-0655 | `az acr build` pushes `:latest` alongside the commit sha, so the tag `functionApp.bicep` declares actually exists — a provision was pointing the Functions host at an image nothing had ever pushed. The param description is corrected in the same commit: it claimed "CI overrides per deploy with the commit sha", and no such override exists | M | `done` | — | #236 |
| T-0656 | Portal drift, the `hidden-link:/app-insights-resource-id` tag on two dev sites — **closed 2026-08-28 without declaring it**. The tag does nothing: all six hosts report telemetry and only two carry it, so `APPLICATIONINSIGHTS_CONNECTION_STRING` is what wires it and the tag is an artefact of a manual portal enable. A provision stripping it is IaC correcting drift, which is the system working. Declaring a portal-managed hidden tag in Bicep to preserve an artefact with no function is machinery for nothing. The row's value was as evidence that the partner API gets hand-edited in the portal, and that is now recorded | S | `done` | — | — |
| T-0657 | `check-backlog-consistency` could not read the backlog it guards — the row reader required a bold id and parsed 0 of 50 rows, the anti-vacuity rule keyed on an optional `tickets/` directory so the blindness looked like an empty backlog, and the summary line printed FAILED unconditionally. Reader accepts both forms; reach check is self-referential; verdict computed. Verified clean at exit 0 and exit 1 with one row made unparseable | S | `done` | — | #236 |
| T-0658 | `check-consistency` E1 and conv narrowed to what they mean — 19 violations of which 10 were the rules being wrong. E1 fired on every `data class *UiState` (9 hits, 1 defensible) and now exempts single-signal states, two-or-more concurrent in-flight signals, and per-field form validation; conv checked only the current line for a disable directive and reported a documented, ESLint-sanctioned exception. E1 9→1, conv 2→1, both survivors genuine. Seven new self-tests, 40 passing | M | `done` | — | #236 |
| T-0659 | Partner Android: the splash screen collects its two ViewModel flows with `collectAsStateWithLifecycle` — 30 files in the module already did, the dependency was already there, these two were stragglers (check-consistency E6) | S | `done` | — | #236 |
| T-0660 | iOS style gates run locally under WSL — `src/cleansia_ios/scripts/ios-style.sh` installs and asserts CI's exact pins (SwiftFormat 0.60.1, SwiftLint 0.65.0), stages an LF snapshot on ext4 because the Windows checkout is CRLF and faked 812 `linebreaks` violations, and prefers `swiftlint-static` because the dynamic build needs a 1.5 GB Swift toolchain. 36s vs a ~16min CI round; verified clean and verified catching an injected violation | M | `done` | — | #237 |
| T-0661 | Dependabot triage of 165 open alerts — **all npm**, 157 in `Cleansia.App`, 8 in `docs`. **127 of 165 are devDependencies** that never reach a browser, including 3 of the 4 criticals (handlebars, shell-quote, websocket-driver — build tooling, supply-chain not runtime). The one critical runtime alert (`@angular/ssr`) is FIXED here by a lockfile bump to 19.2.27 | M | `done` | — | #237 |
| T-0662 | Angular framework moved to 19.2.25, clearing **18 of the 26 runtime alerts** on `@angular/*` — lockfile only, since `~19.2.0` already admitted it. `npm update` could not get there: the packages peer-pin each other exactly, so npm kept reporting the installed 19.2.14 as fixed and refused every coordinated install, and `ng update` needs an angular.json this Nx workspace does not have. Dropping the `@angular` entries from the lockfile let npm re-resolve that family alone. Verified with `nx build cleansia-partner.app` | M | `done` | — | #238 |
| T-0663 | The eight unpatched advisories assessed against **this** codebase — and three of the seven distinct ones do not apply at all. Angular i18n XSS (GHSA-jj27) needs `$localize` / `i18n=`; this app uses ngx-translate and has neither. The two-way-binding sanitization bypass (GHSA-58w9) needs a sanitized sink two-way bound; there is no `innerHTML`, no `bypassSecurityTrust`, no such binding. The formatDate DoS (GHSA-48r7) needs Angular's `formatDate` on hostile input; the app's own `formatDate` is `toLocaleDateString` over server-supplied dates. **Four do apply**, all through SSR + hydration — and two of those are mitigated here by turning the HTTP transfer cache off. The remaining two need Angular 20 (T-0667) | M | `done` | — | #238 |
| T-0664 | The select option's `value: any` is recorded as deliberate rather than refactored — polymorphic by nature across 25+ call sites, PrimeNG types SelectItem the same way, and generics would be a design-system change rather than a lint cleanup | S | `done` | — | #237 |
| T-0665 | Four commands stop returning a bool that was always `true`, and `HandlePaymentNotification` stops returning a Stripe id nothing read — all five are bare `ICommand`, the form B1's own comment sanctions. No test read `.Value`; the one that did asserted `true` was `true`. 5 commands, 8 controllers, 5 test files. Solution builds, 4077 unit tests pass, check-consistency 19 → 2. **Clients not regenerated — MS-11** | M | `done` | — | #238 |
| T-0666 | `.github/dependabot.yml` batches security fixes — version updates off via `open-pull-requests-limit: 0`, security updates grouped three ways, turning 165 alerts into roughly four PRs instead of 165 × five CI workflows. The owner enabled `automated-security-fixes` after the config reached master, which is the order that matters: grouping only applies once the file is there | S | `done` | — | #238 |
| T-0667 | Angular **20.3.30** — the only fix for the advisories 19.x will never get, all of which read *affected ≤ 19.2.25, patched none*. Peer chain moved as one set (CLI 20.3.35, CDK 20.2.14, PrimeNG 20.4.0, NgRx 20.1.0, TypeScript 5.9.3, every `@nx/*` aligned to 21.6.11); `jest-preset-angular` deliberately held at 14.6 because its peer already admits Angular <21, which kept a Jest 30 migration out of scope. 17 breakages in four mechanical shapes — PrimeNG module renames, a nullable `DialogService.open()`, `[severity]` narrowed to a union, and `p-tabs` `valueChange` emitting `undefined`. `withNoHttpTransferCache()` removed: the advisories it worked around are fixed here. Typecheck clean, 68 test projects pass, three builds, SSR renders | L | `done` | — | #239 |
| T-0668 | Nx **23.1.2** and Jest **30**, landed by repairing what #246 left behind rather than by a clean migration — that PR was merged with red CI and master could not `npm ci` at all: it moved `nx` and eight `@nx/*` to 23.1.2 while leaving `@nx/devkit` on 21.6.11, whose peer is `nx ">= 20 <= 22"`. Four families were inconsistent, not one (nx, swc, the jest 30 set, and `angular-eslint` still on ^19 — missed by the T-0667 pass because eslint is not in the build path). Seven lib `test-setup.ts` files moved to `setupZoneTestEnv`, which jest-preset-angular 17 requires. Two overrides for optional peers nothing invokes. Dependabot gains an `nx` group so the family cannot split again | L | `done` | — | #250 |

| T-0669 | Home: one title system (headings `Sky700`), neutral shadows, hero bubbles + wave + uniform scroll-fade removed | M | `done` | — | verified against the artboard by screenshot |
| T-0670 | Home: unprovable claims out (4.9 rating, 2 000+ customers, three invented testimonials), rules section in from `business-rules.md`, Plus corrected to 5 % / +4 h / 1x month / 14 d | M | `done` | — | verified against the artboard by screenshot |
| T-0671 | Home: foam hero with grounded mascot, reusable foam-edge divider, promise cards replacing the icon grid, service mascots normalised to one baseline | L | `done` | — | verified against the artboard by screenshot |
| T-0672 | Home: inline price calculator over the existing `QuoteOrder` endpoint — no NSwag regen, the generated client already exposes `orderClient.quote()` | M | `done` | — | verified against the artboard by screenshot |
| T-0673 | Home: five-column footer, theme + language switchers reachable from the customer header, e-mail promo capture behind a stubbed facade | M | `done` | — | verified; four artboard links have no page yet — see the ticket |
| T-0674 | Home: wire the oven before/after pair (`label_oven` in five locales) and add `srcset` — the eight new photos total 968 KB | S | `done` | — | verified; per-pair durations deliberately not invented |
| T-0675 | Country-configurable property-size presets — ADR first; the domain already stores `Rooms`/`Bathrooms` ints, only the labels are Czech | L | `done` | — | closed 2026-09-15 (T-0756) on ground truth, not on the row: `libs/cleansia-customer-features/home/src/lib/home/components/quick-quote/quick-quote.facade.ts:203` calls `countryClient.getPropertySizes(isoCode, lang)`, `grep -rn CZ_PROPERTY_SIZE_PRESETS src/Cleansia.App` is empty, and the presets are in the root `insert_seed_data.sql` the hosts auto-seed — AC3's frontend half shipped with the customer client regen and MS-14 is discharged. The `blocked` tag outlived the block it named. |

| T-0676 | Promo e-mail rendered from `email-templates/` rather than a hosted SendGrid template — the folder becomes the runtime source, so repo and sent HTML cannot drift | M | `done` | — | — |
| T-0677 | Migrate the six existing e-mails off hosted templates and delete the six `*TemplateId` settings | M | `done` | — | AC4 (a real send) is the owner's; see MS-13 |
| T-0678 | One button and one title component everywhere — `.cl-title` deleted, `cleansia-title` adopts the fluid Sky700 scale (owner ruling) | M | `done` | — | visual check of the partner/admin heading shift is the owner's |
| T-0679 | Serialise PDF rendering — concurrent renders emit an all-zero `/ToUnicode` CMap, so an invoice or receipt looks perfect and its text cannot be copied or searched | S | `done` | — | root cause proven empirically (0/300 serial, 39/1200 concurrent, 0/1200 locked); gate test fails 3/3 without the lock |
| T-0680 | The generated clients answer a non-array 200 with `null` while declaring an array — 21 unguarded call sites | M | `done` | — | all 21 fixed; mutation-verified (reverting all 21 gives exactly 21 distinct failures, no more) |
| T-0681 | Production never serves SSR — `NG_ALLOWED_HOSTS` was set nowhere and Azure's `X-Forwarded-*` deopted the rest | S | `done` | — | verified on the built server, 12,182 → 227,446 bytes; the feared hard-400 on an unlisted host could NOT be reproduced |
| T-0682 | Customer home JS diet — component entry points, dead providers, i18n via TransferState | M | `done` | — | −239 KB eager JS, −1.1 s/−1.6 s load (en/ru) on Slow 4G; items 1 and 4 refuted by measurement, not built |
| T-0683 | PrimeFlex is a CDN dependency for 37 classes - self-host the subset or drop it | M | `done` | — | shipped 2026-09-16 (27cefab1, 7cf02550), branch fix/remove-membership-free-trial, not yet merged; AC1 self-hosted as a subset — the 38 classes the customer app renders (99 sites, `mt-3` was missing from the 37) ship in `apps/cleansia.app/src/primeflex-subset.scss` declaration-for-declaration from `primeflex` 4.0.0 in its source order, first in the cascade; AC2 the CDN `<link>` + `<noscript>` gone; AC3 the navbar 1360px override kept (the subset reproduces the 768 conflict) with its comment naming the file; guard `primeflex-subset.spec.ts` (every used class has an identical bundled rule, no `index.html` names primeflex, the third-party stylesheet allow-list may only shrink). The remaining CDN loads went 2026-09-19 (`a19cb9b3`, owner-plate F1): `primeicons` and `flag-icons` 6.15.0 are self-hosted in all three apps (`@use` in each `styles.scss`, the flags copied from `node_modules` by each `project.json`), Font Awesome is gone, and the allow-list is Google Fonts only (`REMAINING_THIRD_PARTY_STYLESHEETS = ['fonts.googleapis.com/css2']`). |
| T-0684 | CreateDispute with no id: empty string on Android, a refusal on iOS | S | `done` | — | settled on the refusal; Android moved to meet iOS, via wireResult so it refuses instead of crashing |
| T-0685 | GDPR retention sweep never ran - an absent feature flag read as disabled | S | `done` | — | switch moved to config (`DataRetention:Enabled`, default true). Merged in #253. Note: production has never been deployed, so nothing was ever harmed |
| T-0686 | Nothing records that a customer accepted the terms, or which version | S | `done` | — | absorbed by ADR-0062 D4 / T-0732 (T-AUD-3), shipped 2026-09-13 on fix/remove-membership-free-trial: `LegalDocumentVersions` (`"2026-09-draft"`), `UserConsents.DocumentVersion`, `Register.Command.TermsAccepted`, the server grants both consents with version + IP/UA on email/Google/Apple sign-up, `customer.account.register` and `customer.consent.*` audit rows are the history. AC3 (retrieve without SQL) is the admin trail (T-0733/T-0735). |
| T-0687 | Admin order list renders a blank status pill for every New order | S | `done` | — | guard tested the number not the object; also added the missing New/OnTheWay class cases and their styles |
| T-0688 | Multicurrency has never run at a rate other than 1, and CreateOrder accepts any currency id | L | `done` | — | executed as T-0699–T-0709 (2026-09-12) and completed by T-0710–T-0721 (2026-09-13), branch fix/remove-membership-free-trial; the plan of record below is the history. **PLAN OF RECORD → [`final`](T-0688-multicurrency-plan-final.md)**; background in [`research`](T-0688-multicurrency-research.md), [`design`](T-0688-multicurrency-design.md), [`cz-only`](T-0688-multicurrency-plan-cz-only.md) §§1–2. Option B ordered, CZK-only ops, extensibility contract. **Split into tickets before starting.** VAT fix shipped (56ad4d45) |
| T-0689 | The feature-flag mechanism gated nothing and has been removed | S | `done` | — | headline was WRONG: no admin UI ever existed, so no switch could be flipped. Real defect was worse — after T-0685 the table gated nothing at all. Whole mechanism deleted |
| T-0690 | Remove the free trial; every Plus benefit now requires a PAID subscription | M | `done` | — | trial removed AND made unsettable (admin validators refuse it); entitlement split from lifecycle across 10 benefit sites; web stopped advertising it; a lapsed membership now stops the recurring schedule |
| T-0691 | Confirmed meant two things — money settled OR a cleaner took the job | L | `done` | — | ADR-0057 supersedes ADR-0037 D1s status term. Webhook + recurring confirm write money only; a paid card order rests at New. Mobile timelines needed no change — they were already right |

| T-0692 | A lapsed Plus member is never told their recurring schedule stopped | S | `done` | — | shipped 2026-09-16 (881d9cc1 with T-0694; the second session's work, verified after the hand-over: backend 5880 / 515 / 318, Android 1114 / 586, web 2613, CI 7/7) |
| T-0693 | Confirmed can be true with zero assignees — a drop or cover never walks it back | M | `todo` | — | pre-existing, recorded as a known consequence of ADR-0057. **Decision ticket before it is a code ticket**: `AdminOverrideOrderStatus` forbids backward moves, and ~8 test files build exactly this fixture. **Decided 2026-09-19** (owner ruling D2 → ADR-0067): the code ticket is T-0770, which closes this row when it ships (AC1 yes, AC2 = ADR-0067 D7, AC3 = D8 kept-argued, AC4 n/a). The premise "a cover request removes the assignment" was wrong — `RequestCover` removes nobody. |
| T-0694 | The order.confirmed push key has a fulfilment name and a money meaning | M | `done` | — | shipped 2026-09-16 (881d9cc1 with T-0692; order.payment_confirmed emitted, order.confirmed kept resolvable for old rows and devices, both mobile apps map both) |
| T-0695 | ADR-0045 and ADR-0055 still describe the Confirmed overload that ADR-0057 removed | S | `done` | — | Dated banners preserve historical bodies and distinguish status supersession from citation repair. Independent review, documentation checks and build pass |
| T-0696 | sql-scripts/seed/insert_orders.sql cannot run — wrong content and mismatched arity | S | `done` | — | AC1 answered by deletion: the file went in 4a6319a9 (seed-script cleanup, 2026-09-12) with seven other unrunnable seed scripts; `git ls-files sql-scripts/seed/insert_orders.sql` is empty on the branch. AC2/AC3 were conditional on keeping it |
| T-0697 | src/Cleansia.App/CLAUDE.md still says NSwag regeneration is owner-only | S | `done` | — | All seven named pages corrected; independent review, documentation checks and build pass. Root agreement unchanged; outside-scope historical remnants recorded |

| T-0698 | Extras have no admin CRUD — an extra can only be priced by editing the seed | M | `done` | — | shipped 2026-09-12 (c0c11bb4 + e61ebe48 + 91812fce), branch fix/remove-membership-free-trial, not yet merged; owner asked for this during the multicurrency decisions and it was never reached. Split out of chunk 4 by ruling 2026-09-09 rather than growing the batch that needs NSwag + two mobile specs + a Mac session |

> **T-0692–T-0697 filed 2026-09-08** from the out-of-scope findings of T-0690/T-0691, each re-verified
> against the tree on the day it was filed. A seventh finding — claimed drift between the committed
> partner mobile spec and the `Order/RequestCover` / `Order/DropOrder` routes — was **re-checked and not
> substantiated**: both schemas in `src/cleansia_android/openapi/partner-mobile-api.json` match the
> current commands exactly. It is not filed.

| T-0699 | Nothing resolves a booking currency — every order is stamped with the platform default | L | `done` | — | shipped 2026-09-12 (52b5c939), branch fix/remove-membership-free-trial, not yet merged; Two currencies can never be live at once. The only "switch" is promoting a new default, which swaps the whole platform in one click. |
| T-0700 | A currency cannot be switched on, and a new one is on by default with no prices | M | `done` | — | shipped 2026-09-12 (54409fc6 + 0e74cab6), branch fix/remove-membership-free-trial, not yet merged; The market switch has no writer, and the gate that guards promotion is vacuous for every currency an admin can create. |
| T-0701 | The pay-coverage gates are currency-blind while the pay writer is currency-strict | M | `done` | — | shipped 2026-09-12 (70e1c7be), branch fix/remove-membership-free-trial, not yet merged; An order in a currency no cleaner has a rate in passes every gate, and then silently never gets a pay row. |
| T-0702 | Reports and roll-ups add amounts in different currencies into one number | M | `done` | — | shipped 2026-09-12 (1b518be8 + c8f77b3d), branch fix/remove-membership-free-trial, not yet merged; Every total on the admin revenue report, the payroll report, partner earnings and pay-period aggregation is a sum over mixed units the moment a second currency exists. |
| T-0703 | Money constants and thresholds are bare decimals with no currency | M | `done` | — | shipped 2026-09-12 (cdc8a204 + e333fc54), branch fix/remove-membership-free-trial, not yet merged; Six business rules are expressed as numbers that only mean anything in crowns. |
| T-0704 | A pay period holding two currencies can never be invoiced, and the period can still close | M | `done` | — | shipped 2026-09-12 (c69bb275), branch fix/remove-membership-free-trial, not yet merged; The payout-invoice currency is derived from the pay rows being invoiced and fails if they disagree — with no recovery path. |
| T-0705 | Admin goodwill credit lands in the default currency, and the admin sees only one balance | S | `done` | — | shipped 2026-09-12 (899acad1 + 3b2c184e), branch fix/remove-membership-free-trial, not yet merged; The issue command carries no currency and the admin screen renders accounts[0]. |
| T-0706 | The customer surfaces format everything as CZK and offer no way to choose a currency | M | `done` | — | shipped 2026-09-12 (eb92f345 + eb25f93b), branch fix/remove-membership-free-trial, not yet merged; The booking wizard has its own module-level CZK formatters, bound about twenty times, and the catalogue DTOs carry no currency at all. |
| T-0707 | FK_Orders_Currencies_CurrencyId is Cascade — deleting a currency would delete its orders | S | `done` | — | shipped 2026-09-12 (52b5c939), branch fix/remove-membership-free-trial, not yet merged; Every sibling money table is Restrict. Orders is not. |
| T-0708 | A payout can be issued in any currency to any bank account | M | `done` | — | shipped 2026-09-12 (206df58b), branch fix/remove-membership-free-trial, not yet merged; The payout-details model has no currency, and nothing checks that the account can receive what is being sent. |
| T-0709 | docs/ still describes ExchangeRate conversion as the live multicurrency mechanism | S | `done` | — | shipped 2026-09-12, branch fix/remove-membership-free-trial, not yet merged; The architecture pages describe a column that no longer exists and a conversion that no longer happens. |
| T-0710 | Cleansia Plus is priced per market (ADR-0059) | L | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `MembershipPlanPrice` per (plan, currency), `UserMembership.CurrencyId`, `GetPlans?countryId=`, subscribe/swap in the market's / the membership's currency, the webhook reads `Subscription.Currency`, the Stripe Customer-currency refusal classified (sandbox probe: not enforced today); clients/spec regen is the orchestrator's. |
| T-0711 | The market directory and the per-market copy figures (ADR-0058 D1–D2, ADR-0060 D1–D2) | M | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `Market/GetOverview` on both customer hosts, the no-show credit authored per currency, the insurance ceiling per country, the servicing gate, `Country.IsoAlpha2`; clients/spec regen is the orchestrator's after T-0710. |
| T-0712 | Design pass — the market chip and the market selector, three clients | S | `done` | — | delivered 2026-09-13 (canvas + `design-spec.md`, no repo edits); chip = `CleansiaChip` / `.cl-chip`, control with ≥ 2 markets, static label on the web quote with one, hidden on mobile with one; Q-DESIGN-01 → `Country.IsoAlpha2`. |
| T-0713 | Admin web — per-currency plan prices, the no-show credit, the insurance ceiling | M | `done` | — | shipped 2026-09-13 (a2d65191 + bae82513), branch fix/remove-membership-free-trial, not yet merged; a price block per currency (every block optional), `noShowCredit` on the currency form, alpha-2 + Market section on the country form, the serviced toggle snaps back on `country.market_not_ready`. |
| T-0714 | Customer web — the market is chosen, remembered, and read | L | `done` | — | shipped 2026-09-13 (76791b8a), branch fix/remove-membership-free-trial, not yet merged; `preferred_market` cookie only, market store + APP_INITIALIZER on both branches, `<cleansia-market-switcher>` pill/chip, every pre-address reader sends the market's `countryId`, Plus surfaces per market, membership labels from `GetMine.currencyCode`. Finding: the customer app's transfer cache was dead (`withCredentials`) — fixed in 848166e5 (anonymous GETs go credential-less, `auth.interceptor.spec.ts` pins it). |
| T-0715 | Customer web — money figures in copy come from the market | S | `done` | — | shipped 2026-09-13 (2ba96d5d), branch fix/remove-membership-free-trial, not yet merged; `we_cancel_value` `{{amount}}` + refund-only key, `terms_page.section3_text` `{{currency}}` + a new `_no_market` key, web checker pins flipped. |
| T-0716 | Android customer — the market preference, chip and readers | M | `done` | — | shipped 2026-09-13 (6c25b2e0), branch fix/remove-membership-free-trial, not yet merged; `MarketRepository` + `MarketScreen` + home chip (≥ 2 markets), catalogue / plans / subscribe / recurring / rewards read the market, labels from the payload, annual switch gated on the membership's currency; the Plus Google Pay sheet reads `market.isoAlpha2` (reported deviation). |
| T-0717 | Android customer — copy per market | S | `done` | — | shipped 2026-09-13 (12144ffe), branch fix/remove-membership-free-trial, not yet merged; trust badge + FAQ `%1$s` with no-figure twins, push body without a figure, `SeasonalCard` deleted. |
| T-0718 | iOS customer — the market preference, chip and readers | M | `done` | — | shipped 2026-09-13 (144a3b07), branch fix/remove-membership-free-trial, not yet merged; `MarketPreferenceStore` + `MarketStore` + `MarketPickerView` + home chip (≥ 2 markets), readers per market, annual switch gated, plans-failed retry state; tests written, the owner's Xcode build confirms the generated names. |
| T-0719 | iOS customer — copy per market | S | `done` | — | shipped 2026-09-13 (c3540d8b), branch fix/remove-membership-free-trial, not yet merged; `%1$@` insurance copy with no-figure twins, push body without a figure in both catalogues, seasonal section deleted; the customer catalogue carries no currency word anywhere. |
| T-0720 | Docs pass — three ADRs accepted, living docs, roles, business rules | M | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; ADR-0058/0059/0060 `accepted` with every lane deviation recorded, `#market` on business-rules, `api/markets-and-memberships`, two role cards, the expansion checklist with both gates; `npm run build` still to be run by the orchestrator. |
| T-0721 | The five 2026-09-13 market rulings — default-market flag, seeded figures, push amount, Stripe Customer per currency | M | `done` | — | shipped 2026-09-13 (e3fb1227), branch fix/remove-membership-free-trial, not yet merged; `CountryConfiguration.IsDefaultMarket` under a partial unique index with `PUT api/AdminCountry/{id}/default-market`, CZE flagged and its 1 000 000 CZK ceiling seeded, placeholder apology credits on every seeded currency, `order.no_cleaner_refunded` carries the formatted `amount`, `UserStripeCustomers` gives a user one Stripe Customer per currency; `Initial` regenerated (`20260913080510`), DEV drop owed at deploy, NSwag and the mobile spec not regenerated. |
| T-0722 | Tenancy is active from day one — tenant registry, operator map, NOT NULL TenantId, index terms, seed re-homing, fixtures (one regen) | L | `done` | — | shipped 2026-09-13 (e5a45de3, then 5a5f4681 for the default-market operator gate, the dead confirm-token check and the readable pay-config index name), branch fix/remove-membership-free-trial, not yet merged; `Tenants` + `CountryConfiguration.OperatorTenantId`, `TenantId` NOT NULL on every stamped table but `OutboxMessages`/`DeadLetters`, `LoyaltyTierConfig` tenantless, `IX_Users_Email` global, `OrderReceipts (TenantId, ReceiptNumber)`, `IX_EmployeePayConfigs_Tenant_Scope`, seed re-homed to `cleansia-cz`, `Initial` regenerated (final id `20260913132759`), DEV drop owed at deploy (MS-2). T-0723–T-0725 landed in the same commit: NOT NULL is not green without the scope behaviour and the token-mint override. Clients and both mobile specs regenerated in 9bc5da2a. |
| T-0723 | Anonymous requests resolve their operator from the market (ADR-0061 D3) | M | `done` | — | shipped 2026-09-13 inside the T-0722 group (e5a45de3), branch fix/remove-membership-free-trial, not yet merged; `IOperatorScopedRequest` on the nine D3 records (six gain optional `countryId`), `OperatorTenantResolver` (the directory's default choice and its three predicates), `OperatorTenantScopeBehavior` before validation, `tenant.not_found`, `RequestPromoCode` envelope carries the operator. Clients do not send `countryId` yet (T-0728); the customer and partner web clients and both mobile specs regenerated in 9bc5da2a. |
| T-0724 | Identity paths adopt the user's tenant; email is one identity; secret-keyed reads bypass and pin (ADR-0061 D4/D5.1) | M | `done` | — | shipped 2026-09-13 inside the T-0722 group (e5a45de3 + 5a5f4681), branch fix/remove-membership-free-trial, not yet merged; `TokenService` and `RefreshToken` set the override from the user, `Register`/`RegisterEmployee`/`CreateAdminUser`/`ResendConfirmationEmail` pre-checks tenant-ignoring, the unconfirmed re-registration branch compares tenants, `GetByConfirmationCodeIgnoringTenantAsync` (the dead `ExistsWithConfirmationCodeAsync` deleted — ADR-0051 O-2 closed), `LookupOrder`/`LookupOrderBatch` tenant-ignoring on the secret. |
| T-0725 | Tenant / country / currency agreement rules; markets without an operator are not listed (ADR-0061 D6) | M | `done` | — | shipped 2026-09-13 inside the T-0722 group (e5a45de3 + 5a5f4681), branch fix/remove-membership-free-trial, not yet merged; `order.country_operator_mismatch` (unconditional, on the cached country), `employee.work_country_operator_mismatch`, `GetMarkets` fourth predicate with an error log, `SetDefaultMarket` refuses a market with no operator. Locales are T-0727's. |
| T-0726 | Second-tenant isolation host test and the seeded-DB closure scan (ADR-0061 D11) | M | `done` | — | shipped 2026-09-13 with T-0722 (e5a45de3), branch fix/remove-membership-free-trial, not yet merged; `SecondTenantIsolationHostTests`, `SeededDatabaseHasNoOrphanTenantRowsTests` (a–d), `AnonymousWriterLandsInMarketOperatorTests`, `UsersEmailGloballyUniqueTests`, `TenantIdNotNullEnforcedTests`, the two-operator `CleanupStalePendingOrders` smoke, the filter-clause mutation check run locally and restored. |
| T-0727 | Error-key locales for `tenant.not_found`, `order.country_operator_mismatch`, `employee.work_country_operator_mismatch` and the newly reachable `country.not_serviced` — five locales, every app that can reach them | S | `done` | — | shipped 2026-09-13 (web 12ced799, Android e3c8fee0, iOS 89967da2); the row lagged the tree until the 2026-09-16 sweep |
| T-0728 | Clients send the chosen market on register / social / promo request / referral validate; `MarketController` on the two partner hosts; the partner register picker reads `Market/GetOverview` | M | `done` | — | shipped 2026-09-13 (MarketController on the partner hosts e94abf08, partner picker 99afe8f0, web 943d74f9, Android 134477b1, iOS 7bc530c9); the row lagged the tree until the 2026-09-16 sweep |
| T-0729 | Docs — ADR-0061 accepted, ADR-0050 accepted as amended, the living note, S1/S8, CLAUDE.md landmine 1, model, roles, business rules, API pages, catalog, backlog, the owed DEV drop | M | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; every sentence verified against e5a45de3 / 5a5f4681 / 9bc5da2a (the resolver, the behaviour, `Register`, `TokenService`, the EC, the DDL, the seed). Q-TENANCY-01..05 filed with their defaults; MS-2 names `20260913132759`. Docs build not run (no shell in the lane — the orchestrator's). |
| T-0730 | T-AUD-1 — The customer audit table, written by the shipped pipeline through an audience-aware gate (ADR-0062 D1/D2/D5/D7) | L | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `CustomerActionAudit` (`TenantId` NOT NULL, private setters + factory, `Pseudonymise()`), `AuditGate` with two arms (replaces `AdminMutationGate`), `AuditErrorCode.Resolve` on both arms (Q-AUD-O2), `ResolveExact`, `RecordEvidence`, factory/writer/sink overloads, erasure roster row + tracked pseudonymisation, immutability + gate + Postgres pipeline tests; review fix fa2d5672 (signed-in rows record the session's device claim). `Initial` regenerated — final id `20260913174821` after T-0733's index; DEV drop owed at deploy (MS-2 still names the previous id). |
| T-0731 | T-AUD-2 — The thirteen money and entitlement acts carry a descriptor and a typed evidence record (ADR-0062 D3) | M | `done` | — | shipped 2026-09-13 (f4eb839b, fixes fab29459), branch fix/remove-membership-free-trial, not yet merged; sixteen-command roster test, PII guard (identity + free-text + live-secret names), rate-limit guard on every marked customer action, `CreateOrder.Validator` gains `LanguageValidator`, marker gains `ResourceIdProperty`. Regen `2dfa61c2` put `termsAccepted` on `CreateOrderCommand`; the wizard sends it when ticked. |
| T-0732 | T-AUD-3 — Terms and privacy get a version; registration and consent are recorded server-side (ADR-0062 D4; absorbs T-0686) | M | `done` | — | shipped 2026-09-13 (033f13ad + fixes), branch fix/remove-membership-free-trial, not yet merged; `LegalDocumentVersions`, `UserConsent.DocumentVersion` + `AcceptVersion` (a *different* version), `ConsentService` versions Customer subjects only, `Register` grants + `RegistrationEvidence`, Google/Apple provisioning grants with version and no audit row, `ConsentEvidence` top-level, enums by name on all three tables, parity checker pins the two version keys, customer web register form sends the tick, customer-side `signup-consent.service.ts` deleted, the two consent-type refusals carry `invalid_enum_value`. |
| T-0733 | T-AUD-4 — Support reads the customer trail: paged list, entry detail, per-customer / per-resource timeline (ADR-0062 D6 reads) | M | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `GET api/CustomerAudit/{get-paged,get-by-id/{auditId},timeline}` under `CanViewAuditLog`, `/customeraudit/` request-log suppression, three HostTests policy classes, `audit.timeline.filter_required` in five admin locales; `GetActionTimeline` is a validated paged query (`BusinessResult<PagedData<T>>`, A2 deviation declared; `Sort` ignored, `Limit ≤ 100`, 1 000-id cap); `EmployeeActionAudits (OrderId, CreatedOn DESC)` index folded into `20260913174821`. |
| T-0734 | T-AUD-5 — The subject export carries the customer's rows; the admin export becomes an audited Command (ADR-0062 D5/D6-export) | S | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `customerActions` section from both callers, `AdminExportUserData.Command` marked `gdpr.user.export` with a count-only snapshot on `POST api/v1/AdminGdpr/export/{userId}`, `ExportUserData.Command` on `POST api/v1/Gdpr/export` (no marker), both commit their `GdprRequest` (Q-AUD-O3); `SubjectExportAuditTests` on real Postgres. Reported: the consent section still omits IP/UA. Regen `2dfa61c2` made all three web clients and both mobile specs `POST` the export. |
| T-0735 | T-AUD-6 — Admin panel: customer list, entry detail, per-customer timeline + export button, history links (ADR-0062 D6 web) | L | `done` | — | shipped 2026-09-13, branch fix/remove-membership-free-trial, not yet merged; `/audit-log` segment switch, `/audit-log/customers`, `/audit-log/customers/entry/:auditId` (payload table + raw JSON), `/audit-log/resource/:type/:id` on the timeline with source badges, `/customers/:userId` Timeline + Export subject data (`CanAdminExportUserData`), fifteen action labels × five locales pinned by `customer-audit-actions.spec.ts`. AC1 as amended: the resource history reached from order and dispute detail names who acted and links a customer actor to `/customers/:userId` (no customer id on the partner-served order/dispute DTOs). |
| T-0736 | T-AUD-7 — Customer audit rows expire three years after their own act, per row (ADR-0062 D5-retention) | S | `done` | — | shipped 2026-09-13 (2e255b90, fixes 275822c4 + fa2d5672), branch fix/remove-membership-free-trial, not yet merged; `retention.customer_audit.years` (default 3, ≤ 0 refused with a warning), one `RunSafeAsync` task under the master switch, `DeleteExpiredAsync(cutoff)` batched and tenant-agnostic, pinned on SQLite and across two tenants on Postgres; admin/employee tables untouched. |
| T-0737 | T-AUD-8 — Docs: ADR-0062 accepted, security rules, model, roles, features, business rules, flows, backlog | S | `done` | — | shipped 2026-09-14, branch fix/remove-membership-free-trial, not yet merged; ADR-0062 `accepted` with the nine rulings and an *As shipped* block, index (62 records) + sidebar, dated notes on ADR-0012, S2/S5/S6, model (85 entities), two new role cards + the `AuditGate` fix on the payout card, features, business-rules `#customer-record`, five flow pages, T-0730–T-0737 + T-0686 rows, Q-AUD-L1..L6 / O1..O3. Not touched (outside the lane's brief): `agents/architecture/decisions/audit-log.md`, `agents/knowledge/patterns-backend.md`, MS-2, `CHANGELOG.md`. Docs build not run (no shell in the lane — the orchestrator's). |
| T-0738 | Erasure is one commit, reaches guest rows, and keeps dispute text three years (A1, A7, L3) | M | `done` | — | shipped 2026-09-14 (fe51557c, then c26aa541), branch fix/remove-membership-free-trial, not yet merged; `StageRevokeAllForUserAsync` rides the erasure's single commit (a poisoned commit leaves the subject, their sessions and the absent request untouched — `ErasureSingleCommitTests`); `Dispute.TextRetainedUntil` + `retention.dispute_text.years` (3, floor > 0) + the `DisputeText` retention task; evidence blobs still deleted; roster verdict *RetainedForDefence*. **A7 not delivered and cannot be by order**: `PseudonymiseGuestRowsForOrdersAsync` was built and deleted on review — `Order.UserId` is set at creation and only ever nulled, so a guest row on the subject's own order cannot exist; the e-mail lever is Q-GDPR-01. `Initial` regenerated → `20260914115922` (final, after T-0742's). |
| T-0739 | A failed erasure is on record, retried once a day, and visible to admins (A3) | M | `done` | — | shipped 2026-09-14 (8b24e18a + dc20f75f backend; 50026761 + bd00c7c1 admin web; 2a919b7d the pending rule), branch fix/remove-membership-free-trial, not yet merged; `IErasureAttempt` + `ErasureFailureCaptureBehavior` + `OutOfBandGdprDeletionFailureSink` write a `Failed` `GdprRequest` (notes e-mail-redacted, appended per attempt; `ProcessedBy` `self` / admin / `system`); `AdminRetryUserDeletion` on `POST api/v1/AdminGdpr/requests/{id}/retry-deletion` (`CanAdminDeleteUserAccount`, `gdpr.user.delete.retry`; `gdpr.request_not_found`, `gdpr.request_not_retryable`); `RetryFailedUserDeletions` daily 05:00 UTC under the retention switch; `GetAllGdprRequests` status filter; `HasPendingRequestAsync` counts every non-`Completed` row; admin data-protection page: status select, Retry on Failed/stale-Processing deletion rows, stale-response guard. |
| T-0740 | Validation runs for every request type (A2) | S | `done` | — | shipped 2026-09-14 (6f874295 + 7004ada4), branch fix/remove-membership-free-trial, not yet merged; `ValidationPipelineBehavior`'s `BusinessResult` constraint lifted, `RequestValidationException` + `RequestValidationExceptionFilterAttribute` answer the same 400 ProblemDetails; `GetAllGdprRequests.Validator` runs (`validation.page_size_exceeded`); `GetActionTimeline` returns `PagedData<T>` (A2 baseline row + checker allowance withdrawn); `EveryValidatorIsReachedByThePipelineTests`; the audit failure row records the first rule's key on the thrown arm. Admin client regenerated in f433f5ff. |
| T-0741 | The database pool is owned by the host (A4) | S | `done` | — | shipped 2026-09-14 (777a9555 + 3f2a0052), branch fix/remove-membership-free-trial, not yet merged; `AddDbContextBindings` registers `NpgsqlDataSource` through a factory (built above it so the eager type-catalog probe still runs at composition), the DbContext draws from the container's one source, `BootDatabaseIoTests` pins the disposal; the HostTests idle-pruning workaround removed (218 hosts close their own pools); `BaseIntegrationTest`'s `Pooling=false` rationale rewritten (the never-disposed provider `BaseTransactionalPostgresSqlTest` builds). |
| T-0742 | Legal documents are versioned by effective date and stored, and consent stamps the version (L2) | L | `done` | — | shipped 2026-09-14 (87711f25 + 4a93f125 backend/db; 0ff0dacf clients; 0c0de368 + 599df89c customer web; 290761c4 + 4c855fa7 admin web), branch fix/remove-membership-free-trial, not yet merged; `LegalDocument` + `LegalDocumentText` (tenantless, `Version = EffectiveFrom` as `yyyy-MM-dd`, unique per audience/type/market/version), `UserConsent.LegalDocumentId`, embedded seed `Seed/Legal/customer/{terms-of-service,privacy-policy}/any/2026-09-14/{en,cs,sk,uk,ru}.md`, `LegalDocumentSeedHostedService` at every host start after the type-catalog initializer (immutable once in force — logged and skipped), `GET api/Legal/GetDocument` anonymous on both customer hosts (Markdig, `DisableHtml`, `{{currency}}` from the market), `AdminLegalController` under `CanViewCountryConfigurations`, `ConsentService` stamps version + id and compares document identity on regrant, `LegalDocumentVersions` deleted, locale `terms_page.section*` keys deleted, parity checker §4 gone and the no-baked-figures rule reads the newest seed version; customer `legal-pages` fetch-and-render with a component spec; admin `/legal-documents` read-only lib. → ADR-0063. |
| T-0743 | The export shows what was consented and the customer's own export is audited (A5, O3) | S | `done` | — | shipped 2026-09-14 (4ecfd9bd + 1a4fd471), branch fix/remove-membership-free-trial, not yet merged; `GdprExportConsentDto` + `IpAddress`, `UserAgent`, `DocumentVersion`, `LegalDocumentId` (they **survive erasure** on the withdrawn row — `RetainedByPolicy`, pinned); `ExportUserData` marked `customer.gdpr.export` with `GdprExportEvidence(OrderCount, ConsentCount, CustomerActionCount)` — three counts, not the ticket's four; roster 17/16 at that point; admin catalogue + five locales. The JSON export still carries no dispute section — Q-GDPR-02. |
| T-0744 | Sign-in and session acts leave customer audit rows; admin refusals are traceable by order (L5, O2) | M | `done` | — | shipped 2026-09-14 (048e6efb + f8e909d9), branch fix/remove-membership-free-trial, not yet merged; `customer.session.login` on `Login`/`MobileLogin`/`GoogleAuth`/`AppleAuth` (`LoginEvidence`; the social provisioning branch calls `DeclineSuccessRow`), `.logout`, `customer.password.reset_requested`/`.reset_completed`, `customer.account.email_confirmed`; `AuditGate.Resolve` takes `IHostAudienceProvider` (anonymous → customer table on a customer host only); the five session commands implement `IOperatorScopedRequest` with `CountryId => null`; `TokenService` adopts the tenant before the confirmation check and the two reset handlers adopt it themselves (`SessionAuditTests`); `AdminCancelOrder` `order.cancel`, `AdminReassignOrder` `order.reassign`, `UpdateDisputeStatus` `dispute.status.update`, `AddDisputeMessage` `dispute.message.add` with `ResourceType`; roster 25/21/9; five admin labels. Reported, not built: a refused sign-in on a known account is `UserId = null` (Q-AUD-O4); an Administrator's sign-out on the admin host lands in the admin table under `customer.session.logout`. |
| T-0745 | The server refuses a customer registration or booking without the terms tick, and every client sends it (L4) | M | `done` | — | shipped 2026-09-14 (b5b0f6b5 + e48806fd Android; 922f0331 + fcc2ac9c iOS; 444fcda1 backend; ecbf193a iOS partner roster), branch fix/remove-membership-free-trial, not yet merged; `Register.Validator` and `CreateOrder.Validator` (unless both consents held, granted and not withdrawn; the tick short-circuits the read; ahead of the price chain) refuse `consent.terms_not_accepted`; `GoogleAuth`/`AppleAuth` provisioning keeps `auth.social_account_not_found` (ratified); `ConfirmRecurringOrder` and `RegisterEmployee` not gated; Android customer sends the tick on registration and on the booking (review-step sentence unless both consents on record); iOS customer the same and its parking deleted; the key in five locales of customer + partner web, Android customer, iOS Core (+ partner roster). XCTest unrun on Windows. Residual: `CreateOrder` grants no consent rows (A6). |
| T-0746 | The incident file is a PDF (L6) | M | `done` | — | shipped 2026-09-14 (34430596 + cb26e466 backend; f433f5ff clients; d57922fc + c08f06f1 admin web), branch fix/remove-membership-free-trial, not yet merged; `POST api/v1/AdminGdpr/incident-file/{userId}?orderId=` (`CanAdminExportUserData`, `auth` window, `application/pdf`, `incident-<userId>-<yyyyMMdd>.pdf`), `ExportCustomerIncidentFile` marked `gdpr.user.incident_file` with `IncidentFileSnapshot` incl. the data SHA-256; `IncidentFileService` (identity / orders / disputes / consents / trail, 2 000 rows per source, `ProvenOrderIdsAsync` shared with the timeline; scoped = the subject's rows + guest rows, a bystander's probe excluded), `IncidentFileSections` + `IncidentFileDigest` (what the hash proves is stated on the type), QuestPDF layout with footer + Integrity block; admin `/customers/:userId` button with an order-scope **picker** over the subject's own orders (2026-09-16: `OrderFilter.UserId`, admin-only, `OrderListUserScopeTests` in 433ce829; admin client regenerated + `loadSubjectOrders` picker in 9ad0b0ed — until then a typed order id) and the order-detail action under both policies, `FileDownloadService`, `incidentFileName` in `@cleansia/admin-services`, `order.not_found` mapped in the snackbar. |
| T-0747 | The partner register form sends the tick instead of parking it (A8) — step 1 backend, step 2 partner web | S | `done` | — | shipped 2026-09-14 (19dde3e5 step 1 + 752bc963 partner client/spec regen; db0607a3 step 2), branch fix/remove-membership-free-trial, not yet merged; `RegisterEmployee.Command.TermsAccepted` (`bool?`, null never refused) grants `TermsOfService` + `PrivacyPolicy` with no document on both branches (`RegisterEmployeeConsentTests`); `PartnerAuthService.registerEmployee(…, termsAccepted)`, `SignupConsentService` + spec + export + the `setSession` flush deleted, `grep signup-consent src/Cleansia.App` empty. Both mobile partner apps parked the tick until 2026-09-16: e336dbc4 sends `termsAccepted` on `RegisterEmployee` from the Android and iOS register forms and deletes the parking (`ConsentModule`, `GdprConsentClient`; `PartnerSignupConsentClient`, `SignupConsentFlowTests`), 9ad0b0ed removes the now-unbound iOS Core consent seam and `ConsentWireStub`; recorded in ADR-0062 D4 as amended 2026-09-16. |
| T-0748 | Four administrator roles — Administrator / Manager / Support / Accountant: the model, the sets, the map, the admin-host reads, the assignment, the matrix (ADR-0066; was "Roles beyond Administrator", Q-AUD-O1) | L | `done` | — | shipped 2026-09-19 (56fa5aa2 + 45462202), branch fix/remove-membership-free-trial, not yet merged; `AdminRole` column (NOT NULL iff Administrator, `CK_Users_AdminRole_Profile`) + the `admin_role` claim, five administrator sets (`AdminRoleSets`, four `RequireAssertion` registrations, startup semantics), `PolicyBuilder.Map` per ADR-0066 D3 with the eleven new constants (eight admin-host read twins, `CanViewLegalDocuments`, `CanSetAdminRole`, `CanViewAdminNotifications`) and sixteen attribute edits, `POST api/AdminUser/{userId}/role` + `DemoteAdministratorIfAnotherRemainsAsync` / `DeactivateAdministratorIfAnotherRemainsAsync` each one transaction under `pg_advisory_xact_lock(hashtext(tenantId))`, `ActorAdminRole` on the audit row + filter, the notifier narrowing by the entry's audience, `AdminRolePolicyMatrixTests` + `AdminHostPermissionCoverageTests` + four behavioural host classes + the Testcontainers race and constraint tests; **`Initial` → `20260919142517`** (the batch's one regen; DEV drop at the deploy, MS-2); the admin client regenerated. Unit 6174 green at the lane; the Postgres and host suites compiled, first executed by CI. The `User.SetAdminRole()` domain writer was removed in 45462202 (no caller; the guarded update is the only writer). Every O-R1..O-R10 shipped as its default; F12/F13 stay findings. Filed 2026-09-14, opened 2026-09-19 on *"let's do those 3 for now"*. |
| T-0749 | Docs for the audit-log follow-up batch — ADR-0063, ADR-0062/0061 amended in place, living pages, changelog, backlog, knowledge | M | `done` | — | shipped 2026-09-14, branch fix/remove-membership-free-trial, not yet merged; ADR-0063 new, ADR-0062 dated *As amended* blocks on D3/D4/D5/D6/D7/D8 + Consequences + Verification + §Rulings, ADR-0061 D3/D4 amended, index (63) + sidebar, S2/S5/S6, model (87), `legal-document` + `incident-file` cards + the two audit cards, features, business rules (terms gate, retention table, dispute text, failed erasure, PDF), five flow pages + infrastructure timers + local-orchestration `#legal-seed` + the two app overviews, CHANGELOG, T-0738–T-0749 rows + ticket files, Q-AUD-* closed into ADR-0062 §Rulings and three new questions filed, consistency A2, patterns-frontend blankable-copy note, patterns-backend paged-validator note, the audit-log living note, MS-2 → `20260914115922`, CLAUDE.md counts (63 ADRs, 27 roles). Docs build not run (no shell in the lane — the orchestrator's). |
| T-0750 | A refused sign-in or reset on a KNOWN account names the account, and its row is stamped with the account's operator (Q-AUD-O4) | S | `done` | — | shipped 2026-09-15 (e53346cf, fix 380896cf), branch fix/remove-membership-free-trial, not yet merged; `LoginValidator` (one `ResolveAsync` — `Login`, `MobileLogin`, the unmarked partner/admin sign-ins), `ChangePassword`, `ConfirmUserEmail`, `RequestPasswordChange`'s account predicate and the `GoogleAuth`/`AppleAuth` handlers name the account through `RecordEvidence("User", id, null, actorUserId: id)` before refusing; both failure arms drain the snapshot (subject + resource, `PayloadJson` null, session wins); `OutOfBandAuditFailureSink` stamps a subject-named row with the subject's `TenantId` read past the filter (nameless rows keep the ambient stamp); an unknown address names nobody, unchanged. `SessionAuditTests` on Postgres (second operator), `OutOfBandAuditFailureSinkTenantTests`. No schema change. Recorded in ADR-0062 D3/D7 as amended 2026-09-15. |
| T-0751 | Erasure and export reach the guest bookings placed with the erased account's e-mail (Q-GDPR-01) | M | `done` | — | shipped 2026-09-15 (6ab64fff, fix ca6dc84e), branch fix/remove-membership-free-trial, not yet merged; `SubjectOrders.Of(userId, email)` in `Core.Domain/Orders` (the account's orders, or `UserId == null` with a case-folded `CustomerEmail` match), read **past the tenant filter** by the walk and the export (a guest checkout is stamped with the market's operator; pinned by the predicate, then by the ids); the ended guest bookings go through the per-order path (photo, customer data, address, pay rows) and their guest audit rows are blanked by `PseudonymiseGuestRowsForOrdersAsync` (the second sanctioned `Pseudonymise` caller; the immutability walk expects both). **The fix reverted the review's own first cut:** the blocking check is back to the account's own live orders and a LIVE guest booking is left out of the walk (no cancel path — T-0753; whether it should refuse is Q-GDPR-03). The export's orders section is the same set; its trail stays the account's own rows (a guest row's IP/device may be a stranger's). The Postgres proof the lane could not run (Docker down) landed with T-0752's `Features.Gdpr` 38/38. No schema change. Recorded in ADR-0062 D5 as amended 2026-09-15. |
| T-0752 | The subject export carries disputes, the self-export's request row names `self`, and the incident file prints the operating company and market (Q-GDPR-02 + two S4/S6 minors) | S | `done` | — | shipped 2026-09-15 (9c0b9801, fix 31fac812), branch fix/remove-membership-free-trial, not yet merged; `GdprExportDisputeDto` (enum names, description, notes, refund + currency code, messages as author role/time/text, evidence names; filed on the account or on a `SubjectOrders` order, text as stored under the three-year window) from both callers, `DisputeCount` on `GdprExportEvidence` and `GdprExportSnapshot`; `ExportUserData` stamps `GdprAuditReasons.SelfActor` into `ProcessedBy` (was the live e-mail); `IncidentFileSubject.Operator` → `OperatorName` + `Market` from the market registry, the tenant id absent from the hashed section model. `SubjectExportDisputesTests`, `IncidentFileTests`, `ExportUserDataAuditEvidenceTests`; IntegrationTests `Features.Gdpr` 38/38, HostTests 239/239. No schema change. The NSwag regen landed in d99f4b26 (all three web clients carry `disputes`). |
| T-0753 | A guest can cancel their booking — anonymous cancel keyed like the guest lookup, on web + Android + iOS | M | `done` | — | shipped 2026-09-16 — backend e5d28767, web + Android 0f4d301a, iOS 28ccf8b1 + 4156fc1a (guest cancel rides the generated client; XCTest unrun on Windows, iOS CI builds the target) |

> **T-0699–T-0709 filed 2026-09-10**, split out of T-0688 by a readiness audit of the multicurrency
> programme. Every claim was re-verified against the tree that day and the ones that did not survive
> were not filed — three audit claims were refuted outright, and two more were narrowed. Two defects
> the audit turned up were fixed rather than filed, because one destroyed data and both were reachable
> with EUR still switched off: `CreatePayConfig`'s duplicate probes ignored the currency, so a second
> currency's rate could never be created; and `BulkCreateEmployeePayConfigs` deleted rates it could
> not replace. Both are in `b269efe4`.


> **T-0730–T-0737 filed 2026-09-14 as `done`** — the customer audit programme (ADR-0062), worked on
> the branch under the working ids T-AUD-1 … T-AUD-8 and numbered here at the end, after the tenancy
> tickets. Each row names what shipped and what did not; the two gaps (T-0731's client regen for
> `CreateOrderCommand.termsAccepted`, T-0735's customer link from order/dispute detail) are in the rows
> and in ADR-0062 §Consequences, not silently closed.

> **T-0738–T-0749 filed 2026-09-14** — the owner's rulings on ADR-0062's nine questions and eight
> review findings, worked as one batch on the branch and numbered here at the end. Every row names
> its commits and what did *not* ship (T-0738's guest-row walk, T-0743's dispute section, T-0744's
> known-account attribution; T-0746's order picker and T-0747's mobile parking followed on 2026-09-16) — in the row and in
> ADR-0062's dated amendments, not silently closed. T-0748 is the one open row.

> **T-0750–T-0753 filed 2026-09-15** — the owner's rulings on the three questions that batch left
> open (Q-AUD-O4, Q-GDPR-01, Q-GDPR-02 — all *yes*), shipped as T-0750 … T-0752 with their review
> fixes, plus T-0753 filed open on the owner's word. The three earlier rows that named those questions
> as open (T-0738, T-0743, T-0744) are superseded on that point by these; the rows above stand as the
> record of what shipped *then*. Open rows: T-0748, T-0753.

| T-0754 | The partner hosts stop provisioning customers — `Register` gone from both, `GoogleAuth` / `ConfirmUserEmail` off a customer host sign in an existing Employee or Administrator only | S | `done` | — | shipped 2026-09-15 (`db4f9dcf`, review `362f97ef`), branch fix/remove-membership-free-trial, not yet merged; owner ruling *"remove it"*. `POST api/Auth/Register` deleted from `Web.Partner` and `Web.Mobile.Partner`; `GoogleAuth` decides by `IHostAudienceProvider` in the handler (`auth.social_account_not_found` for a first-time identity, `auth.insufficient_privileges` for a Customer account — the `PartnerLogin` rule), `ConfirmUserEmail` the same; `TokenService.GenerateTokenAsync` throws on an audience/profile pair the host does not serve. `SessionAuditRouteTests` rewritten, `AuditGateTests` docstring, partner parity spec's `Auth/Register` scoped line removed, HostTests on both partner hosts. Clients/spec regenerated in `1f3c70b8` (a deliberate contract shrink; the partner web's dead `register()` dropped with it). |
| T-0755 | An administrator's sign-in and sign-out are admin acts with admin labels | S | `done` | — | shipped 2026-09-15 (`ac8cd84c`, review `540b93cc`), branch fix/remove-membership-free-trial, not yet merged; owner ruling *"change to be as admin"*. `AuditActionAttribute.AdminAction` (the admin arm's label for a customer-marked command; `Logout` declares `admin.session.logout`), `AuditEntryFactory.Build` writes `descriptor.AdminAction`; `AdminLogin` marked `admin.session.login` with the anonymous **admin** arm on the admin host (`AuditGate.HostServes`), a refused sign-in on a known account naming it and stamped with its company, an anonymous admin row recording the named account's own profile; `auth` tenant refusal added to the admin app's error contract. `CustomerAuditActionRosterTests`, `AuditActionDescriptorTests`, `AuditLogBehaviorTests`, `CustomerAuditPipelinePostgresTests`, `SessionAuditRouteTests` updated; a HostTest for the admin sign-in row. Recorded in ADR-0062 D1 as amended 2026-09-15. |
| T-0756 | Q-PUSH-01 recorded (the digest is not silenceable), T-0675 closed | XS | `done` | — | shipped 2026-09-15 with the docs lane, branch fix/remove-membership-free-trial, not yet merged; `FeedKeysetClientReadinessTests` doc reads the owner's ruling instead of "an omission", `NotificationEventCatalog.ReminderTomorrow` carries the date; ADR-0054 §Rulings; `business-rules#cleaner-non-mutable`; Q-PUSH-01 deleted from `questions/open.md`. T-0675 closed on ground truth (its row); MS-14 discharged. |
| T-0757 | Each operating company numbers its own payout invoices — the counter keyed `(TenantId, Year, Scope)`, `INV-YYYY-NNNNNN` per company, both `EmployeeInvoices` reference indexes per company | M | `done` | — | shipped 2026-09-15 (`33dda62d`, review `8c3beb8a`), branch fix/remove-membership-free-trial, not yet merged; owner ruling Q-TENANCY-02 *"separate everything"*. `PayoutReferenceCounter : TenantAuditable` with `Scope` (`VariableSymbol` / `InvoiceNumber`), `IX_PayoutReferenceCounters_Tenant_Year_Scope` NULLS NOT DISTINCT, the repository reads `ITenantProvider` and arbitrates on the triple (no ambient company → `23502`); `IX_EmployeeInvoices_TenantId_InvoiceNumber` and `(TenantId, VariableSymbol)` filtered, both NULLS NOT DISTINCT; `PayoutReferenceAllocator.AllocateInvoiceNumberAsync` / `FormatInvoiceNumber`; the random-hex `GenerateInvoiceNumber` deleted; the sweep's invoice PDF addressed by employee id (review). `PayoutReferenceAllocatorTests` inverted (per company, two scopes, independent exhaustion, `23502`, DDL read from `information_schema`), thirteen-row NULLS roster, `PayoutReferenceProductionCensusTests` two-company case. Folded into the `Initial` regen (`20260915172310`; superseded by `20260915232921` on 2026-09-16 — T-0760). ADR-0046 superseding note; ADR-0061 D7/D9 as amended. |
| T-0758 | `TenantId` is a real foreign key on every stamped table and leaves `Auditable` — `TenantAuditable`, the 21 tenantless tables lose the column and its index | M | `done` | — | shipped 2026-09-15 (`43157f62`, comments `0751c055`), branch fix/remove-membership-free-trial, not yet merged; owner ruling Q-TENANCY-04 *"do the recommended step"*. `TenantAuditable : Auditable, ITenantEntity` carries `TenantId`; `Auditable` has none (`grep -n TenantId Auditable.cs` empty); `TenantAuditableEntityConfiguration` maps NOT NULL (bar the two envelopes), `HasOne<Tenant>().WithMany().HasForeignKey().OnDelete(Restrict)` and the index; the two `BaseEntity + ITenantEntity` audits by hand. 47 `FK_<T>_Tenants_TenantId` at this commit (48 after T-0757), 21 `IX_<tenantless>_TenantId` gone. `TenantIdRequiredModelTests` sweeps the FK and "no tenantless type has a `TenantId`", `InitialMigrationTenantDdlTests` pins the migration's own counts, `TenantForeignKeyEnforcedTests` a real `23503`; the eleven Postgres tests with ad-hoc tenant ids now use registered ones. `Initial` regenerated → **`20260915172310`** (final for Batch 1, after T-0757; regenerated again as **`20260915232921`** by T-0760 on 2026-09-16); DEV drop owed at deploy (MS-2). ADR-0061 D1/D8 as amended; CLAUDE.md landmine 1. |
| T-0759 | `TenantConfiguration` has a key catalogue, a reader that works under jobs, an admin writer and a Company settings page | M | `done` | — | shipped 2026-09-15 (backend `e7f2c6b9`, review `24df12ee`; admin web `a84d205c`, review `9f9bedb3`; consumers `c8bd9e13` + `c4fe56b9`), branch fix/remove-membership-free-trial, not yet merged; owner ruling Q-TENANCY-04 *"introduce reader and writer"*. `TenantSettingCatalog` (nine `retention.*` definitions — type, `min`/`max`, default; `tenant_setting.unknown_key` / `tenant_setting.invalid_value`), `TenantSettingReader.GetAsync(definition)` resolving under the ambient company with the default fallback, `DataRetentionBackgroundService` loops `ITenantRepository.GetAllIdsAsync` and runs the nine sweeps per company under its override (pinned on Postgres: B's one-year window deletes B's rows and keeps A's); `GetTenantSettings` / `SetTenantSetting` / `ResetTenantSetting` on `api/AdminTenantSettings/{get-all,set,reset/{key}}` under the existing `CanView/Update/DeleteTenantConfiguration` policies, audited `tenant_setting.set` / `.reset` with a key/value snapshot; five admin locales; HostTests for routes and policies. Admin web: `company-settings` lib (one row per catalogue key, inline typed edit, confirmed reset, `switchMap` load, in-flight guard; sidebar gated by `CanViewTenantConfigurations`; `tenant-setting-catalogue.spec.ts` ties the locales to the catalogue). Phase-A/B review: every envelope consumer guarded for the tenant override, the pay-calc consumer dead-letters a body it cannot run, an empty refresh profile pin fails closed. ADR-0061 O-4 as ruled; `/domain/roles/tenant-configuration`. |

> **T-0754–T-0759 filed 2026-09-15 as `done`** — Batch 1 of the tenancy programme: the owner's rulings
> on Q-TENANCY-01..05 (recorded in ADR-0061 §Rulings) and on two findings — the partner hosts
> provisioning customers (the batch's scout) and an administrator's session acts under a customer label
> (T-0744's review) — plus Q-PUSH-01. Worked on the branch and numbered here at the end. Batch 2 (a company's lifecycle —
> Q-TENANCY-03's a → b → c) and Batch 3 (cross-market booking on one account — Q-TENANCY-01) are the
> owner's to open; nothing is filed for them yet. Open rows: T-0748, T-0753.

| T-0760 | A company can be deactivated and reactivated — the Tenant row carries its lifecycle, "serviced" requires an active operator everywhere, its cleaners are refused on the partner audiences | M | `done` | — | shipped 2026-09-16 (`0601b0f2`, review `92c5d674`, cross-ticket review `3f2e8018`), branch fix/remove-membership-free-trial, not yet merged; ADR-0064 D1. `Tenant : Auditable` + `Auditable.Reactivated()` + nine lifecycle columns, the five predicates and the transition methods; `Initial` regenerated → **`20260915232921`** (the batch's one regen; both fixtures' raw inserts carry `CreatedBy`/`CreatedOn`; DEV drop owed at deploy — MS-2); `CountryRepository.GetServicedAsync`/`IsServicedAsync` carry the operator term, `OperatorTenantResolver` asks `IsServicedAsync`, `SetCountryServiced` refuses a deactivated operator (`country.market_not_ready`), `SetDefaultMarket` refuses its market through its own serviced rule (`country.not_serviced` — not `market_not_ready` as drafted), the materialiser skips a deactivated company; **review:** `CreateRecurringBooking` and `AddSavedAddress` read no serviced predicate at all and each gained one (`country.not_serviced`); `ICompanySignInGate` (scoped, memoised; Employee on Partner/Mobile; `auth.company_deactivated`) in `PartnerLogin`, `MobilePartnerLogin`, `GoogleAuth`, `AppleAuth`, `RefreshToken` and `TokenService`; `DeactivateCompany` (`tenant.not_found` first, `company.archived`, `company.already_deactivated`, `company.operates_default_market`), `ReactivateCompany`, `GetCompanyLifecycle` → `CompanyLifecycleDto` (actors by e-mail, `OperatesDefaultMarket`, the facts) over `ICompanySettlementReader`; `lifecycle.chargeback_horizon_days` (180, 0–730); `AdminCompanyLifecycleController` `get`/`deactivate`/`reactivate`; five `AdminOnly` policies; `company.deactivate`/`.reactivate` with `CompanyLifecycleSnapshot`. Locales: four `company.*` keys in the admin app, `auth.company_deactivated` in partner web/Android/iOS. Admin client regen `87ff17e0`. `TC-LC-DEACT-1..6` on Postgres and the hosts. |
| T-0762 | A company winds down — everyone is told, then from a date open orders are cancelled and refunded (failed refunds re-driven), templates paused, every Plus ended, credit discharged and the last period invoiced once the door is closed; the sweep converges on re-run | L | `done` | — | shipped 2026-09-16 (`b2deb803`, review `9a3e4bb3`, cross-ticket review `3f2e8018`, follow-up `e1a1beb3`), branch fix/remove-membership-free-trial, not yet merged; ADR-0064 D2. `IPlatformOrderCancellation` (`CancelAsync` + `RefundAsync`, the `AdminCancelOrder` body moved — its key still `refund:{id}:cancel`); `OrderCancellationReasons.CompanyWindDown`; `WindDownCompany.Command(FromDate?)` (`tenant.not_found`, `company.archived`, `company.wind_down_in_progress`, `company.wind_down_already_requested`, `common.required`, `company.wind_down_date_in_past` read in the easternmost market) + `CompanyWindDownDispatch` (outbox-deduplicated on `wind-down:{tenant}:{second}`; `DeactivateCompany` re-enqueues); `CompanyWindDownService` — no-op on missing/dateless/frozen, `StartWindDownRun` first, notices (`IsActive && IsEmailConfirmed && !anonymised`, `Approved` cleaners, keyset 200, `IQueueClient`, `Code = WindDownRequestedOn`, `CampaignProgress`), orders (`New`/`Confirmed`/`OnTheWay`, `Paid || Cash`, `WindDownCutoff` per market, floor lifted when deactivated, untracked status re-read, `refund:{id}:admin`, commit per order), the re-drive **minus this run's cancels** (reviewer #4), templates, every Active Plus, credit via `TryDebitAsync` keyed `wind-down-credit:{account}:{run}` when deactivated, `ClosePeriodAsync(period, note, openNext: false)` when deactivated + no open order + no order awaiting pay; the rollover opens no successor for a deactivated company and (`e1a1beb3`) excludes the closing period from the successor check; `EmailType` 8/9, two templates, five locales, `SendEmailHandler` arms; `company-wind-down` queue + poison + alert in Bicep. **Review:** a tender-neutral reason sentence on web/Android/iOS; `FiscalReconciliationService` and the settlement facts exclude cancelled orders. Locales: three `company.*` keys admin; `order.cancelled.company_wind_down` on the three customer clients (parity gate green). Full-suite evidence at review: Tests 5754/0, Integration 458/0, HostTests 277/0. |
| T-0763 | A company archives — once every live fact is settled and the chargeback horizon has passed, its books freeze at the request, a sealed bundle goes to blob storage with its hash on the row, and the commit refuses every later write except the law's; the Stripe webhook is acknowledged, never refused | L | `done` | — | shipped 2026-09-16 (`4d040b59`, review `44d30a28`, cross-ticket review `3f2e8018`), branch fix/remove-membership-free-trial, not yet merged; ADR-0064 D3. `ArchiveCompany.Command()` — the D3 table's fifteen refusals after `tenant.not_found`, `RequestArchive` = the freeze, one `company-archive` message (outbox-deduplicated); `CompanyArchiveService` (no-op when missing / archived / not frozen / a stale request instant; nineteen JSONL files keyset-paged through `CreateFileForWritingAsync`, receipt and invoice PDFs `CopyAsync` + hashed from the landed bytes, `manifest.json` last with tenant, ADR, `ISchemaVersionReader` migration id, freeze/build instants, `CompanyInfo` rows, per-file rows + SHA-256; **review:** uploaded if absent — the first manifest to land is the seal and the row carries its hash; `disputes.jsonl` drops the customer's `UserId`; the record guard matches a forbidden part anywhere in a name); `CompanyArchivedException`, `IArchiveWriteGate` (opened in `DataRetentionBackgroundService` and `GdprDeletionService` only — `LegalObligationGateCallSiteTests`), `ArchivedCompanyWriteGuard.AccountSurface` + the guard in `CommitAsync` (memoised per context), `ArchivedCompanyWriteGuardRosterTests` (every stamped type sorted; **nine** tenantless children named, not four), the 409 arm (`tenant.archived` on `TenantId`), `ArchivedCompanyWebhookAcknowledgeFilterAttribute` on the three webhook actions + `ArchivedCompanyDeadLetter` (fresh scope, `stripe-webhook`; **review:** the Error line asserted on the host), `CalculateOrderPayHandler`/`GenerateReceiptHandler` classify it permanent; `company-archives` container + `company-archive` queue/poison/alert in Bicep; the invoice download reads the blob by the name the upload wrote (`3f2e8018`). Locales: thirteen more `company.*` keys (twenty in all) + `tenant.archived` admin; `tenant.archived` on the three customer clients. `TC-LC-ARCH-1..5`. **Open:** `credit-accounts.jsonl` / `promo-code-redemptions.jsonl` carry `UserId` (Q-LC-03). |
| T-0764 | The admin app gets a Company lifecycle page — the state, the settlement facts with the date the seal becomes admissible, and the four acts behind confirmations | M | `done` | — | shipped 2026-09-16 (`b18de583`), branch fix/remove-membership-free-trial, not yet merged; ADR-0064 D4. `libs/cleansia-admin-features/company-lifecycle` on `/company-lifecycle` beside Company settings, sidebar under `CanViewCompanyLifecycle`: the state banner (five states, every stamp with the actor's e-mail, the manifest hash once archived), the sixteen facts as a table whose counts link to the list that settles them and whose horizon row reads *archive admissible from ‹date›* and links to Company settings, the four acts each behind a confirmation stating exactly what it does — *Wind down from a date* (date picker floored at today, the six consequences in the sweep's order; *Run wind-down again* once set, disabled while a run is in progress), *Deactivate* / *Reactivate*, *Archive* (*Build archive again* once frozen) — gated by policy and by the state table with the server's own refusal sentence as the reason line, refused client-side while the company holds the default market, a run is in progress or a fact is unsettled; the facade holds the DTO, the act in flight and the dialog in signals, `switchMap` loads, one act at a time, re-reads after every act admitted or refused; specs pin the fact table and act matrix per state against the generated DTO, the one-hour run staleness, the wire bodies, the confirm sentences, the refused act and the in-flight guard; the five lifecycle policies in the frontend `Policy` mirror; `AdminCompanyLifecycleClient` on the aggregator; two route enum entries; five locales with a copy spec against the generated client. |
| T-0761 | `order.cancelled.no_cleaner_available` is written by the unfilled-order sweep and rendered by no client — the copy and the three reason maps | S | `done` | — | shipped 2026-09-16 (`4fd9991b` + `608360e0`); **the row was left open by mistake and closed 2026-09-19 on ground truth**: five locales of copy on customer web (`pages.order_detail.cancellation_reason.no_cleaner_available` + the reason map in `order-detail.component.ts`), Android (`order_cancelled_reason_no_cleaner_available` in five `strings.xml` + `OrderDetailScreen.kt`) and iOS (`Localizable.xcstrings` + `CancellationReasonCopy.swift`), each with a test; `check-booking-policy-parity.mjs:396` reads `REASONS_NOT_YET_RENDERED = new Set()`. No backend change. Filed 2026-09-16 by the docs lane from the gate's allow-list. |
| T-0765 | A customer books in any market the holding serves — the order belongs to the market's operator, loyalty and credit follow the account, one holding Stripe account (Q-TENANCY-01/05) | M | `done` | — | shipped 2026-09-16 (7751c5fa + 556943f0 + 7168b757 backend, b097c2b6 regen, 0da053a0 clients, bb08fda3/3ab24fc8 docs; b1fe50eb iOS market labels + ace84b54 its catalog-guard allow-list entry), CI 7/7 on b5470030 |
| T-0766 | iOS: content draws under the status bar — audit every screen of both apps for safe-area handling and fix what overlays | S | `done` | — | shipped 2026-09-16 (b5470030 fix, a836a9e1 audit notes for both apps) |
| T-0767 | Responsiveness audit of the three web apps at phone width — a report of what breaks, no changes without the owner's pick | S | `done` | — | Reviewed report: 242 route/width attempts, seven layout/control-sizing groups and two runtime groups; isolated-fixture and runner-provenance limits disclosed. Fix selection remains with the owner |
| T-0776 | Responsiveness fixes from the 2026-09-16 audit — R01–R07, X01, X02 across the three web apps | M | `done` | — | shipped 2026-09-19 (55661dea X01 package edit renders; d7a54a59 R01 one navigation mode at exactly 768 px; f2562d67 X02 nested-form input binds its own control; 24299a99 R02–R06 standalone actions size to their label, with a template guard against labelled buttons in small slots; 0299e127 + 355ee133 R07 the 44 px touch floor as a hit ring around the painted control on the shared button, text field, password eye, menu button, sidebar close and table buttons — the visual pill unchanged), branch fix/remove-membership-free-trial, not yet merged; owner ruling 2026-09-19 D6 "go with recommended" taken as all nine groups (the layout groups are partner/admin, not customer). |

> **T-0760–T-0764 filed 2026-09-16 as `done`** — Batch 2 of the tenancy programme, the owner's ruling on
> Q-TENANCY-03 (*"I'd build up to (c)"*) under ADR-0064, worked serially on the branch: a → b → c on one
> backend lane, d on the frontend lane after c's client regen, the docs lane last. Each row names its
> commits and where the tree departed from the ADR's draft (recorded in ADR-0064 §*What shipped*). Three
> owner-facing items are filed as questions (Q-LC-01 administrators of a deactivated company, Q-LC-02 the
> bundle's estate, Q-LC-03 the `UserId` on two bundle files); one finding is filed as T-0761. Batch 3
> (cross-market booking on one account — Q-TENANCY-01) is the owner's to open. Open rows: T-0748,
> T-0753, T-0761.

| T-0768 | Administrators are told — the notifier, the feed audience, the admin feed routes, the first event (ADR-0065) | M | `done` | — | shipped 2026-09-19 (7078e617 + 8e4219b2), branch fix/remove-membership-free-trial, not yet merged; the notifier writes one feed row per administrator of the named company (recipients by argument, no push, no commit), the admin host serves /api/AdminNotification/* under CanViewAdminNotifications, a filed dispute is the first event; the admin client regenerated and both mobile specs re-dumped for the Admin audience member. Postgres and host tests written; first executed by CI / the main-session run once Docker is up. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0769 | Admin web — the bell and the notifications page (ADR-0065 D5) | M | `done` | — | shipped 2026-09-19 (8086c376 + fcfdb81d), branch fix/remove-membership-free-trial, not yet merged; a sidebar bell with the unread count and the /notifications page (paged list, mark read, mark all read with a microsecond-safe watermark, rows deep-link to order / dispute / customer / company); the sidebar gate has a spec that goes red without it. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0770 | Confirmed → New when the last cleaner leaves; the administrators are told (ADR-0067, D2) | M | `done` | — | shipped 2026-09-19 (c30e2580 + 667de9f7), branch fix/remove-membership-free-trial, not yet merged; Order.ReturnToBoardIfUnstaffed() walks Confirmed → New when a drop or admin rejection empties the crew; admin.order.crew_lost fires at any non-terminal status; the override refuses Confirmed on an unstaffed order; RejectEmployee ends the preferred hold; the sweeps keep their crew terms; AdminReassignOrder loads OrderStatusHistory (the Sequence collision) with AsSplitQuery; no invariant claimed; the customer is told nothing (O-D2-1). Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0771 | The revenue report — completed and paid, by completion date, minus both refund legs (D3) | M | `done` | — | shipped 2026-09-19 (c45a4af4 + 1fa06aa8 · web 56884360 + df7c9ba9), branch fix/remove-membership-free-trial, not yet merged; completed + paid by CompletedAt, net of both refund legs (card refunds and credit returned), cancelled/unpaid excluded, the admin override stamps CompletedAt and a test goes red for any Completed writer that forgets to; lost chargebacks named on the page (O-D3-2); the admin client regenerated; the report page reads the net figure and names both legs. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0772 | My Pay — the currency switch comes from the period's pay rows (F6) | S | `done` | — | shipped 2026-09-19 (8fb1e549 + b9f18e50 · web 2407b701), branch fix/remove-membership-free-trial, not yet merged; PeriodPaySummaryDto.availableCurrencies from the period's pay rows (a cancelled invoice's currency is not offered); the partner client regenerated and the partner-mobile spec refreshed (additive); My Pay derives its switch from the DTO. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0773 | Admin web — the role in the session, the guards, the sidebar, the administrators page (ADR-0066 D6) | M | `done` | — | shipped 2026-09-19 (14c833bf + bef0838e; the create form's Support default, the two `api.admin_user.*` refusal keys in five locales and the guards pinned against another company's administrator in 45462202), branch fix/remove-membership-free-trial, not yet merged; `AUTH_COOKIE_KEYS.adminRole` stored by `setSession` (the refresh path re-runs it), `PermissionService.currentAdminRole()` + the four set cases against `ADMIN_ROLE_SETS`, `POLICY_MAP` rewritten to D3, `policy-map-mirror.spec.ts` reading the C# map / physical policies / sets / enum out of the source tree (no JSON export — the parity-spec shape), `adminGuard` Administrator-profile only, `permissionGuard` on `route.data.permission` on every top-level route, `ADMIN_MENU_ITEMS` every entry with a permission and `resolveLandingRoute`, the administrators list's role column / create select / detail picker under `CanSetAdminRole`, the audit-log `actorAdminRole` column + filter; `admin-role-surface.spec.ts` walks both arrays, `admin-role-visibility.spec.ts` composes a Support and an Accountant session end to end. Filed 2026-09-19 from the batch-6 panel. |
| T-0774 | Administrators are told by e-mail — the channel, the template, the mailbox key (ADR-0065 D2 step 4, D3) | M | `done` | — | shipped 2026-09-19 (c099d23d + 77531caf · web 90d3800e), branch fix/remove-membership-free-trial, not yet merged; EmailType.AdminNotification with one embedded template and per-event copy in five locales rides the send-email outbox; notifications.admin_email (Email value type) read by the event's company — the mailbox when set, every active administrator otherwise; the Company settings page edits it with client-side validation. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |
| T-0775 | Administrators are told — the seven remaining events (ADR-0065 D4) | M | `done` | — | shipped 2026-09-19 (b27fc069 + 4d6d6f11), branch fix/remove-membership-free-trial, not yet merged; the seven remaining events at the sites that prove them — a new offerable order, a first card decline (guarded by the order's money and fulfilment state), a chargeback, a failed erasure retry and the three wind-down milestones — each idempotent on redelivery. Filed the same day from the batch-6 panel (ADR-0065/0066/0067). |

> **T-0768–T-0775 filed 2026-09-19** — batch 6, the owner's rulings of that day on D2 (walk-back), D3
> (revenue report), D5 (admin notifications), D8 / T-0748 (roles) and owner-plate F6 (My Pay currency
> switch), designed as ADR-0065, ADR-0066 and ADR-0067 (all `proposed`) plus two ADR-less designs recorded
> in T-0771 and T-0772, panel-accepted the same day. Backend lanes run one at a time — T-0768 → T-0770 →
> T-0771 → T-0772 → T-0774 → T-0775 → T-0748 — with the web parts (T-0769, T-0771/T-0772/T-0774 web, T-0773)
> following their backend lane's client regen; the batch's one `Initial` regen is T-0748's, with the DEV
> drop at the deploy. **A status describes the ticket, not the plan:** each row flipped when its lane picked
> it up and again when it shipped (the transitions the ticket's status log records). The same day, batch 5
> shipped owner-plate F1–F5, F7, F8 and D4 (recorded on the plate and in T-0683's row) and T-0761
> was closed on ground truth (shipped 2026-09-16, the row left open). **By the night of 2026-09-19 every
> batch-6 row is `done`** — T-0768–T-0775, then T-0748 and T-0773 last, with `Initial` at
> `20260919142517` and ADR-0065 / ADR-0066 / ADR-0067 `accepted`. Open rows: none from this batch.

| T-0777 | Backend core — the work-contract document + the order stamp, the acceptance row, the take/accept/start/complete rules, the reads, export DTOs, erasure site, roster entries, one regen, three clients, two spec re-dumps (ADR-0068 D1–D5) | L | `done` | — | shipped 2026-09-20 (`a15d3af0` + `1067d938`; AC13 by `0cb13f11`; the Development boot seed `b34dff07`), branch fix/remove-membership-free-trial, not yet merged; `LegalDocumentType.WorkContract` seeded from `customer/work-contract/any/2026-09-20` in five languages; `Orders.WorkContractDocumentId` stamped by `OrderFactory` (throws when none in force); `WorkContractAcceptances` (one row per seat, `UNIQUE (OrderEmployeeId)`, the request trio, the facts as jsonb; `Create` + `Pseudonymise` only); `TakeOrder.AcceptedWorkContractTextId` — `contract.not_accepted` before existence, `contract.text_mismatch` last, the acceptance and its `ContractAccepted` audit row staged inside the take's commit; `AcceptWorkContract` (any not-over order, idempotent per seat); `StartOrder` + `CompleteOrder` refuse a seat with no row as `contract.acceptance_required`; `GetWorkContractPreview` (partner hosts, the board's floor since `1067d938`) and `GetWorkContract` (five hosts, keyed on the acceptance, access from its order + the caller's employee id); `OrderItem.workContractAcceptances` redacted for browsers; the exports and the erasure site; `Initial` → **`20260919231739`** (88 tables); partner, customer and admin clients regenerated, both mobile specs re-dumped, the admin type label + the partner `api.contract.*` keys in five locales. **The record items of T-0783 landed here too.** The Postgres and host suites first execute in CI (Docker down on the box). |
| T-0778 | Partner web — the contract dialog on the board take and the detail take; the job-detail line and banner; the four keys (ADR-0068 D4, D6) | M | `done` | — | shipped 2026-09-20 (`5effc18d`), branch fix/remove-membership-free-trial, not yet merged; `components/work-contract-dialog/` (modes take / accept / read, the facts table, `contentHtml` through the sanitizer, the tick + *Accept and take the job*), every take through it with the previewed `legalDocumentTextId`, the facade's reload exactly once; the detail's line / banner, Start and Complete refusals open `accept` mode, the mismatch re-fetches and unticks; the four keys on `PARTNER_SURFACE_ERROR_KEYS` in five locales. |
| T-0779 | Partner Android — the contract sheet with the swipe on the three take paths; the detail line/banner; the shared HTML view; the pin (ADR-0068 D4, D6) | M | `done` | — | shipped 2026-09-20 (`9fea7853`), branch fix/remove-membership-free-trial, not yet merged; `:core`'s `HtmlContentView` (JavaScript off), `WorkContractSheet` + view-model (take / accept / read, `SlideToCommit` *Swipe to accept the contract for work*), the three entry points, the repository members + `OrdersMutation.AcceptWorkContract`, the detail's line / banner / refusal mapping, five locales, the wire pin and the view-model tests. |
| T-0780 | Partner iOS — the same as T-0779 on iOS (ADR-0068 D4, D6) | M | `done` | — | shipped 2026-09-20 (`32fdd5d2` + `53d005f8`), branch fix/remove-membership-free-trial, not yet merged; `CleansiaCore`'s `HtmlContentView` (`WKWebView`), `WorkContractSheet` + view-model under `SlideToConfirm`, `PartnerOrderClient` members + `OrdersStaleness`, the three entry points, the detail's line / banner / refusal mapping (`53d005f8`: a row with no version is dropped, the cleaner's id is asked for alongside the fetch), five locales with the catalogue-guard entries; XCTest in CI. |
| T-0783 | Backend record — the incident file section, the metadata retention task + key, the archive stream (ADR-0068 D5) | S | `done` | — | **shipped inside T-0777's `a15d3af0`** (no separate commit — the backend lane carried the record with the entity): the incident file's *Contracts for work* section + trail row, `retention.work_contract_metadata.years` (default 3, min 1) + the `WorkContractAcceptanceMetadata` task in the per-company loop (never deletes), `books/work-contract-acceptances.jsonl` without the trio + `workContractDocumentId` on the orders stream; `WorkContractAcceptanceRetentionTests` (Postgres) and `CompanyArchiveBundleTests` first execute in CI. |
| T-0781 | Customer surfaces — `/work-contract`, the wizard sentence, the acceptance line, the contract page/screen (web, Android, iOS) (ADR-0068 D1, D4, D6) | M | `done` | — | shipped 2026-09-20 (web `9f634730` + `b96cf5f3`; Android `a24e1595`; iOS `9d8ea2b5`), branch fix/remove-membership-free-trial, not yet merged; `/work-contract` (SSR, the footer link), the wizard's unconditional P074 sentence on all three clients, the per-crew-member line on the order detail with **Read the contract** → web `/orders/:orderId/contract/:acceptanceId` / Android `WorkContractScreen` / iOS `WorkContractView` (the stored facts, the acceptance facts, the text in the UI language, an error state for a foreign id); the shared web fact rows in `@cleansia/utils`; five locales with the copy specs / catalogue guards. |
| T-0784 | Admin web — the crew acceptance line with Read on the order detail; the settings description key (ADR-0068 D4, D5) | S | `done` | — | shipped 2026-09-20 (`1dd91c24` + `0d663d1b`), branch fix/remove-membership-free-trial, not yet merged; the crew list's *accepted {date}, v{version}* / *contract pending* per seat, **Read** → `admin-work-contract-dialog` (the facts and acceptance rows from the shared helper, the text through the sanitizer, and the accepted text row's SHA-256 via `adminLegalClient.getDocument` only for a session holding `CanViewLegalDocuments` — the other roles are told so); `descriptions.retention.work_contract_metadata.years` in five locales. |
| T-0782 | Docs — ADR-0068 in `docs/` with its trail, the role cards, business rules, flows, model, feature pages, the living companion, the plate and the questions | S | `done` | — | pages written 2026-09-20 in the docs commit that closes batch 9 (+ the review-fix commit: the archived challenge, the changelog entry, the tier token, the four corrections), branch fix/remove-membership-free-trial, not yet merged; AC1 closed 2026-09-20 by the orchestrator (docs-refs 0 unresolved, catalog-claims 0 violations, VitePress build green); ADR-0068 `accepted` with the trail and a §What shipped block (the T-0783 fold, the admin commits, 88 tables, the tenant-ignoring reads, the preview's floor, the read's policy, the Development boot seed, the test names); ADR-0063 D9's pointer; the role card + index; business rules `#work-contract` + the tenth retention row; features (three audiences); the take, execution, booking and cross-cutting flows; order lifecycle; model (47 / 49 stamped), database (`20260919231739`, 88 tables), security rules (S8), local orchestration, the API reference (three endpoints, the fourteen-rule take chain, the two gates), the GDPR flow (the erasure's twenty-first repository); the three app overviews, the ordering flow, the two order-management pages + the mobile features page; the living companion; the backend idiom; the eight tickets, Q-WC-01…07, the plate, MS-2 and the handover. |

> **T-0777–T-0784 filed 2026-09-20 as `done`** — batch 9, the owner's ruling of that day (*"I want to
> implement 'smlouva o dílo' (lawyer suggested it)"*), designed as ADR-0068 and panel-accepted the same
> day (challenge C1–C17). T-0777 ran first and alone (the schema, the take's wire shape, the three
> client regens and the two spec re-dumps), then two lanes at a time — T-0778 + T-0779, T-0780 + T-0783,
> T-0781 + T-0784 — and the docs lane last. **Two departures from the plan, both recorded:** T-0783's
> record items shipped inside T-0777's commit rather than in a lane of their own, and the same day's
> company-registration ruling (a cleaner contracts as a natural person — `c400d47f`, `20e78001`; web
> `828622d6`, Android `2ef0401b`, iOS `f3cc4927`; the preservation fix `1a06fad0` + `3980201c`) ran on
> the branch without a ticket and is recorded on the owner plate only. The batch's one `Initial` regen
> is `20260919231739` with the DEV drop at the deploy (MS-2, A1). Q-WC-01…07 are in `questions/open.md`
> with their defaults, every one shipped as its default. Open rows: none — T-0782 closed on 2026-09-20
> once the orchestrator ran the three checks the ticket names (docs-refs, catalog-claims, the VitePress build).

| T-0785 | Admin shell — one page wrapper, one header, one button width, one field height, one section with an action slot, one label/value grid, the 18 referenced-but-undefined tokens, the z-index scale, one focus ring; `page-shell.spec.ts` guards it in `frontend-ci` | M | `todo` | — | phase 2, web-shared lane, first of the serial four; after T-0793 |
| T-0786 | Shared primitives — `cleansia-status-badge` (ten kinds, five tones), `formatDate`, every money figure through `formatMoney`, table `align` honoured, the paginator hidden on zero rows, one row-action style | M | `todo` | — | phase 2, web-shared lane; after T-0785 |
| T-0787 | Admin list pages — the 28 lists on one pattern (header, `Filtry` then `Vytvořit`, right-aligned numbers, width budgets ≤ 90 %, one row-action style, `h1`) plus the page-local defects the 1440 captures show | M | `todo` | — | phase 3, admin lane, first of four; after T-0785, T-0786, T-0794 |
| T-0788 | Admin detail pages — the 11 details on one pattern (breadcrumb above the title, audit link on the title row's right, `[section-actions]`, `.detail-grid`, one outlined `Akce` row, no green/orange/blue fills, the 41 inline `style=""` gone, the not-found block) | L | `todo` | — | phase 3, admin lane, second of four; after T-0791, T-0796, T-0797 (the breadcrumb) |
| T-0789 | Admin create / edit forms — the 14 forms on the pay-config shape (12-column `form-grid` in `common/`, spans not pixels, 44 px fields, row hints, `Zrušit` + primary footer), `cleansia-multiselect` adopted, the last five `*ngIf` gone | M | `todo` | — | phase 3, admin lane, third of four; after T-0785 |
| T-0790 | Admin dialogs — the 12 `p-dialog` templates on one footer shape, the two confirmations that cannot open (*Deactivate plan*, *Erase user*) fixed, translated labels on the 11 bare `.confirm({...})` sites; the "Create pay period" stub's layout only (Q-UI-04) | S | `todo` | — | phase 3, admin lane, last of four; after T-0785, T-0796 |
| T-0791 | Remove the availability module end to end — `TimeRange`, `DayOfWeek`, two commands, two routes, the `Employees.Availability` column, the admin section, the 1 018-line shared component, the partner-web and mobile remnants, the two wire members (Q-UI-01); **`Cart` ×3, `EmailTranslations`, `Employee.PreferredCurrencyCode`, `MembershipPlan.TrialPeriodDays` folded in (Q-UI-02)**; the batch's one `Initial` regen, three NSwag clients, both mobile spec re-dumps, the DEV drop owed at the deploy | L | `in_progress` | — | opened 2026-09-20 on the branch; phase 1, runs first and alone |
| T-0792 | Backend dead code — `DeleteDocument`, eight unattributed `Policy` constants (one hunk across four files), 36 unemitted error keys with their 15-file locale twins, 25 repository methods, 25 domain members, three orphan files, the partner-host routes no client calls, three admin routes with no caller; `PolicyAttributionTests` + `BusinessErrorMessageEmittedTests` so it stays gone | M | `in_progress` | — | opened 2026-09-20 on the branch; phase 1, after T-0791, beside T-0793 |
| T-0793 | Web dead code — `cleansia-menu`, the table skeleton, three pipes, two unrouted admin components, both `user` NgRx slices (one wasted `GET /User/GetCurrent` per partner login), the declaration-only exports, the dead i18n sections in five locales, the orphan stylesheets and 12+ dead `.p-datatable` blocks; the free-trial UI T-0690 left; the pre-ADR-0057 help copy rewritten | M | `in_progress` | — | opened 2026-09-20 on the branch; phase 1, after T-0791, beside T-0792 |
| T-0794 | `cleansia-filter-drawer` — one drawer (the partner's dialog semantics), one chip row, a `FilterDrawerState` helper replacing the eleven methods re-declared in 13 admin + 2 partner components, the 55 raw drawer buttons gone with it | M | `todo` | — | phase 2, web-shared lane; after T-0785, before T-0787 |
| T-0795 | Shell parity — `cleansia-mobile-toolbar`, one `common/auth.scss`, the print rule shared, four guards return a `UrlTree`, admin routes on `CleansiaAdminRoute`, sidebar `aria-current` + translated labels, two route titles | M | `todo` | — | phase 2, may run beside the web-shared four (disjoint files); after T-0785 |
| T-0796 | One confirmation, one toast, one loading, one empty state — the root `DialogService` only (17 local `<p-confirmDialog>` + 18 providers gone), PrimeNG locale from the bundle, the 16 double-toast `*_ERROR_KEY_MAP`s retired, in-place skeletons, `not-found-state` + `empty-state`, one Cancel key | M | `todo` | — | phase 2, web-shared lane, last of the serial four; after T-0785 |
| T-0797 | Partner web sweep onto the shared primitives — the `cleansia-button` legacy API deleted (one two-token customer edit), five dialogs on the admin shape, eleven raw icon buttons wrapped, `Nová` in the same pill family as `Zaplaceno`, headers on the shell rule, `cleansia-breadcrumb` (Q-UI-08), `!important` 61 → ≤ 1, `::ng-deep` 19 → 0 | M | `todo` | — | phase 3, partner lane, beside the admin lane; after T-0785, T-0786, T-0793 |
| T-0798 | `check-consistency.mjs` — F1–F15 filed at `--warn` with today's counts, one self-test each, promoted to failing where the sweeps reach zero; `apps/` and `.html`/`.scss`/`.json` walked | S | `todo` | — | phase 4, tools lane; definitions any time, promotion after phase 3 |
| T-0799 | Docs and living pages, last — availability out of the docs, the removed routes off the API reference and the two permission tables, `Cart` off the model, design-language + pattern pages, MS-2 / A1 to the new `Initial` id, the rows verified, the Q-UI rulings recorded, the changelog | S | `todo` | — | phase 4, docs lane, once everything is green |

> **T-0785–T-0799 filed 2026-09-20** — the UI-polish and dead-code batch, on the owner's ruling of
> that day (*"I want to polish alignments in admin app a bit on all of the overview/detail pages.
> Also remove redundant and deprecated things that are no longer used (like availability module,
> the same for backend). And make overall check on both apps to make them consistent"* — both = the
> admin and partner **web** apps), planned from three scouts (visual, dead-code, consistency) whose
> every load-bearing claim was re-grepped at `069b72ae` before a ticket was cut; only zero-reader
> verdicts are ticketed, every OWNER verdict is a Q-UI question. Four phases on one branch
> (`chore/ui-polish-and-dead-code`, stacked on #255): **phase 1** removals — T-0791 alone (the
> batch's **only** `Initial` regen, three client regens, both spec re-dumps), then T-0792 ‖ T-0793;
> **phase 2** the shared surface — T-0785 → T-0786 → T-0794 → T-0796 serial on the web-shared lane
> (they all write under `libs/shared/`), T-0795 beside them; **phase 3** pages — the admin lane
> T-0787 → T-0788 → T-0789 → T-0790 ‖ the partner lane T-0797; **phase 4** T-0798 then T-0799 last.
> Removals run first so the polish tickets never restyle dead CSS. **The Q-UI defaults in force** (the
> owner's *go* of 2026-09-20 accepted the list as put): Q-UI-01 the two availability wire members
> drop now; **Q-UI-02 the four dead schema things fold into T-0791's regen** (the orchestrator's
> recommendation over the analyst's park — one migration, one DEV drop, the customer client and
> customer mobile spec join the regen list); Q-UI-03 / 04 / 05 / 12 keep; Q-UI-06 amend the design
> language rather than load Poppins; Q-UI-08 breadcrumb; Q-UI-07 / 09 / 10 / 11 / 13 leave. All
> thirteen are in `questions/open.md` with their defaults and on the owner plate. The DEV drop the
> regen owes is recorded at the deploy (MS-2 shape), not run on the branch. Open rows: T-0785–T-0799.

*Next id: **T-0800**.*
