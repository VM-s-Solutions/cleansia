---
id: T-0172
title: "Dispute transition-guard: Close/Escalate/LinkStripe reachable + guarded"
status: done
size: M
owner: —
created: 2026-06-01
updated: 2026-06-15
depends_on: [T-0140]
blocks: []
stories: []
adrs: [0001]
layers: [backend]
security_touching: true
manual_steps: []
sprint: 2
source: finding DA-2
---

## Context

Audit finding **DA-2** (`agents/backlog/audits/AUDIT-2026-06-01-slice-reports.md:2063-2067`,
`AUDIT-2026-06-01-findings.json:2375-2389`, severity **critical**, functional GAP): the dispute
status machine is half-built. The domain models a full lifecycle —
`Pending/UnderReview/WaitingForResponse/Resolved/Closed/Escalated`
(`Cleansia.Core.Domain/Enums/DisputeStatus.cs:6-14`) — and the entity exposes intent-named
transition methods `Close` (`Dispute.cs:92-96`), `Escalate` (`:98-102`) and `LinkStripeDispute`
(`:104-108`), but **grep confirms zero callers** of those three methods in `src`. The only write
paths are:

- `Dispute.UpdateStatus(newStatus, updatedBy)` (`Dispute.cs:64-68`) — **free-sets any value with no
  transition guard**, and
- `Dispute.Resolve(...)` (`:82-90`).

`UpdateDisputeStatus.Handler` (`Cleansia.Core.AppServices/Features/Disputes/UpdateDisputeStatus.cs:45`)
calls `dispute.UpdateStatus(request.NewStatus, actorId)` unconditionally after the not-found guard,
so a dispute can illegally jump `Pending → Closed` skipping resolution, or be **re-opened after
`Resolved`**. The validator only checks `IsInEnum` (`:20-22`) — no legal-transition check exists.

This ticket **closes the correctness hole**: it makes `UpdateStatus` route through the intent-named
methods and enforce a legal-transition table, rejecting illegal transitions as a `BusinessResult`
failure (not a thrown exception, not a silent set). The legal transition graph and the
refund/chargeback money-path are frozen by **T-0140 (ADR-REFUND)** — this is a `depends_on` and must
be `accepted` first (T-0140 AC5 hands the `charge.dispute.*` / `LinkStripeDispute` *webhook wiring*
to the Wave-2 `D-06` ticket; T-0140 AC6 names the admin-only authz gate). This ticket implements the
in-process transition guard only; it does **not** add the Stripe chargeback webhook case.

`security_touching: true` — this is an authorization/lifecycle integrity surface (illegal state
transitions on a money-bearing entity); the Security gate is mandatory per
`agents/process/quality-gates.md`.

## Acceptance criteria

- [ ] **AC1 — Legal transitions succeed.** Given a `Dispute` in state X and a legal target state Y
  per the transition table frozen by T-0140 (ADR-REFUND), When `UpdateDisputeStatus.Handler.Handle`
  is invoked by an actor holding `Policy.CanUpdateDisputeStatus` (Admin), Then the dispute's `Status`
  becomes Y, `Updated(actorId, …)` is stamped, and the handler returns `BusinessResult.Success()`.
  A unit test in `Cleansia.Tests` proves each legal edge.

- [ ] **AC2 — Illegal transitions are rejected (the hole is closed).** Given a `Dispute` in a
  terminal/incompatible state (e.g. `Resolved` → re-open to `Pending`, or `Pending` → `Closed`
  skipping resolution), When `UpdateDisputeStatus.Handler.Handle` is invoked, Then the status is
  **unchanged** and the handler returns `BusinessResult.Failure` with a dedicated
  `BusinessErrorMessage` code (new `dispute.invalid_status_transition`, added in
  `Cleansia.Core.AppServices/Common/BusinessErrorMessage.cs:196-201` next to the other `dispute.*`
  codes). A unit test asserts the failure `Error.Message` equals the new constant for each illegal
  edge — no exception is thrown, no `CommitAsync` side effect occurs.

- [ ] **AC3 — Transition logic lives in the domain entity.** Given the half-built methods
  `Close`/`Escalate` (`Dispute.cs:92-102`), When the change lands, Then `Dispute.UpdateStatus`
  enforces the legal-transition table inside the domain entity (returning a result/failure the
  handler maps, OR exposing a guarded transition the handler consumes — per the shape T-0140 freezes),
  so `Close` and `Escalate` are **reachable** through the existing `UpdateDisputeStatus` write path.
  Grep for callers of `Dispute.Close`/`Dispute.Escalate` now returns the production path (not just
  tests).

- [ ] **AC4 — `Resolve` remains the only writer of resolution fields.** Given `Dispute.Resolve`
  (`:82-90`) sets `RefundAmount`/`ResolvedBy`/`ResolvedOn`/`ResolutionNotes`, When `UpdateStatus` is
  used to reach a non-`Resolved` state, Then it does **not** mutate those resolution fields, and a
  test confirms `UpdateStatus` cannot be used to set `Status = Resolved` (resolution stays owned by
  `ResolveDispute` / `Dispute.Resolve`) so the two money-path writers do not overlap (consistent with
  T-0140 AC3).

- [ ] **AC5 — Authorization unchanged and asserted.** Given `UpdateStatus` is gated by
  `Policy.CanUpdateDisputeStatus` (Admin-only, `Policy.cs:105`,
  `Cleansia.Web.Partner/Controllers/DisputeController.cs:79-84`), When the change lands, Then the
  policy mapping is unchanged and an integration test in `Cleansia.IntegrationTests` confirms a
  non-admin caller is rejected (per `agents/knowledge/testing.md` must-cover #5). This ticket does
  **not** re-home the endpoint to the Admin API (that is the DA-1 / D-01 bundle).

- [ ] **AC6 — Tests predate the implementation (test-first).** Given the must-cover #2 (order/state
  transitions, incl. the illegal ones that must be rejected) in `agents/knowledge/testing.md`, When
  the diff/commits are reviewed, Then the transition unit tests appear **before/with** the
  implementation, the status log records "red → green", and every legal+illegal edge from the T-0140
  table maps to a test case (Reviewer Gate 6).

## Out of scope

- The `charge.dispute.*` Stripe webhook case and the actual call to `LinkStripeDispute` from an
  inbound chargeback — explicitly handed to **D-06** (Wave 2) by T-0140 AC5. This ticket leaves
  `LinkStripeDispute`/`StripeDisputeId` for D-06; it only makes the *in-process* transition machine
  correct. (If T-0140 deletes chargeback linkage as out-of-scope, D-06 carries the removal — not here.)
- Re-homing dispute admin endpoints to the Admin API or building any admin UI — that is **DA-1 /
  D-01/SEC-DSP-06/07** (Wave 2, `L`).
- Issuing the actual Stripe refund or changing `IStripeClient` — frozen by T-0140, implemented by its
  consumers (AUD-01 / the D-01 bundle).
- The `AddDisputeMessage` NRE guard (DA-4), CT propagation (DA-11), or any other DA-* finding.

## Implementation notes

- **Built TEST-FIRST per `agents/knowledge/testing.md`.** State-machine transitions are on the
  **strict red-green-refactor** list ("order/pay state transitions" / must-cover #2): write the
  failing transition tests (legal edges pass, illegal edges rejected with the new error code) first,
  confirm they fail for the right reason, then write the minimum guard to make them green. After-the-
  fact tests for this pure logic fail Gate 6.
- **Governing ADR:** ADR-0001 (authorization — `Policy.CanUpdateDisputeStatus` is Admin-only, the
  refund authz boundary cited in T-0140 AC6) and the legal-transition graph frozen by **T-0140
  (ADR-REFUND)** — read T-0140's accepted ADR for the exact legal edges *before* coding; do not invent
  the graph here.
- **Serialization cluster:** YES. This ticket is in the dispute-backend cluster in
  `agents/backlog/TICKET-MAP.md:25` — strict order **SEC-DSP-01 (handler `IsStaffMessage` derive) →
  DA-2 (this ticket, transitions) → D-01 bundle (admin UI)**. It edits `Dispute.cs` /
  `UpdateDisputeStatus.cs` / `BusinessErrorMessage.cs`, which downstream cluster members also touch.
  The PM must **not** run this concurrently with SEC-DSP-01 or the D-01 bundle; SEC-DSP-01 lands first.
- **Verified anchors (2026-06-01):**
  - `Cleansia.Core.Domain/Disputes/Dispute.cs:64-68` — unguarded `UpdateStatus` (the hole).
  - `Cleansia.Core.Domain/Disputes/Dispute.cs:82-90` — `Resolve` (resolution-field writer, leave owned).
  - `Cleansia.Core.Domain/Disputes/Dispute.cs:92-108` — dead `Close`/`Escalate`/`LinkStripeDispute`.
  - `Cleansia.Core.Domain/Enums/DisputeStatus.cs:6-14` — the 6 states.
  - `Cleansia.Core.AppServices/Features/Disputes/UpdateDisputeStatus.cs:35-48` — handler to guard;
    `:20-22` — validator (currently `IsInEnum` only).
  - `Cleansia.Core.AppServices/Common/BusinessErrorMessage.cs:196-201` — existing `dispute.*` codes;
    add `dispute.invalid_status_transition` here. **Note:** a new `BusinessErrorMessage` code needs a
    matching `errors.dispute.*` key in all 5 locales — but the customer disputes locales are empty
    (DA-7); flag the frontend i18n add as a fast-follow on the DA-7 ticket, not a blocker here.
  - `Cleansia.Core.AppServices/Authentication/Policy.cs:105` — `CanUpdateDisputeStatus` (Admin).
  - `Cleansia.Web.Partner/Controllers/DisputeController.cs:79-84` — current `UpdateStatus` route.
- **No manual steps.** No schema change (`Status` column already exists), no DTO/endpoint shape change
  → no migration, no NSwag regen. `manual_steps: []` is correct. (If the architect's T-0140 contract
  forces a `Command`/`Response` shape change, escalate before going `ready` — adding a `Response`
  record would make it `nswag-regen`.)

## Status log
- 2026-06-01 — draft (created by pm)
- 2026-06-09 — AC5 + AC6 finished (backend, Batch 3A wave-1).
  - **AC5 (non-admin rejection on UpdateStatus).** Added `Ac10DisputeUpdateStatusAdminOnlyTests`
    (`src/Cleansia.HostTests/Tests/Ac10DisputeUpdateStatusAdminOnlyTests.cs`), mirroring the existing
    `Ac6DisputeMessageSplitTests` (WebApplicationFactory + Testcontainers Postgres + the real
    `[Permission]` gate). `Cleansia.IntegrationTests` cannot host this assertion — it dispatches through
    the MediatR pipeline only and never runs the MVC `[Permission]`/physical-policy filter — so the
    AC's load-bearing mechanism (a real non-admin caller is 403'd over HTTP on the
    `Policy.CanUpdateDisputeStatus = AdminOnly` route) lives in the host harness where the gate actually
    executes (deviation from the ticket's `Cleansia.IntegrationTests` path noted here for the reviewer).
    Cases: a Partner-audience **Employee** caller on `/api/Dispute/UpdateStatus` → **403 Forbidden**,
    dispute status untouched (still `Pending`); the **Administrator** companion clears the same gate
    (**200**). **red** (before the test the AdminOnly gate had no end-to-end non-admin-rejection proof
    on this route — the AC5 coverage gap) → **green** (2/2 host authz tests pass).
  - **AC6 (red→green recorded).** The transition-guard units predate the guard:
    `src/Cleansia.Tests/Domain/Disputes/DisputeTransitionTests.cs` and
    `src/Cleansia.Tests/Features/Disputes/UpdateDisputeStatusHandlerTests.cs`. **red reason:** before
    the guard, `UpdateDisputeStatus` returned no error code (free-set `UpdateStatus`, illegal edges
    silently applied / re-opened terminal disputes) → the illegal-edge `Assert.Equal(
    BusinessErrorMessage.InvalidDisputeStatusTransition, …)` + state-unchanged assertions failed. After
    routing `UpdateStatus` through the `CanTransitionTo` table + intent methods → **green**.
  - Comment hygiene: stripped the `DA-2` finding refs from the two test doc-comments and the decorative
    `──── … ────` divider banners in `DisputeTransitionTests.cs` (behavioral descriptions kept).
  - Full `src/Cleansia.Tests` suite: **844 passed** (842 baseline + 2 chargeback regressions added by
    the T-0174 wave-1 fixes); `src/Cleansia.Api.sln` builds clean.

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->
