---
id: T-0179
title: Unify membership subscribe path (web/mobile)
status: draft
size: S
owner: —
created: 2026-06-01
updated: 2026-06-01
depends_on: [T-0111]
blocks: []
stories: []
adrs: [0001]
layers: [backend, frontend]
security_touching: false
manual_steps: [nswag-regen]
sprint: 2
source: finding LG-07
---

## Context
Minor GAP / consistency finding from the 2026-06-01 audit (LG-07). The membership subscribe surface
has **two paths that nothing documents**, so the native-SDK path looks orphaned from the web side and
risks being mistaken for dead code and deleted.

- `MembershipController.Subscribe` → `CreateMembershipSubscription` is the **native-SDK** flow
  (SetupIntent + EphemeralKey, see `CreateMembershipSubscription.cs:79-114`). It is wired on both the
  web customer API (`src/Cleansia.Web.Customer/Controllers/MembershipController.cs:15-25`) and the
  mobile customer API (`src/Cleansia.Web.Mobile.Customer/Controllers/MembershipController.cs:15-25`).
- The **web** `MembershipFacade`
  (`src/Cleansia.App/libs/cleansia-customer-features/profile/src/lib/membership/membership.facade.ts`)
  only calls `createCheckoutSession`, `getMine`, `getPlans`, `cancel`, `swapPlan` — it **never** calls
  `subscribe`. Web subscribes via Stripe-hosted Checkout (`createCheckoutSession`, facade `:122-153`);
  mobile uses the PaymentSheet/SetupIntent `Subscribe` path. This split is legitimate but is recorded
  nowhere.
- The finding also pins this handler to two tracked consistency debts: **B5** (failure construction
  uses `nameof(Command)` instead of the offending field — `consistency.md:64-66`, real call sites at
  `CreateMembershipSubscription.cs:47,61`) and **B8** (Stripe-with-no-try/catch — `consistency.md:75-79`).

This ticket **depends_on T-0111** (LG-SEC-02, mobile direct-subscribe idempotency) and rebases on the
post-fix `CreateMembershipSubscription.cs`; do not start until T-0111 is `done`.

Source: finding LG-07 — `agents/backlog/audits/AUDIT-2026-06-01-findings.json:469-483`;
`agents/backlog/audits/AUDIT-2026-06-01-slice-reports.md:1901-1905`;
plan `agents/backlog/audits/AUDIT-2026-06-01-execution-plan.md:173`.

## Acceptance criteria
- [ ] **AC1 (the two-path design is documented at the contract)** — Given a developer reading the
  subscribe surface, When they open `CreateMembershipSubscription.cs` and the web `MembershipFacade`,
  Then a doc comment on the handler/`Command` states the intended split (web → Checkout Session;
  mobile → SetupIntent/PaymentSheet `Subscribe`) and the web facade notes that web deliberately uses
  `createCheckoutSession` and never `subscribe`. The `Subscribe` endpoint is observably **not**
  orphaned — its sole consumer (mobile) is named.
- [ ] **AC2 (B5 closed — failure errors name the offending field)** — Given the post-T-0111 handler,
  When `CreateMembershipSubscription.Handler` returns a `BusinessResult.Failure`, Then the first
  `Error` arg is `nameof` of the **offending field/entity**, not `nameof(Command)` — i.e. the
  `BusinessErrorMessage.UserNotFound` failure (`CreateMembershipSubscription.cs:47`) no longer uses
  `nameof(Command)`, per `consistency.md` B5 (`consistency.md:64-66`). The existing per-field error
  keys (`MembershipPlanNotFound`, `MembershipAlreadyActive`) are preserved.
- [ ] **AC3 (no functional change to either subscribe path)** — Given an unchanged web Checkout
  subscribe and an unchanged mobile SetupIntent subscribe, When each runs after this ticket, Then both
  behave exactly as before T-0179 (no endpoint added/removed, no DTO field renamed, idempotency from
  T-0111 intact). This is a documentation + B5-rename ticket, not a behavior change.
- [ ] **AC4 (a test proves the contract is intact — TDD)** — A unit test in `Cleansia.Tests` (written
  red-first per `agents/knowledge/testing.md`) asserts that the `UserNotFound` failure carries the
  correct field name (locks B5 against regression) and that the confirmed/unconfirmed branches still
  return their respective `Response` shapes. The handler change lands only after the failing test
  exists; status log notes the red→green transition.

## Out of scope
- **B8 provider try/catch** (narrow `catch (StripeException)` → `PaymentGatewayUnavailable` around the
  membership Stripe calls) — owned by **LG-06 / T-0147** (Wave 1); do not fold it here.
- **Idempotency of the confirmed-subscribe branch** — already delivered by **T-0111 (LG-SEC-02)**;
  this ticket assumes it is merged and does not re-touch the idempotency-key logic.
- **Collapsing the two paths into one route / removing the web-side `Subscribe` endpoint** — the split
  is intentional; this ticket documents and de-confuses it, it does **not** delete the endpoint or
  re-point web onto the SetupIntent flow.
- LG-SEC-03/04, LG-08 (the stale OrderFactory floor comment), and any new error constant.

## Implementation notes
- **Built TEST-FIRST** per `agents/knowledge/testing.md` — the B5/contract test (AC4) is written red
  before the handler edit; the handler is changed only to make it pass.
- **Governing ADR: ADR-0001** (`agents/backlog/adr/0001-authorization-model.md`) — both `Subscribe`
  endpoints are gated by `[Permission(Policy.CanManageMembership)]`
  (`Cleansia.Web.Customer/Controllers/MembershipController.cs:16`,
  `Cleansia.Web.Mobile.Customer/Controllers/MembershipController.cs:16`); the documentation must not
  change that gate. `security_touching: false` — no authz/money-path logic changes here.
- **Serialization cluster:** `CreateMembershipSubscription.cs` is a **cross-wave shared file** —
  T-0111 (Wave 0) edits it first, then this ticket (Wave 2) rebases on the post-fix version. Per
  `TICKET-MAP.md` this is handled by wave ordering, not a concurrent cluster; **do not** start T-0179
  until T-0111 is `done`, and never run them concurrently against this file.
- **B5 fix site:** `CreateMembershipSubscription.cs:46-47` — replace `nameof(Command)` with the field
  the error is about (the user-not-found case), per `consistency.md:64-66`. Leave `:53-54` and `:60-61`
  as-is (already correct per-field/per-entity).
- **Web facade doc:** annotate
  `src/Cleansia.App/libs/cleansia-customer-features/profile/src/lib/membership/membership.facade.ts`
  (the class already has a NOTE block at `:13-20`) to record that web subscribes via Checkout and
  intentionally never calls `subscribe`.
- **manual_steps: nswag-regen** — if the handler doc/field touch alters the generated OpenAPI contract
  for the customer client, flag `nswag-regen` to the owner at review and **HOLD** any frontend wiring
  until the owner confirms regeneration. Do not run `npm run generate-customer-client` yourself. (If
  the change is comment-only with no schema delta, note that no regen was needed.)

## Status log
- 2026-06-01 — draft (created by pm)

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->
