---
id: T-0188
title: Device / active-session management (GetMyDevices + revoke UI)
status: draft
size: M
owner: —
created: 2026-06-01
updated: 2026-06-01
depends_on: []
blocks: []
stories: []
adrs: [0001]
layers: [backend, frontend, mobile]
security_touching: true
manual_steps: [nswag-regen, ef-migration]
sprint: 2
source: finding IA-05; story
---

## Context

The device read side is half-built and orphaned. `DeviceDto`
(`src/Cleansia.Core.AppServices/Features/Devices/DTOs/DeviceDto.cs:3-9`) is shaped exactly like a
"list my devices" row yet is referenced **only by its own declaration** — dead code, which
`agents/knowledge/conventions.md` ("No dead code") treats as a hard fail. The data access already
exists and is implemented (`IDeviceRepository.GetByUserIdAsync`,
`src/Cleansia.Core.Domain/Repositories/IDeviceRepository.cs:9`, used by `GdprDeletionService` and
`SendPushNotificationFunction`) but is never surfaced to the user who owns the devices: there is **no
`GetMyDevices` query**, no list endpoint on any `DeviceController`, and no client UI.

`Features/Devices/` contains only `RegisterDevice.cs` and `UnregisterDevice.cs`; `UnregisterDevice`
requires the caller to already know the handset's `DeviceId` string (fine for the app self-unregistering
on sign-out, useless from a *different* phone for a device you no longer hold). On the session side
`Logout.cs` revokes one refresh token and `RefreshToken.cs` rotates the current one — neither lets a
user enumerate or revoke *other* sessions. The only device lifecycle a user benefits from today is the
invisible `DataRetentionBackgroundService.CleanStaleDevicesAsync` bulk sweep. A user who loses a phone
cannot see or cut off that device's push + session.

This ticket closes that gap: a `GetMyDevices` query returning `DeviceDto`, an ownership-checked
revoke-by-id command that also kills the device's refresh tokens, the mobile self-service surface, and
the optional admin read panel.

Sources: audit finding **IA-05**
(`agents/backlog/audits/AUDIT-2026-06-01-findings.json:1053-1068`,
`agents/backlog/audits/AUDIT-2026-06-01-slice-reports.md:631-638`); user story **US-customer-0142**
(`agents/backlog/stories/AUDIT-2026-06-01-user-stories.md:5459-5536`).

## Acceptance criteria

- [ ] **AC1 — List my devices (query + endpoint).** Given an authenticated user with one or more
  registered devices, When the app requests the device list via the new `GetMyDevices` query exposed on
  the `DeviceController` (S2 `[Permission]` policy), Then it receives a list of **`DeviceDto`** rows (the
  existing record, now wired up — not a new parallel type) showing platform, device label/id and
  `LastActiveAt`, scoped to the caller's own `UserId` **derived from the JWT via `IUserSessionProvider`,
  never from request input** (S1), the current device is identifiable in the response, and a test proves
  user A's call never returns user B's devices.

- [ ] **AC2 — Revoke a device I no longer hold (ownership-checked, NotFound on miss).** Given a device in
  my list, When I revoke it by its server-side id through the new revoke-by-id command, Then that
  `Device` row is removed (receives no further push) **only if it belongs to me**; a device id I do not
  own returns **NotFound** (not Forbidden) and does not reveal the row exists (S3), proven by a test
  asserting cross-user revoke yields NotFound and leaves the row intact.

- [ ] **AC3 — Revoke also ends that device's session, not just its push token.** Given I revoke a device
  with an active sign-in, When the revoke completes, Then the refresh token(s) associated with that
  device are revoked through the existing `IRefreshTokenService.RevokeAsync` path that `Logout` already
  uses, so the lost handset can no longer mint new access tokens on next refresh — covered by a test.

- [ ] **AC4 — Mobile self-service surface.** Given the partner or customer mobile app profile/settings
  area, When I open "Your devices", Then I see my devices with platform and last-active, can revoke any
  device **other than the one I am on**, and the list reflects the change after revoke; the three explicit
  data states (loading, empty, error) are all handled per `conventions.md` production-ready bar. Strings
  live in Android string resources (no hardcoded text).

- [ ] **AC5 — No leaked identifiers; dead code resolved.** Given the new query returns `DeviceDto`, When
  the response is serialized, Then it carries **no `TenantId`** and no foreign user identifiers (S4);
  `DeviceDto.UserId` is removed from the returned shape or justified as the caller's own id; **and
  `DeviceDto` is no longer dead code** (it is now produced by `GetMyDevices`), satisfying the audit's
  "wire it up or delete it" disposition.

- [ ] **AC6 (optional / admin) — Admin can view a user's registered devices.** Given an admin on a user's
  detail page, When they open that user's devices panel, Then they see the user's registered devices
  (read-only acceptable for v1) sourced from the same `GetByUserIdAsync` data via the generated admin
  client, the admin endpoint is gated by an admin `[Permission]` policy (S2), and the DTO carries no
  cross-user PII beyond what admins are already authorized to see (S4).

## Out of scope

- Renaming/relabelling devices or setting a friendly device name.
- Geolocation, IP, browser/OS fingerprint, or a per-device "login activity / audit history" timeline
  (the `Device` entity stores no IP/location; `RefreshToken` `DeviceLabel`/`IpAddress` metadata is a
  separate, larger story).
- A "revoke ALL other sessions / sign out everywhere" bulk action (distinct story; v1 is per-device).
- Changing `DataRetentionBackgroundService.CleanStaleDevicesAsync` cutoff/notify behaviour.
- Web (Angular customer/partner SPA) self-service device management beyond the optional admin read panel
  in AC6 — web push session management is not in this ticket.
- Any change to `RegisterDevice` / `UnregisterDevice` semantics or the FCM dispatch path
  (`SendPushNotificationFunction`); 2FA / trusted-device / "remember this device" flows.

## Implementation notes

- **TEST-FIRST per `agents/knowledge/testing.md`** (red-green; cross-user authz and ownership-checked
  revoke are on the strict list): write the failing tests **before** the handler/controller/UI code —
  the cross-user list isolation (AC1), the cross-user revoke → **NotFound** + row-intact case (AC2), and
  the refresh-token-revoked-on-device-revoke case (AC3).
- **Governing ADR:** `ADR-0001` (ADR-AUTHZ, `agents/backlog/adr/0001-authorization-model.md`) — every
  new endpoint needs its `[Permission]` policy resolved from the fail-closed `PolicyBuilder` map (S2),
  ownership is enforced server-side, and the JWT trust boundary holds (`UserId` from
  `IUserSessionProvider`, S1). Cite, do not re-decide.
- **Serialization cluster (TICKET-MAP):** this ticket is **not in any shared-file cluster** in
  `agents/backlog/TICKET-MAP.md` — `GetMyDevices`/revoke are net-new in
  `src/Cleansia.Core.AppServices/Features/Devices/`, and the new list/revoke endpoints are additive on
  the `DeviceController`s, which no other in-flight ticket edits. No serialization constraint; safe to run
  concurrently with other Wave-2 work.
- **Routing:** lock the contract first (`architect` confirms the query/command + `DeviceDto` shape and
  the `[Permission]` policies against ADR-0001 → `backend` builds `GetMyDevices`, the revoke-by-id
  command, a `DeviceMapper`, the repository helper, and the controller endpoints) then fan out the
  `mobile` consumer (and optional `frontend` admin panel for AC6). Spawn a `reviewer` per developer
  instance, concurrent on the same ticket.
- **Security gate mandatory** (`security_touching: true`): the story engages S1 (UserId from JWT), S2
  (policy on new endpoints), S3 (ownership-checked revoke returns NotFound), S4 (no leaked ids in
  `DeviceDto`) — all must clear Security before `done`. `optimizer` not required (no hot path). `qa` last.
- **`manual_steps: nswag-regen` (owner-only):** new endpoints + the now-produced `DeviceDto` change the
  mobile/customer (and admin, AC6) API contract. **Hold the mobile and admin-frontend layers until the
  owner confirms the regen.** Never hand-edit `libs/core/*-services/.../client/*` and never run
  `npm run generate-*-client`.
- **ef-migration:** **not expected** — `Device` already has all needed fields. Flag `manual_step:
  ef-migration` to the owner **only if** a `RefreshToken → Device` association turns out to be required
  for AC3 and is missing; the story's default is to reuse the existing `IRefreshTokenService.RevokeAsync`
  path without a schema change.
- **Key file:line anchors (verified 2026-06-01):**
  - `src/Cleansia.Core.AppServices/Features/Devices/DTOs/DeviceDto.cs:3-9` — the orphaned record to wire up.
  - `src/Cleansia.Core.AppServices/Features/Devices/RegisterDevice.cs`, `UnregisterDevice.cs` — only existing handlers (archetype for the new query/command; UserId pattern at `RegisterDevice.cs:35-36`).
  - `src/Cleansia.Core.Domain/Repositories/IDeviceRepository.cs:7-9` — `GetByUserIdAsync` exists; may add a `GetByIdAndUserAsync` for the ownership-checked revoke.
  - `Cleansia.Web.Customer`, `Cleansia.Web.Mobile.Customer`, `Cleansia.Web.Mobile.Partner` `DeviceController`s — add `GET` (list) + revoke endpoints, each with the correct `[Permission]` (S2); optional admin endpoint on `Cleansia.Web.Admin` for AC6.
  - `Logout.cs` / `IRefreshTokenService.RevokeAsync` — reuse for AC3 (the path Logout already uses).
  - `src/cleansia_android/` (`:customer-app` + `:partner-app`, sharing `:core`) — "Your devices" Compose screen, `@HiltViewModel` with sealed `UiState`/`ActionState`, repository method.

## Status log
- 2026-06-01 — draft (created by pm)
- 2026-06-10 — backend (Pass-1 fix, per architect-blessed spec): the AC3 security FAIL is fixed for real.
  `RevokeByDeviceAsync` previously matched `t.DeviceLabel == deviceId` — DeviceLabel (human-readable,
  from `X-Device-Label`/User-Agent) and the registered `Device.DeviceId` (stable per-install id) are
  never the same string, so the session-kill silently matched zero rows. Fix: new
  `RefreshToken.DeviceId` (nullable, `MaxLength(64)`, private set) written at issue time from the new
  `X-Device-Id` header via `IRequestMetadataProvider.DeviceId` (no User-Agent fallback; absent header
  → null), inherited on rotation (`existing.DeviceId ?? header` — DB value wins), and the revoke
  predicate is now `t.DeviceId != null && t.DeviceId == device.DeviceId`. `Issue`/`RotateAsync` gained
  optional defaulted `deviceId` params (additive — no caller breaks; no DTO change → no nswag-regen).
  `[EnableRateLimiting("auth")]` (ADR-0003) added to the session-mutating `Mine`/`Revoke` endpoints on
  Mobile.Customer, Mobile.Partner and Web.Customer DeviceControllers.
- 2026-06-10 — backend — **honest degradation contract (AC3 is best-effort, not a universal kill):**
  a device revoke always deactivates the push row and returns Success; the session-kill fires ONLY for
  refresh tokens carrying a `DeviceId` — i.e. tokens issued/rotated after this ships by a client
  sending `X-Device-Id`. Pre-existing tokens and tokens from clients without the header (web) have
  `DeviceId = null`, never match (the null-guard is load-bearing), and survive until natural sliding-
  window expiry; the next login/refresh from that handset writes the id and makes the session
  revocable. AC3 must not be overstated beyond this.
- 2026-06-10 — backend — TEST-FIRST proof (red→green): `RefreshTokenServiceRevokeByDeviceTests`
  (`src/Cleansia.Tests/Services/`, real `CleansiaDbContext` over SQLite in-memory — NOT a mock of
  `IRefreshTokenService`; the mocked `RevokeDeviceHandlerTests` stays as handler-wiring coverage only).
  Seeds two devices' active tokens + one null-DeviceId token; mutation check confirmed RED (2/3 fail)
  against the old `DeviceLabel` predicate, GREEN against the `DeviceId` match. Proves: target device's
  token revoked with reason `device_revoked`, sibling device's token stays alive, null-DeviceId token
  never matches.
- 2026-06-10 — backend — `manual_step: ef-migration` (RefreshToken.DeviceId nullable varchar(64), no
  backfill, no index — revoke scans the per-user active set). A temporary verification migration
  `20260609180604_TempDeviceIdVerify` (also carries `Users.LastLoginAt` for T-0189) is in the tree so
  the Postgres integration suite runs; the owner should regenerate/replace it via the official
  ef-migration step. `manual_step: android` — `AuthInterceptor.kt` must send `X-Device-Id` =
  `resolveDeviceId()` (separate android-lane ticket); until then mobile tokens carry no DeviceId.
- 2026-06-10 — backend — ADR posture per architect: additive session-lifecycle linkage; complements
  ADR-0001's session model (D2/D5 untouched — rotation still re-checks IsActive/profile/audience), no
  new or superseding ADR required. If device-session revocation is ever made a guaranteed kill
  (mandatory `X-Device-Id`), that is a session-model decision warranting its own ADR.
- 2026-06-10 — android — **`manual_step: android` closed + AC4 shipped (both apps).** iOS parity surface:
  - **Header:** `:core` `AuthInterceptor` now sends `X-Device-Id` on every request alongside
    `X-Device-Label` (ASCII-filtered, 64-char cap). Id source is the new `:core`
    `DeviceIdProvider` (ANDROID_ID, MANUFACTURER-MODEL fallback) — THE single per-install id;
    both apps' `PushTokenRepository.resolveDeviceId()` was deleted and now delegates to the same
    provider, so push registration and the header can never diverge. Hilt-provided in customer
    `AuthModule` + partner `NetworkModule`.
  - **"Your devices" screens:** customer `features/profile/DevicesScreen` (route `Routes.Devices`,
    profile row in the security group) and partner `features/devices/DevicesScreen`
    (`NavRoute.Devices`, ProfileScreen preferences card). API: `GET api/Device/Mine?currentDeviceId=
    {installId}` → list with `isCurrent` flag; `DELETE api/Device/{rowId}` behind a destructive
    `CleansiaDialog` confirm. States: sealed `DevicesUiState` Loading/Error(retry)/Loaded(empty-state)
    + `ActionState` Idle/Submitting/Error for revoke (re-entry guarded); one-shot `revoked`
    SharedFlow closes the dialog and the row is removed from local state (no refetch). Current
    device shows a "This device" chip and hides revoke. All text via string resources in
    en/cs/sk/uk/ru (customer 19 keys, partner 21 keys + shared period-pay block).
  - **Tests:** `DevicesViewModelTest` in both apps (load transitions, revoke success/failure/
    re-entry, effect emission) — green; `:core`/`:partner-app`/`:customer-app` assembleDebug green.
  - `ActionState` now has its canonical home in `:core` (`cz.cleansia.core.ui.state.ActionState`,
    used by partner); customer's identical local copy remains until its call sites migrate
    (patterns-mobile.md updated).

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->
- 2026-06-10 android note: harvested `ActionState` into `:core` (`cz.cleansia.core.ui.state.ActionState`)
  as its canonical home; `patterns-mobile.md` updated to point new code there (customer's identical
  local copy stays until call sites migrate).

### Security re-gate (Pass 2) — 2026-06-10 — VERDICT: PASS-WITH-NOTES

Prior FAIL (AC3 session-kill matched zero rows via `DeviceLabel == deviceId`) is **genuinely closed**:
- Real linkage: `RefreshToken.DeviceId` (nullable, MaxLength 64, `Users/RefreshToken.cs:52`) written at
  issue (`TokenService.cs:42`) and inherited on rotation (`RefreshTokenService.cs:98`, DB value wins);
  revoke predicate is `t.DeviceId != null && t.DeviceId == deviceId` (`RefreshTokenService.cs:129`).
- Non-mocked proof: `RefreshTokenServiceRevokeByDeviceTests` (real CleansiaDbContext/SQLite) — target
  token revoked with reason `device_revoked`, sibling device token survives, null-DeviceId token never
  matches. Seed design discriminates the old bug (identical DeviceLabels, distinct DeviceIds), so the
  RED claim is structurally sound, not vacuous. 14/14 green on re-run.
- S1 PASS — `RevokeDevice.Handler`/`GetMyDevices.Handler` take UserId only from `IUserSessionProvider`.
- S2 PASS — all four endpoints on all three DeviceControllers carry `[Permission(Policy.Authenticated)]`;
  `Policy.Authenticated` is in the fail-closed PolicyBuilder map (`PolicyBuilder.cs:207`).
- S3 PASS — `GetByIdAndUserAsync` scopes by `(Id, UserId, IsActive)` (`DeviceRepository.cs:21-25`);
  cross-user id is indistinguishable from missing (`device.not_found`), row + tokens untouched
  (`RevokeDeviceHandlerTests`). Note: project-wide failure shape is HTTP 400 + `device.not_found`
  (CleansiaApiController has no 404 branch) — consistent with every sibling, no existence leak.
- S4 PASS — `DeviceDto(Id, Platform, DeviceId, LastActiveAt, IsCurrent)`: no UserId/TenantId/PII;
  DeviceId is the caller's own per-install id.
- S5 PASS — `[EnableRateLimiting("auth")]` on `Mine` + `Revoke` in all three hosts; all hosts inherit
  `CleansiaStartupBase`, which registers the partitioned ADR-0003 policy.
- Honest degradation PASS — Success(true) asserts only what is always delivered (push row deactivated);
  best-effort session kill is documented at `RefreshToken.cs:43-50`, `RefreshTokenService.cs:124-128`,
  `IRequestMetadataProvider.cs:20-25` and the status log. The null-guard makes the no-header case a
  documented survive-until-expiry, not a false kill claim.
- S9 PASS — additive nullable column; `manual_step: ef-migration` flagged; temp migration
  `20260609180604_TempDeviceIdVerify` is additive/reversible and marked for owner replacement.

NOTES (non-blocking):
1. **Latent multi-tenant gap (recorded in `agents/backlog/security/auth-sessions.md`):** tokens are
   stamped `TenantId = null` on the anonymous login/refresh path, but `GetActiveByUserIdAsync` runs on
   an authenticated request — a user with a `tenant_id` claim would be filtered away from their own
   null-stamped token rows and the session kill would silently no-op. Pre-existing class (Logout shares
   it); dormant in single-tenant production; needs its own ticket before multi-tenant onboarding.
2. Web.Customer controller XML doc "ends its session" slightly overstates for sessions without a
   DeviceId (web); the load-bearing docs are honest — cosmetic.
3. Until the Android `X-Device-Id` manual step ships, mobile tokens carry no DeviceId and AC3 is
   pending-by-design — correctly flagged as `manual_step: android` in the status log.

### Reviewer (android lane) — 2026-06-10 — VERDICT: CHANGES REQUESTED

**R1 (BLOCKER) — `X-Device-Id` never reaches the endpoints that stamp it; the session-kill stays
dead for mobile.** The backend writes `RefreshToken.DeviceId` from the header on the **Login /
RefreshToken request** (`TokenService.cs:42` via `IRequestMetadataProvider.DeviceId`;
`RefreshTokenService.RotateAsync` inherits `existing.DeviceId ?? header`). Both apps send Login AND
RefreshToken through the **NoAuth** OkHttp client, which does not carry `AuthInterceptor`:
- customer `core/auth/AuthModule.kt:104-115` (`provideNoAuthOkHttpClient` = timeZone+networkError+logging
  only) and `:137` (`provideAuthApi(@NoAuthRetrofit ...)` — used by AuthRepository login and the
  authenticator refresh flow);
- partner `core/network/NetworkModule.kt:144-154` (`provideNoAuthOkHttp` = logging+timeZone only) and
  `:170-171`/`:175` (AuthApi + RefreshClient on NoAuthRetrofit).
So the header is only attached on business calls (where tokens are never issued), mobile tokens are
issued/rotated with `DeviceId = null`, and `RevokeByDeviceAsync`'s load-bearing null-guard
(`RefreshTokenService.cs:129`) means device revoke kills push but never the session — the exact
silent-zero-rows class the backend pass fixed. The status-log claim "`manual_step: android` closed"
is not true yet. **Fix expected:** extract the device-metadata headers (X-Device-Id, and
X-Device-Label while you're there — today its issue-time value comes from the User-Agent fallback,
i.e. "okhttp/4.x") into a small `:core` interceptor (mirror the TimeZone-interceptor pattern) and
install it on BOTH the Auth and NoAuth clients in BOTH apps; keep `AuthInterceptor` for Bearer only,
or have it delegate. Add a test (OkHttp MockWebServer or interceptor-level) asserting a
Login/RefreshToken request carries `X-Device-Id`.

**R2 (required, same theme) — normalize the id in the provider, not the interceptor.**
`AuthInterceptor.headerSafeDeviceId()` (`core/.../auth/AuthInterceptor.kt:53-57`) applies
`.filter{ascii}.take(64)` that push registration and the `currentDeviceId` query do NOT apply
(`PushTokenRepository` both apps, `DeviceManagementRepository.kt:29`, `DevicesRepository.kt:31-34`
send the raw value). On the `MANUFACTURER-MODEL` fallback a non-ASCII or >64-char id makes
`RefreshToken.DeviceId` (server-truncated 64, `RequestMetadataProvider.cs:46`) ≠ `Device.DeviceId`
(raw, MaxLength 256) → revoke silently degrades to push-only. This contradicts `DeviceIdProvider`'s
own contract ("two consumers must agree on the exact same string"). **Fix expected:** move the
ASCII-filter + 64-cap into `DeviceIdProvider.deviceId` and delete `headerSafeDeviceId()`.

Nits (fix with R1 or note): (a) `patterns-mobile.md:85-87` now says new code imports `ActionState`
from `:core`, but the NEW customer `DevicesViewModel.kt:10`/`DevicesScreen.kt:59`/test import the
local `cz.cleansia.customer.ui.state.ActionState` — flip the 3 imports or soften the sentence;
(b) both DevicesScreens' empty state is plain `Text`, catalog says `MascotEmptyState`
(`patterns-mobile.md` "Strings & states"); (c) partner `NetworkModule.kt:50` comment ("adds
X-Device-Label") will be stale after R1 — update it.

Verified good (no re-check needed on re-review): single id source wiring (both
`resolveDeviceId()` deleted, provider injected); contract parity with both mobile
`DeviceController`s (`Mine?currentDeviceId` → `DeviceDto` incl. `isCurrent`, `DELETE {deviceRowId}`,
`[Permission(Policy.Authenticated)]` + rate-limited); screens follow E1-E8 (sealed UiState,
:core/local ActionState, re-entry guard, one-shot `revoked` flow, Screen/Content split,
collectAsStateWithLifecycle, typed routes); current device chip + revoke hidden; strings 19
(customer) / 28 (partner) keys, identical key-sets across en/cs/sk/uk/ru, genuinely localized;
`device.not_found` resolves via the `error_*` key convention in both apps; check-consistency: 0 NEW
findings (hits in touched files are pre-existing E1/E6/F16 baseline); unit suites re-run by reviewer:
exit 0 — partner Devices 5/5 + PeriodPay 3/3, customer Devices 5/5 + Booking 18/18.
