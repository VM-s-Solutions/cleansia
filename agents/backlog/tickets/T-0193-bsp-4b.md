---
id: T-0193
title: Account-lockout / per-confirmation-code throttle (rate-limit fast-follow)
status: done
size: M
owner: backend
created: 2026-06-01
updated: 2026-06-15
depends_on: [T-0115, T-0189, T-0190]
blocks: []
stories: []
adrs: [0003]
layers: [backend, db]
security_touching: true
manual_steps: [ef-migration]
sprint: 2
source: ADR-0003 companion; Q-RATELIMIT-03
---

## Context

ADR-0003 (`agents/backlog/adr/0003-partitioned-rate-limiting.md`) ships **per-IP-only** partitioned
rate limiting in Wave 0 (T-0115). The ADR's "Residual risk" section names two classes it explicitly
does **not** close, both of which per-IP partitioning *structurally cannot* touch:

- **Distributed credential-stuffing** against `/Login` â€” a botnet of N IPs Ã— the per-IP window =
  N Ã— guesses/min against one account; each bot stays under its own IP window. (ADR-0003 Residual
  risk, bullet 1.)
- **Distributed confirmation-code guessing** â€” the worst surface, because the code space is small and
  the lookup is account-unbound. Verified in real code: `ConfirmUserEmail` looks up by **code alone**
  with no account binding (`Cleansia.Core.AppServices/Features/Auth/ConfirmUserEmail.cs:37,65` â†’
  `IUserRepository.GetByConfirmationCodeAsync`, implemented in
  `Cleansia.Infra.Database/Repositories/UserRepository.cs:45-48` as a global
  `FirstOrDefaultAsync(u => u.ConfirmationCode == token)`), the endpoint is `[AllowAnonymous]`
  (`Cleansia.Web.Customer/Controllers/AuthController.cs:58-67` + the Partner/Mobile equivalents), and
  the code is a **6-digit numeric** (`User.cs:95` `CreateWithPassword`, `User.cs:175`
  `UpdateConfirmationCode` â€” `Random.Shared.Next(100000, 999999)`, 15-min expiry). A 10^6 space with
  no per-code attempt cap is brute-forceable across a botnet in hours. Per-`sub` cannot apply
  (anonymous); per-IP cannot stop distribution.

The owner answered **Q-RATELIMIT-03 â†’ "SHIP IT"** (`agents/backlog/questions/answered.md`): Wave 0
ships per-IP-only and **this** (BSP-4b) is the **fast-follow**, not an in-wave blocker. This ticket is
that fast-follow: it adds the per-identity controls the partitioned limiter cannot â€” an
**account-lockout-after-N-failed-logins** counter and a **per-confirmation-code attempt counter** â€”
so the distributed residual is bounded by attempts-per-identity, not by source IP. ADR-0003 governs;
this ticket does **not** re-litigate the partition key or the limiter shape.

## Acceptance criteria

- [ ] **AC1 â€” Per-code attempt cap closes the confirmation-code hole.** Given a user with an active
  6-digit `ConfirmationCode`, When the `ConfirmUserEmail` path receives N wrong codes that resolve to
  that user's account (or, where no user resolves, N consecutive wrong-code attempts against the
  confirm surface within the window), Then the (N+1)-th attempt is rejected by an attempt-counter
  guard **before** a successful confirmation can be brute-forced, and a test proves a wrong-code spray
  that would have eventually hit a valid code is stopped at the cap. (ADR-0003 Residual risk bullet 2;
  hole at `ConfirmUserEmail.cs:37,65`.)
- [ ] **AC2 â€” Account-lockout-after-N-failed-logins.** Given an internal-auth user, When N consecutive
  failed `Login` attempts occur for that account (failure path today is
  `Login.cs:31-34` / `Handler` `Login.cs:81-85`), Then further login attempts for that account are
  refused for a cooldown window even from a **new IP** (proving distributed stuffing is bounded
  per-account, not per-IP), and a successful login or cooldown expiry resets the counter. A test proves
  attempt N+1 from a different IP is refused. (ADR-0003 Residual risk bullet 1.)
- [ ] **AC3 â€” Honest users are not locked out.** Given a legitimate user who fails login once or twice
  then succeeds (or confirms with the correct code on first try), Then no lockout/throttle fires and
  the success resets any partial counter â€” a test asserts a 1-2-failure-then-success sequence yields a
  normal token, and the counter is cleared.
- [ ] **AC4 â€” Lockout/throttle state is server-side and survives process restart.** Given the lockout
  and per-code attempt counters live on persisted state (new `User`/identity columns, e.g. a failed-
  attempt count + lockout-until timestamp, alongside the existing `ConfirmationCode` /
  `ConfirmationCodeExpiresAt` fields on `User.cs:50-52`), Then the guard is enforced by the
  application layer (validator/handler/repository), not by the in-process rate limiter, and a test
  confirms the counter is read from the store (not memory). (Distinct from T-0115's in-memory limiter.)
- [ ] **AC5 â€” Observable + S6-safe rejection.** Given a lockout or per-code-cap rejection, Then it
  returns a deterministic business error (a new `BusinessErrorMessage` constant in the existing
  `auth.*` namespace â€” e.g. `auth.account_locked` / `auth.too_many_attempts`, following the
  `category.specific_error` pattern in
  `Cleansia.Core.AppServices/Common/BusinessErrorMessage.cs`) with the corresponding `errors.auth.*`
  keys in all 5 locale files, and the log line for the rejection never logs the attempted code or the
  password (S6).
- [ ] **AC6 â€” No weakening of T-0115's limiter.** Given this ticket, Then it makes **zero** edits to
  `CleansiaStartupBase.cs`, the limiter policies, or the partition-key functions â€” the only
  rate-limiter interaction is that these per-identity controls sit *behind* the per-IP windows
  T-0115 already establishes. A grep proves no `AddPolicy`/`AddRateLimiter`/`AddFixedWindowLimiter`
  edits are in this diff. (Serialization safety â€” see Implementation notes.)
- [ ] **AC7 â€” Test-first evidence.** Given the tests for AC1-AC4, Then the diff shows the failing tests
  land before/with the implementation and the status log records redâ†’green with each AC mapped to a
  case, per `agents/knowledge/testing.md` (this is security + auth logic â†’ strict).

## Out of scope

- **The partition key / forwarded-headers / cardinality cap / limiter shape** â€” all owned by
  ADR-0003 / T-0115 (this ticket's `depends_on`). Do not touch `CleansiaStartupBase.cs`.
- **`ip + attemptedUsername` keying on `/Login`** â€” ADR-0003 D2 rejected it for v1 (attacker-controlled
  body, runs before model binding). The correct per-account control is the lockout this ticket adds.
- **`/Register` account-farming bound and the â‰¤2Ã— mixed-auth residual** â€” ADR-0003 names these; if the
  owner wants the `/Register` IP-throttle tightened beyond what T-0115 already provides, file separately.
- **BSP-4c** (client-side `Retry-After` back-off jitter) and **BSP-4d** (attribute coverage of money
  endpoints) â€” separate tickets.
- **Distributed (cross-instance) correctness** â€” instances are pinned to 1 (Q-RATELIMIT-01); a
  distributed store is a future ADR. The lockout counter here is DB-backed, so it is already correct
  across a future scale-out for *this* control.
- No NSwag regen and no controller attribute edits are expected (the change is in the auth
  command/validator/repository + an EF migration); flag if the contract surface changes.

## Implementation notes

**Serialization cluster (TICKET-MAP):** This ticket is a fast-follow of the
`CleansiaStartupBase.cs` (pipeline + limiter) cluster head **BSP-4 â†’ SEC-W3 â†’ PROD-CONFIG**. It is
**not** in that cluster itself because AC6 forbids touching `CleansiaStartupBase.cs` â€” its edit
surface (the auth feature handlers/validators in `Cleansia.Core.AppServices/Features/Auth/`, the
`User` entity + its EF config, and `UserRepository`) does not overlap the limiter cluster, so it may
run concurrently with that cluster's later members. **Note the dependency:** it must land **after**
T-0115 (BSP-4) so the per-IP limiter is already in place â€” the two controls are layered, not
alternatives.

**Governing ADR: ADR-0003** â€” read its "Residual risk" section in full; bullets 1 and 2 are the exact
holes this closes, and the ADR pre-decides that the fix is account/per-code attempt counters (not a
body-keyed limiter). This ticket does not change any ADR-0003 decision.

**Real edit sites (where known):**
- `Cleansia.Core.AppServices/Features/Auth/ConfirmUserEmail.cs` â€” the `Validator.ValidateUserTokenAsync`
  (`:35-53`) and `Handler` (`:63-69`) must consult/increment a per-code (or per-resolved-account)
  attempt counter and refuse past the cap before the happy path.
- `Cleansia.Core.AppServices/Features/Auth/Login.cs` â€” the failed-password path
  (`Validator` `:31-34` / `Handler` `:81-85`) must increment a per-account failed-login counter and
  the validator/handler must short-circuit a locked account; a success resets it.
- `Cleansia.Core.Domain/Users/User.cs` â€” add the persisted counter/lockout state next to
  `ConfirmationCode`/`ConfirmationCodeExpiresAt` (`:50-52`) with the entity methods to
  increment/reset/clear (mirror the existing `ConfirmEmail()` / `UpdateConfirmationCode()` style); add
  the corresponding cleanup to `Anonymize()` (`:198-215`).
- `Cleansia.Infra.Database/EntityConfigurations/UserEntityConfiguration.cs` â€” map the new columns.
- `Cleansia.Infra.Database/Repositories/UserRepository.cs` â€” if a per-code counter keyed off the code
  is used, extend the lookup; keep `GetByConfirmationCodeAsync` (`:45-48`) account-aware so the
  counter is bound to the resolved account (the ADR notes the lookup-by-code-alone is itself part of
  the weakness).
- `Cleansia.Core.AppServices/Common/BusinessErrorMessage.cs` â€” new `auth.*` error constants; mirror in
  `apps/<app>/src/assets/i18n/{en,cs,sk,uk,ru}.json` under `errors.auth.*`.

**MANUAL_STEP â€” ef-migration (owner-only):** the new `User` columns require a migration. Do **not**
run `dotnet ef migrations add` / `database update`. The owner creates and applies it; dependent
verification of AC4 holds until the owner confirms the migration is applied.

**TEST-FIRST (`agents/knowledge/testing.md`).** Security + auth logic â†’ strict red-green-refactor.
Unit-test the lockout/attempt-counter logic on the `User` entity + validators/handlers; the cross-IP
lockout (AC2) and per-code cap (AC1) are the load-bearing cases. The existing
`Cleansia.IntegrationTests` harness (`BaseIntegrationTest` / `PostgresContainerFixture`, e.g.
`Features/Auth/ConfirmUserEmailTests.cs`, `Features/Auth/LoginTests.cs`) is MediatR/DbContext-level
and **is** sufficient here (this control is in the app/DB layer, not the middleware pipeline) â€” reuse
it; the new host harness from T-0115 is not required. Status log must show redâ†’green and map each AC
to a case.

**Security gate mandatory** (`security_touching: true`): this closes the distributed-guessing residual
ADR-0003 deferred. Reviewer + security verdicts required before `done`. The cross-instance residual is
accepted (instances pinned to 1, Q-RATELIMIT-01) â€” do not re-litigate.

## Status log
- 2026-06-01 â€” draft (created by pm)
- 2026-06-12 — TDD red: authored the failing tests FIRST — `UserLockoutStateTests` (9),
  `LoginLockoutValidatorTests` (9, all three login surfaces), `LoginHandlerThrottleResetTests` (1),
  `ConfirmUserEmailAttemptCapTests` (4), `ChangePasswordAttemptCapTests` (4) — captured red
  (CS1061/CS0117 against the intended `User`/`IUserRepository`/`BusinessErrorMessage` contract; the
  members did not exist). Then implemented; full `Cleansia.Tests` green 1137/1137 (27 new green).
- 2026-06-12 — AC map: **AC1** → `ConfirmUserEmailAttemptCapTests.When_The_Budget_Is_Spent_Even_The_Correct_Live_Code_Is_Refused`
  (unit) + `ConfirmationCodeAttemptCapTests.Once_The_Budget_Is_Spent_Even_The_Correct_Live_Code_Cannot_Confirm`
  + `ChangePasswordResetCodeCapTests.A_Wrong_Code_Spray_Is_Stopped_At_The_Cap_Before_The_Valid_Code_Succeeds`
  (integration — the literal spray-stopped-at-cap proof on the account-bound reset surface; see Review
  for the post-T-0189 AC1 reading). **AC2** → `LoginLockoutValidatorTests.When_The_Account_Is_Locked_Even_The_Correct_Password_Is_Refused`
  + `AccountLockoutTests.After_Five_Wrong_Passwords_Even_The_Correct_Password_Is_Refused` (the Login
  command carries no caller IP — the guard keys on the account row, so it holds from any IP by
  construction; cooldown-expiry reset covered by `When_The_Lockout_Window_Has_Expired_The_Correct_Password_Passes`).
  **AC3** → `LoginHandlerThrottleResetTests` + `AccountLockoutTests.A_Couple_Of_Failures_Then_Success_Issues_A_Token_And_Clears_The_Counter`
  + `ConfirmationCodeAttemptCapTests.A_First_Try_Valid_Code_Confirms_Without_Tripping_The_Cap`.
  **AC4** → persisted `User` columns + atomic `ExecuteUpdateAsync` counters in `UserRepository`;
  store-read proof in `AccountLockoutTests.Failed_Attempts_Are_Persisted_To_The_Store` and
  `ConfirmationCodeAttemptCapTests.Expired_Code_Retries_Are_Charged_To_The_Account_In_The_Store`
  (fresh DI scope per attempt = fresh DbContext). **AC5** → `auth.account_locked` /
  `auth.too_many_attempts` in `BusinessErrorMessage` + `api.auth.*` keys in all 5 locales of all 3
  apps (admin also `errors.auth.*`); rejection logs carry userId only, never the code/password (S6).
  **AC6** → zero edits to `CleansiaStartupBase.cs`; grep of the diff for
  `AddPolicy|AddRateLimiter|AddFixedWindowLimiter` is empty. **AC7** → this log + commit order.
- 2026-06-12 — build: `Cleansia.Api.sln` 0 errors; `Cleansia.Tests` 1137/1137 green (single-threaded).
  Integration tests compile but the three new classes need the owner's Users migration to run
  (see MANUAL_STEP) — until then the IntegrationTests suite is schema-blocked on the new columns.
- 2026-06-12 — MANUAL_STEP ef-migration (owner): new `Users` columns `FailedLoginAttempts`
  (int, default 0), `LockoutEndsAt` (timestamptz, null), `ConfirmationCodeAttempts` (int, default 0),
  `ResetPasswordCodeAttempts` (int, default 0). Additive + defaulted (S9-safe). After applying, run
  `Cleansia.IntegrationTests` to close AC4 verification.

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->

**Backend handoff note (2026-06-12) — for the security reviewer:**

- **Model + thresholds.** Per-account state on `User`: `FailedLoginAttempts` + `LockoutEndsAt`
  (lockout: 5 consecutive failures → 15-min window, counter resets when the window opens so expiry
  restores a fresh budget; constants `User.MaxFailedLoginAttempts` / `User.FailedLoginLockout`), and
  per-code budgets `ConfirmationCodeAttempts` / `ResetPasswordCodeAttempts`
  (`User.MaxCodeVerificationAttempts = 5` verification attempts per issued code; a freshly issued
  code re-grants the budget; consuming the code clears it).
- **Failure-path persistence is atomic SQL, not tracked writes** — a failing command never reaches
  the UoW commit, so the validator charges the counter via conditional `ExecuteUpdateAsync`
  (`WHERE counter < cap` / `WHERE not locked`), mirroring `PromoCodeRepository
  .TryIncrementGlobalRedemptionsAsync` (S7a; race-proof under distributed guessing). Pattern
  harvested into `agents/knowledge/patterns-backend.md` (Repository pattern section).
- **All three internal-auth login surfaces** (`Login`, `AdminLogin`, `PartnerLogin`) carry the
  identical lockout rules — the ticket names `Login.cs`, but the same `User` row backs :5000/:5001,
  and leaving admin/partner login un-locked would keep the stuffing hole open on the most valuable
  accounts. Lockout state is shared per-account across surfaces by construction.
- **AC1 reading post-T-0189 (deliberate, please confirm):** the AC was written against the 6-digit
  `Random.Shared` code. T-0189/T-0190 already replaced confirm/reset codes with 128-bit
  `SecurityTokens` stored hashed, so an unresolvable wrong guess (a) can no longer be attributed to
  any account and (b) can no longer "eventually hit a valid code" within any feasible window. The
  per-code cap is therefore enforced where attempts ARE attributable: every confirm attempt that
  RESOLVES an account is charged before validity is checked (expired-token retries burn budget; once
  spent, even the correct live code is refused), and the email-bound reset path charges every guess
  against the targeted account — the literal AC1 spray test lands there
  (`ChangePasswordResetCodeCapTests`). The AC1 parenthetical's surface-wide counter for unresolvable
  guesses was deliberately NOT built: with 128-bit tokens it mitigates nothing real while handing any
  attacker a platform-wide confirm-surface DoS lever; that residual stays bounded by token entropy +
  T-0115's per-IP window. If the reviewer wants the surface-wide window anyway, it is an additive
  follow-up (new counter store), not a rework.
- **No new enumeration oracle:** the login surface already discloses account existence
  (`NotExistingUserWithEmail` — pre-existing contract, unchanged). `auth.account_locked` is only
  reachable past that disclosure, and AC5 mandates the distinct code so honest users understand why
  the correct password fails. `auth.too_many_attempts` on confirm is only reachable by the holder of
  the real code; on reset it differs from `invalid_reset_token` only for the targeted account. While
  locked, the password is NOT evaluated (Cascade.Stop) — no correctness oracle and no counter burn.
- **Known benign residual:** a same-request success reads the tracked entity loaded before the charge,
  so the consumed code's attempt value may persist non-zero; it is meaningless once the code is
  cleared and is reset by the next issuance. Asserted accordingly in the integration tests.
- **Out-of-scope residual (flagging, not fixing):** `ChangeOwnPassword`'s current-password check is an
  authenticated guessing surface not covered by this lockout; bounded by the per-`sub` 30/min window.
  File separately if wanted.

**Reviewer verdict (2026-06-12): APPROVED** — conditional on (a) the security gate (mandatory,
`security_touching: true`) ratifying the post-T-0189 AC1 reading (no surface-wide counter for
unresolvable confirm guesses; residual = 128-bit token entropy + T-0115 per-IP window) and the
reset-path `too_many_attempts`-vs-`invalid_reset_token` shape, and (b) AC4 closure: owner applies the
Users migration, then `Cleansia.IntegrationTests` (AccountLockoutTests, ConfirmationCodeAttemptCapTests,
ChangePasswordResetCodeCapTests) must run green before `done`.

Gates verified mechanically by the reviewer: `dotnet build src/Cleansia.Api.sln` 0 errors;
`Cleansia.Tests` 1137/1137 green (41 in the new/updated lockout classes);
`check-consistency.mjs` on the touched dirs → 11 violations, ALL pre-existing at HEAD (B3
`BaseAuthValidator` on the login validators, B1 scalar returns on Logout/Register/ResendConfirmationEmail)
— none introduced by this diff; AC6 grep for `AddPolicy|AddRateLimiter|AddFixedWindowLimiter` over the
diff is empty and `CleansiaStartupBase.cs` is untouched; `api.auth.account_locked` /
`api.auth.too_many_attempts` present in all 5 locales × 3 apps (admin also `errors.auth.*`), matching
the runtime key path in `libs/core/services/src/lib/interceptors/http-error.interceptor.ts:29,41`.

Notes for the PM (non-blocking):
1. **Ledger gap:** the checker's B3 hits on `Login/AdminLogin/PartnerLogin/GoogleAuth/Register/RegisterEmployee`
   (`BaseAuthValidator`) pre-date this ticket but are NOT in `backlog/audits/consistency-violations.md`
   (F6 lists only the PayConfig/PayPeriod/User variants). Architect should extend F6 or file the
   canonicalization ticket.
2. **Harvest ratification:** the new `patterns-backend.md` paragraph ("Failure-path counters bypass the
   UoW deliberately") defines a new canonical shape (validator-invoked atomic `ExecuteUpdateAsync`),
   i.e. a sanctioned exception to "validators validate shape only / handlers happy-path only". The code
   mirrors the existing `PromoCodeRepository.TryIncrementGlobalRedemptionsAsync` precedent and is fine,
   but the standard-change itself is an Architect call — have the Architect ratify the paragraph.
3. **Doc mismatch:** AC5 and root `CLAUDE.md` say backend error keys map to `errors.*`, but the shared
   interceptor translates `api.${errorKey}`. The dev followed the runtime contract (correct). Worth a
   docs/conventions fix so future ACs name the real namespace.
4. Out-of-scope residuals the dev flagged stand as flagged: `ChangeOwnPassword` current-password
   guessing (per-`sub` 30/min only) — file separately if wanted.
