---
id: T-0201
title: Decompose AddSavedAddress god-method
status: ready
size: M
owner: â€”
created: 2026-06-01
updated: 2026-06-13
depends_on: [T-0150]
blocks: []
stories: []
adrs: []
layers: [backend]
security_touching: false
manual_steps: []
sprint: 3
source: finding DA-8
---

## Context
Audit finding **DA-8**: `AddSavedAddress.Handler.Handle`
(`src/Cleansia.Core.AppServices/Features/SavedAddresses/AddSavedAddress.cs:89-147`) is a ~58-line
god-method that interleaves several distinct responsibilities in one flow:

1. Identity resolution â€” `userSessionProvider.GetUserId()!` (line 91).
2. Default-country fallback â€” resolve missing `CountryId` to ISO `"CZE"`, else first configured
   country, else throw (lines 92-99).
3. Address dedup/reuse â€” `addressRepository.GetAddressAsync(...)` or `Address.Create(...)`
   (lines 101-103).
4. Duplicate-saved-address guard â€” load the user's saved set and fail with
   `BusinessErrorMessage.SavedAddressAlreadyExists` if the address is already saved (lines 105-114).
5. Conditional add of a new `Address` row (lines 116-119).
6. Default-clearing â€” `ClearDefaultForUserAsync` when `SetAsDefault` (lines 121-124).
7. Persist the `SavedAddress` (lines 126-132).
8. **Inline DTO projection** â€” a hand-built `new SavedAddressDto(...)` (lines 135-146).

This is a long, multi-concern handler. The inline projection at step 8 is also a `consistency.md`
**B9** violation and is **triplicated**: the same `new SavedAddressDto(...)` projection appears in
`AddSavedAddress.cs:135`, `UpdateSavedAddress.cs:150`, and `GetSavedAddresses.cs:23`. There is no
`Mappers/` folder and no `SavedAddressMapper` in this feature today, and no SavedAddress test exists
in `Cleansia.Tests`.

This is a **Wave 3 consistency/quality refactor â€” behavior must not change.** The fix is to pin the
current behavior with a characterization test, then decompose: extract the cohesive steps into named
private helpers (and/or a small domain/app service) and replace the inline projection with the
canonical `address`/`saved` â†’ `SavedAddressDto` mapper extension, leaving the handler a readable
orchestration of well-named steps.

## Acceptance criteria
- [ ] **AC1 (TEST-FIRST, characterization)** â€” Given the current `AddSavedAddress.Handler` behavior,
  When a characterization unit test is written **before any production change** (mocked
  `IAddressRepository`, `ISavedAddressRepository`, `ICountryRepository`, `IUserSessionProvider`), Then
  it pins every existing branch and passes green against the **unrefactored** handler: (a) success when
  `CountryId` is supplied; (b) default-country fallback to ISO `"CZE"`; (c) fallback to the first
  configured country when `"CZE"` is absent; (d) `InvalidOperationException("No countries configured")`
  when none exist; (e) reuse of an existing `Address` vs. `Address.Create`; (f) duplicate guard returns
  `Failure` with `Error.Code == nameof(command.Street)` and
  `BusinessErrorMessage.SavedAddressAlreadyExists`; (g) `ClearDefaultForUserAsync` is invoked iff
  `SetAsDefault` is true; (h) the returned `SavedAddressDto` field-for-field. The status log records
  "redâ†’green: characterization pinned" per `agents/knowledge/testing.md`.
- [ ] **AC2 (behavior unchanged)** â€” Given the characterization test from AC1, When the handler is
  decomposed, Then the test stays green **without modification** â€” same `BusinessResult` success/failure
  outcomes, same `Error.Code`/`BusinessErrorMessage`, same repository call sequence and side effects,
  same DTO. No wire-shape change to `Command` or `SavedAddressDto`, so **no nswag-regen**.
- [ ] **AC3 (smell removed â€” decomposition)** â€” Given the refactored `AddSavedAddress.Handler.Handle`,
  When read, Then it is a short orchestration delegating to **named, single-responsibility helpers**
  (e.g. resolve-country-id, get-or-create address, the duplicate guard, build-the-dto), and no single
  method still bundles all of steps 1â€“8.
- [ ] **AC4 (consistency â€” B9, de-triplicate)** â€” Given the inline `new SavedAddressDto(...)`
  projection, When the refactor lands, Then it is replaced by a single canonical mapper extension
  (`SavedAddressMapper.MapToDto(...)` in a `Features/SavedAddresses/Mappers/` folder, per
  `consistency.md` B9 and `patterns-backend.md`), and the hand-built projection no longer appears in
  `AddSavedAddress.cs` â€” **and** the same extension is reused in `UpdateSavedAddress.cs:150` and
  `GetSavedAddresses.cs:23`, eliminating the triplication.
- [ ] **AC5 (gate green)** â€” Given the touched SavedAddresses feature area, When the consistency
  checker runs, Then `node agents/tools/check-consistency.mjs` reports clean for the touched area, and
  `dotnet build Cleansia.Api.sln` + `dotnet test src/Cleansia.Tests` pass.

## Out of scope
- Any change to validation, ownership, or business rules â€” this is a no-behavior-change refactor.
- The default-country `"CZE"` / Mapbox-bounds / length-cap **magic constants** â€” that is **DA-9
  (T-0150)**. T-0201 may consume the named constant **if T-0150 has already landed**, but introducing
  the constant is not this ticket's job; do not duplicate it.
- `UpdateSavedAddress`/`GetSavedAddresses` beyond switching their projection to the shared mapper
  (AC4) â€” no other refactor of those handlers here.
- Soft-delete of `SavedAddress` / the `DeleteSavedAddress` `ICommand`-no-Response B1 fix (DA-10).
- Adding/altering any `BusinessErrorMessage` keys or `errors.*` translations (DA-7).

## Implementation notes
- **TEST-FIRST is mandatory** (`agents/knowledge/testing.md` â€” "When you're changing existing untested
  code: write a characterization test first that pins the *current* behavior, confirm it passes, then
  TDD the change on top"). The handler is currently untested. Write AC1's test, see it green against
  the original handler, *then* decompose â€” the test must never be edited to accommodate the refactor.
- **Canonical pattern:** `consistency.md` **B9** â€” "Map outputs with the `entity.MapToDto()` extension;
  never inline-project a DTO in a handler"; `patterns-backend.md` (`Mappers/` extension methods,
  `.MapToDto()`). Create `Features/SavedAddresses/Mappers/SavedAddressMapper.cs` exposing the extension
  that builds `SavedAddressDto` from the `Address` + `SavedAddress` + resolved country name; reuse it
  in all three handlers.
- Keep helpers in the AppServices layer; do not push new I/O or rules into the handler. Domain mutation
  still goes through rich domain methods (`Address.Create`, `SavedAddress.Create`) per B7 â€” the
  decomposition only regroups orchestration, it does not move logic into entities.
- **No serialization conflict by default** (DA-8 is not in `TICKET-MAP.md`'s shared-file map). BUT it
  edits `AddSavedAddress.cs`, `UpdateSavedAddress.cs`, and `GetSavedAddresses.cs` â€” the same
  SavedAddress files as **DA-9 (T-0150)**. Per the T-0150 note, **do not run T-0201 and T-0150
  concurrently; serialize them.** If T-0150 lands first, rebase onto the named constants; if T-0201
  lands first, T-0150 rebases onto the extracted helpers.
- **No serialization payload concern:** no wire/DTO change, so the JSON contract and NSwag clients are
  untouched (`manual_steps: []`, no nswag-regen).
- All cited line numbers verified against live code on 2026-06-01.

## Status log
- 2026-06-01 â€” draft (created by pm)
- 2026-06-13 — **ready** (PM, Wave-5 intake / Batch **5E**). Dep T-0150✓ is `done` (the DA-9 `"CZE"`/
  geo/length constants merged Wave 1), so T-0201 consumes the named constants and there is **no
  serialization conflict with T-0150** (it has landed). DoR met: AC1–AC5 observable (characterization
  pins all 8 branches; B9 mapper de-triplicates the inline `SavedAddressDto` projection across
  AddSavedAddress/UpdateSavedAddress/GetSavedAddresses), M, not security-touching, no migration/regen
  (wire shapes unchanged). **Serialize against T-0203** — both touch SavedAddress-area handlers; in
  this wave T-0201 owns the SavedAddress feature files (AddSavedAddress/UpdateSavedAddress/
  GetSavedAddresses + new Mappers), so run them in the same lane (5E SavedAddress lane), not concurrently.)

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->
