---
id: T-0205
title: "Remove dead/unsafe code: HandlebarsTemplateEngine XSS+FormatException trap, dead SendGridFactory, dead scrap/auth"
status: done
size: S
owner: â€”
created: 2026-06-01
updated: 2026-06-14
depends_on: [T-0144]
blocks: []
stories: []
adrs: []
layers: [backend, mobile]
security_touching: false
manual_steps: []
sprint: 3
source: findings BLIND-4/9/10/11
---

## Context

Wave-3 consistency/quality cleanup. This ticket removes four pieces of dead-or-unsafe code uncovered
by the "blind-spot" slice of the audit (`agents/backlog/audits/AUDIT-2026-06-01-slice-reports.md`
Â§BLIND-4/9/10/11, findings JSON `â€¦-findings.json`). All four are dead/divergent code, not live
behavior â€” the goal is to remove the smell with **zero behavior change** for any current caller. The
title also folds in the sibling mobile dead-code finding **IA-11** ("dead scrap/auth"), which is the
reason `mobile` is a layer here.

The four (+1) smells, all confirmed against the real code:

1. **BLIND-4 (latent XSS / dead code)** â€” `Cleansia.Infra.Services/Templates/HandlebarsTemplateEngine.cs`
   is registered at `Cleansia.Infra.Services/ServiceCollectionExtensions.cs:13`
   (`AddScoped<ITemplateEngine, HandlebarsTemplateEngine>()`) but has **zero production consumers** â€”
   grep confirms `ITemplateEngine`/`CompileAsync` is referenced only inside its own
   `Templates/*` files; all real email goes through SendGrid **dynamic templates** (`EmailService`),
   so rendering happens in SendGrid's cloud, not here. Its helpers (`formatNumber`, `add`, `multiply`,
   `eq`) call `writer.WriteSafeString(...)` with **no HTML-encoding** on the result, and the engine
   is built with `Handlebars.Create()` defaults â€” a latent stored/reflected-XSS trap the moment
   anyone wires it.
2. **BLIND-9 (FormatException trap)** â€” same file, `HandlebarsTemplateEngine.cs:33,84-85,104-105,117-118`:
   the numeric helpers use `Convert.ToDecimal(parameters[0])` / `Convert.ToInt32(...)` with no
   try-parse and **current-culture** parsing (itself a bug in a 5-language app). A single non-numeric
   value would throw `FormatException` and crash the whole render. Latent (gated by BLIND-4's "unused").
3. **BLIND-10 (divergent dead abstraction)** â€” `Cleansia.Infra.Clients/SendGrid/SendGridClientFactory.cs`
   exposes a `BusinessResult`-returning `SendTemplateEmailAsync(...)` and an `EmailNotSentError =
   "email.sending_failed"` constant that have **no production consumer**. The real send path
   (`Cleansia.Core.AppServices/Services/EmailService.cs:348,390`) ignores the factory's send method,
   does `new SendGridClient(...)` and throws `EmailDeliveryException` â€” a second, divergent contract
   for the same operation. The only live use of the factory is `HealthController.cs`, which calls
   `CreateClient()` only. Two contracts â†’ the unused one rots; violates `conventions.md` "one way to
   do each thing".
4. **BLIND-11 (log-spam)** â€” `Cleansia.Infra.Clients/Fcm/FcmPushDispatcher.cs:48-53`: when
   `ServiceAccountJson` is empty the dispatcher correctly no-ops, but logs a `Warning` (with token
   count + event key) on **every** dispatch. In an environment with push intentionally disabled this
   buries real signals. (The terminal config-missing branch at `:152` already latches `_initAttempted`
   correctly â€” keep that.)
5. **IA-11 (dead mobile code, the "scrap/auth")** â€” `src/cleansia_android/scrap/partner-app-pre-rebuild/`
   ships **13 `.kt` files** under `kotlin/auth/{viewmodels,screens}/` that declare the **same package**
   as the live partner-app auth code (`cz.cleansia.partner.features.auth.*`). They are an unreferenced
   pre-rebuild backup that risks duplicate-class confusion / accidental edits to the wrong copy.

## Acceptance criteria

This is a refactor/dead-code-removal: **characterization-test-first, then delete the smell, behavior
identical.** Per `agents/knowledge/testing.md:53-57`, when touching untested code write a
characterization test that pins **current** behavior, confirm it passes, then make the change keeping
it green.

- [ ] **AC1 (BLIND-4/9 â€” delete the dead engine, behavior unchanged)** â€” Given `HandlebarsTemplateEngine`
      has zero production consumers (grep evidence in the diff: `ITemplateEngine`/`CompileAsync` used
      only in `Templates/*`), When the engine file, `ITemplateEngine.cs`, and its DI registration at
      `ServiceCollectionExtensions.cs:13` are removed, Then `dotnet build Cleansia.Api.sln` succeeds,
      no remaining reference to `ITemplateEngine`/`HandlebarsTemplateEngine` exists anywhere
      (grep returns nothing), and every existing test still passes â€” i.e. **no observable behavior
      changes** for any current caller (there are none). The XSS `WriteSafeString` surface and the
      `Convert.ToDecimal` `FormatException` trap are gone by deletion.
- [ ] **AC2 (BLIND-10 â€” collapse the divergent SendGrid contract, behavior unchanged)** â€” Given the
      live email path is `EmailService.cs:348,390` (`new SendGridClient` + `EmailDeliveryException`)
      and `HealthController` uses only `ISendGridClientFactory.CreateClient()`, When the **dead**
      `SendTemplateEmailAsync` method + `EmailNotSentError` constant are removed from
      `SendGridClientFactory` and `ISendGridClientFactory` (keeping `CreateClient()` intact), Then
      `dotnet build` succeeds, the HealthController behavior is unchanged, and grep confirms no
      remaining reference to the removed member. **Characterization test first:** a test pinning that
      `HealthController` health output is unchanged (or, minimally, that `CreateClient()` still
      returns a client) is green before and after.
- [ ] **AC3 (BLIND-11 â€” silence the per-dispatch warning, dispatch behavior unchanged)** â€” Given a
      characterization test that asserts `FcmPushDispatcher.SendAsync` with empty `ServiceAccountJson`
      returns `PushDispatchResult(0, tokenCount, [])` (a no-op skip), When the per-dispatch `Warning`
      at `:48-53` is changed to log at most **once** for the disabled state (latched flag) or at
      `Debug` after the first occurrence, Then the return value / dispatch outcome is **identical**
      (test still green) and a second consecutive disabled-state dispatch emits no second `Warning`.
- [ ] **AC4 (IA-11 â€” delete the scrap tree)** â€” Given `src/cleansia_android/scrap/partner-app-pre-rebuild/`
      holds 13 unreferenced `.kt` files declaring live packages, When the `scrap/` tree is deleted,
      Then the partner-app Android module still compiles (no source set referenced it) and a grep for
      `scrap/partner-app-pre-rebuild` returns nothing.
- [ ] **AC5 (no behavior change overall + tool clean)** â€” All pre-existing backend tests pass
      (`dotnet test src/Cleansia.Tests`), no new behavior is introduced, and
      `node agents/tools/check-consistency.mjs` is **clean for the touched areas**
      (`Cleansia.Infra.Services/Templates`, `Cleansia.Infra.Clients/SendGrid`,
      `Cleansia.Infra.Clients/Fcm`, the deleted Android scrap path) â€” no new violations reported.

## Out of scope

- **Auditing the SendGrid hosted (dynamic) templates** for `{{{triple-stache}}}` around user-supplied
  fields (`CustomerName`/`Address`/`EmployeeName`) â€” that's the **security story** half of BLIND-4
  (out-of-repo, ops task), not this dead-code removal.
- **Routing Stripe/SendGrid through `IHttpClientFactory`** (resilience + OTel) â€” that is **BLIND-5**
  (T-0144, Wave 1). Do **not** rewire `EmailService`'s send path here; only delete the dead factory
  member. If T-0144 has already landed and naturally collapsed the two contracts, reconcile rather
  than re-delete.
- **Error classification** (Transient/Permanent/Configuration) â€” **BLIND-6**.
- **Push idempotency / dedup** (the FCM at-most-once double-send) â€” **BLIND-8 / T-0182 (F7)**. This
  ticket only quiets the disabled-state log noise; it must not alter dispatch/dedup semantics.
- Any DTO/endpoint contract change â†’ none here, so **no NSwag regen and no EF migration**.

## Implementation notes

- **Canonical pattern (consistency):** the unifying rule is **"one way to do each thing / no dead
  code"** â€” `agents/knowledge/consistency.md` (intro + the Â§B/Â§C/Â§E "âœ— deviation" framing) and
  `agents/knowledge/conventions.md` ("reuse the real types", "one way to do each thing", "no dead
  code â€” delete unreferenced methods/classes"). BLIND-10 is the textbook two-contracts deviation;
  BLIND-4/9 and IA-11 are textbook dead code.
- **TEST-FIRST** per `agents/knowledge/testing.md:53-57` (changing existing untested code â†’
  characterization test pinning current behavior first, confirm green, then change, keep green).
  Strict red-green is not required for pure deletions where the only assertion is "build + existing
  tests stay green and the symbol is gone"; for AC2/AC3 write the characterization test for the
  surviving behavior (HealthController `CreateClient()`, FcmPushDispatcher disabled-state no-op)
  before touching the code.
- **Sequence:** the four sub-edits touch **disjoint files** (Templates/, SendGrid/, Fcm/, Android
  scrap/) so they can be done in one pass; build + test between each is cheap. Do the deletions
  (AC1, AC4) first, then the two narrowing edits (AC2, AC3).
- **Serialization (TICKET-MAP):** not in any shared-file cluster. The one overlap to watch is
  `SendGridClientFactory.cs` / the EmailService send path, shared with **BLIND-5 (T-0144)** and
  **BLIND-9-as-named-in-T-0144** â€” per `T-0144-blind-5.md:84-86,108-111`, do **not** run T-0205
  concurrently with T-0144 if it is editing the SendGrid/EmailService surface; serialize on that file.
- **Serialization note:** no data serialization concerns â€” no DTOs, entities, or wire contracts
  change. The removed `EmailNotSentError = "email.sending_failed"` key has no frontend i18n consumer
  (it was never returned to a client); confirm with a grep before deleting.
- **Keep, do not touch:** `ISendGridClientFactory.CreateClient()` (live, used by HealthController);
  `FcmPushDispatcher` dispatch/prune/classification logic and the terminal-config latch at `:152`
  (`agents/backlog/audits/AUDIT-2026-06-01-slice-reports.md:2504` lists FCM token classification as
  "correct â€” don't regress"); `EmailService`'s send path (BLIND-5's job, not this ticket).
- **Routing:** `backend` (engine + factory + FCM dispatcher) and `mobile` (scrap deletion) in
  parallel; reviewer-per-developer. Not `security_touching` â€” the dead XSS surface is removed, not
  modified, and the live SendGrid-template XSS audit is the separate security story (Out of scope).

## Status log
- 2026-06-01 â€” draft (created by pm)
- 2026-06-13 — **ready** (PM, Wave-5 intake / Batch **5B**). Dep T-0144✓ is `done` (Stripe/SendGrid via
  IHttpClientFactory merged Wave 1) — **AND it collapsed nothing this ticket re-deletes**: confirm at
  review whether T-0144 already removed the dead `SendGridClientFactory.SendTemplateEmailAsync`; if so,
  reconcile rather than re-delete (AC2). DoR met: dead-code/no-behavior-change removal across 4 disjoint
  surfaces (Handlebars engine, dead SendGrid factory member, FCM per-dispatch log-spam, the 13-file
  Android scrap tree), S, not security-touching (the dead XSS surface is **removed**, not modified; the
  live SendGrid-template XSS audit is a separate out-of-repo security story). No migration/regen. **Lane
  note:** the SendGrid surface is shared with T-0144 (done) — serialize only if T-0144 ever re-opens (it
  won't). backend (engine/factory/FCM) ∥ mobile (scrap deletion). sprint re-tagged 5.
- 2026-06-13 — **review** (backend). Implemented all 4 surfaces; build + tests green; consistency clean
  on touched areas. Details:
  - **AC1 (BLIND-4/9 — dead Handlebars engine):** DELETED the whole `Cleansia.Infra.Services/Templates/`
    tree — `HandlebarsTemplateEngine.cs`, `ITemplateEngine.cs`, and the 8 dead `Samples/*.html`
    Handlebars templates that only the deleted engine could render (orphaning them would itself leave
    dead code). Removed the DI line `AddScoped<ITemplateEngine, HandlebarsTemplateEngine>()` +
    its `using` in `ServiceCollectionExtensions.cs`. Dropped the now-unused `Handlebars.Net`
    `PackageReference` (`Cleansia.Infra.Services.csproj`) and central `PackageVersion` pin
    (`src/Directory.Packages.props`) — confirmed zero other consumers repo-wide. Two stale
    `using Cleansia.Infra.Services.Templates;` imports (never used the type) in
    `Features/EmployeePayroll/RegenerateInvoicePdf.cs` and `Services/PayPeriodBackgroundService.cs`
    were removed (build would otherwise CS0234). Grep for `ITemplateEngine`/`HandlebarsTemplateEngine`/
    `CompileAsync` → no code matches.
  - **AC2 (BLIND-10 — divergent SendGrid contract):** RECONCILED — T-0144 (done) did **NOT** remove the
    dead member; `SendTemplateEmailAsync` + `EmailNotSentError` were still present in both
    `SendGridClientFactory.cs` and `ISendGridClientFactory.cs`. Removed both (and the now-unused
    `Cleansia.Infra.Common.Validations`/`SendGrid.Helpers.Mail` usings); kept `CreateClient()` intact.
    `HealthController` (the only live factory consumer, `CreateClient()` only) unchanged and still
    builds. Grep for `SendTemplateEmailAsync` → no code matches.
  - **AC3 (BLIND-11 — FCM per-dispatch log-spam):** ALREADY RESOLVED in the committed tree (the
    Wave-1 1B integration-resilience refactor introduced the `InitOutcome` enum + terminal config
    latch). The `Disabled` skip branch (`:53`) no longer logs; the only disabled-state `Warning` is the
    latched one inside `EnsureInitialized` (`:178`), guarded by `_initAttempted` set immediately before
    it, so it fires at most once per process. NO production change needed. Added a **characterization
    test** `Dispatch/FcmPushDispatcherDisabledStateTests.cs` (3 cases) pinning: disabled-config →
    `PushDispatchResult(0, tokenCount, [], Skipped: true)`; empty-token → empty no-op, no warning;
    two consecutive disabled dispatches → exactly one Warning. Green against current code.
  - **AC4 (IA-11 — Android scrap):** DELETED `src/cleansia_android/scrap/` (13 `.kt` + res/manifest/
    gradle). Confirmed no Gradle settings / source set referenced it; grep for `scrap/partner-app-pre-rebuild`
    → only agents/planning docs remain (out of code scope).
  - **AC5:** `dotnet test Cleansia.Tests` → 1380/1380 pass excluding 5 failures in
    `S6LoggingHygieneCharacterizationTests` — an **UNTRACKED** test file (`?? git status`) from a
    concurrent logging-hygiene lane (RED test-first WIP on `EmailService.cs`, which that lane also has
    modified). Those 5 reference **none** of my surfaces and fail identically in isolation — NOT caused
    by this ticket. `node agents/tools/check-consistency.mjs` → 0 violations in
    Templates/SendGrid/Fcm/Android-scrap (the 171 reported are all pre-existing frontend C1/C3/D2).
  - **Deviations:** (1) deleted `Templates/Samples/*.html` and the `Handlebars.Net` package refs in
    addition to the literal AC1 file list (both are part of surface #1's dead engine; story
    `AUDIT-…-user-stories.md:6607-6608` explicitly calls for dropping the package ref). (2) Removed two
    stale `Templates` `using`s in AppServices to keep the build green (required by the deletion).
  - **Follow-up (not fixed here, out of scope):** `Cleansia.Core.AppServices/Common/BusinessErrorMessage.cs:35`
    still declares `EmailNotSentError = "email.sending_failed"` with **zero** `BusinessErrorMessage.EmailNotSentError`
    consumers repo-wide — a separate dead constant on a different surface (not the factory's). Left
    untouched (out of this ticket's 4 surfaces / risk of cross-lane collision); flag for a follow-up
    dead-code ticket.
  - **MANUAL_STEPs:** none. No DTO/endpoint/wire-contract change → no NSwag regen. No schema change →
    no EF migration. Removed `email.sending_failed` factory constant had no frontend i18n consumer
    (never returned to a client) — confirmed by grep.

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->
