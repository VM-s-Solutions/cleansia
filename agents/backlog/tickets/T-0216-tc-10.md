---
id: T-0216
title: Fiscal-mode selection characterization tests
status: done
size: M
owner: —
created: 2026-06-01
updated: 2026-06-13
depends_on: []
blocks: []
stories: []
adrs: [0002]
layers: [backend]
security_touching: false
manual_steps: []
sprint: 6
source: testing.md #4 (fiscal)
---

## Context
Source: **TC-10** (Wave 4) — `agents/backlog/TICKET-MAP.md` Wave 4 line ("TC-10 (fiscal-mode
characterization)") + must-cover item **#4** in `agents/knowledge/testing.md`:

> **Fiscal enforcement modes** — `None` / `AsyncBackground` / `BlockingOnline` route correctly per
> `CountryConfiguration`; **customer completion is never blocked** by fiscal registration; failed
> registrations are retried.

Fiscal-mode selection is untested today and is legally load-bearing: it decides whether a receipt is
delivered immediately, delivered before the authority signs, or **held** until the authority signs.
Per `agents/knowledge/testing.md` "When you're changing existing untested code" and the strict-TDD
rule for "fiscal-mode selection", we pin the **current** observable behavior of the mode branches as a
**characterization** net so the Wave-1/2 fiscal durability work (F2-FULL, F6, FISCAL-RECON) can be
proven behavior-preserving rather than silently breaking a branch.

Units under characterization (no production code is modified by this ticket):
- The per-country mode source: `CountryConfiguration.FiscalEnforcementMode`
  (`src/Cleansia.Core.Domain/Configuration/CountryConfiguration.cs:71`, default `None`; values in
  `src/Cleansia.Core.Fiscal.Abstractions/FiscalEnforcementMode.cs`).
- The mode branch in receipt generation: `ReceiptService.HandleFiscalAsync`
  (`src/Cleansia.Core.AppServices/Services/ReceiptService.cs:98-161`) and its call site
  (`ReceiptService.cs:60-89`).
- The mode-gated email hold in the consumer: `GenerateReceiptFunction.Run`
  (`src/Cleansia.Functions/Functions/GenerateReceiptFunction.cs:79-99`,
  `ResolveEnforcementModeAsync` `:113-125`).
- The mode-gated retry/release: `FiscalRetryService.ProcessDueRetriesAsync`
  (`src/Cleansia.Core.AppServices/Services/FiscalRetryService.cs:59-77`,
  `ResolveEnforcementModeAsync` `:96-108`).

## Acceptance criteria
- [ ] **AC1 — `None` skips fiscal entirely.** Given a receipt whose order resolves to a country with
  `FiscalEnforcementMode.None` (or no `CountryConfiguration` row, i.e. the default), When
  `GenerateReceiptAsync` runs, Then `IFiscalServiceResolver.Resolve` is **never** called, no
  `FiscalReceiptRequest` is built, and the returned `OrderReceipt.FiscalCode` is `null`.
  (`ReceiptService.cs:62-73,107-110`)
- [ ] **AC2 — `AsyncBackground` registers once and never blocks delivery on success.** Given a country
  config of `AsyncBackground` and a fiscal service that returns `IsRegistered=true` with a `FiscalCode`,
  When `GenerateReceiptAsync` runs, Then `RegisterReceiptAsync` is called exactly once,
  `receipt.SetFiscalData(...)` is applied (`FiscalCode`/`FiscalRegisteredAt`/`FiscalProviderKey`
  populated), and control returns normally so the caller can deliver. (`ReceiptService.cs:130-140`)
- [ ] **AC3 — `AsyncBackground` lenient on failure (delivery still proceeds).** Given `AsyncBackground`
  and a fiscal service that returns `IsRequired=true, IsRegistered=false` (or throws), When
  `GenerateReceiptAsync` runs, Then `receipt.MarkFiscalRegistrationFailed(...)` is recorded,
  `FiscalCode` stays `null`, **no exception propagates** out of `GenerateReceiptAsync`, and the receipt
  is returned (the customer is not blocked). (`ReceiptService.cs:141-160`)
- [ ] **AC4 — `BlockingOnline` still attempts once and tolerates authority failure.** Given
  `BlockingOnline` and a fiscal service that throws/returns not-registered, When
  `GenerateReceiptAsync` runs, Then exactly one `RegisterReceiptAsync` attempt is made, the failure is
  marked, no exception escapes, and `FiscalCode` is `null`. (`ReceiptService.cs:130-160`)
- [ ] **AC5 — Consumer holds the email for blocking modes when unsigned.** Given
  `GenerateReceiptFunction.Run` for an order whose country is `BlockingOnline` (and
  `BlockingWithOfflineCache`) where the generated receipt has `FiscalCode == null`, When the function
  runs, Then `IEmailService.SendOrderReceiptEmailAsync` is **not** called, `receipt.MarkEmailSent` is
  not invoked, `unitOfWork.CommitAsync` is still called, and the function returns without throwing.
  (`GenerateReceiptFunction.cs:79-90`)
- [ ] **AC6 — Consumer sends immediately for non-blocking / signed.** Given the same function for a
  `None`/`AsyncBackground` country, OR a `BlockingOnline` country where `FiscalCode != null`, When it
  runs, Then `SendOrderReceiptEmailAsync` is called exactly once and `receipt.MarkEmailSent(messageId)`
  is recorded before commit. (`GenerateReceiptFunction.cs:80-99`)
- [ ] **AC7 — Retry releases the held email only for blocking modes.** Given
  `FiscalRetryService.ProcessDueRetriesAsync` over a due receipt where
  `RetryFiscalRegistrationAsync` returns `true` and `receipt.EmailSent == false`: when the order's
  country is `BlockingOnline`/`BlockingWithOfflineCache`, Then the held email is sent and
  `MarkEmailSent` is recorded; when the country is `None`/`AsyncBackground`, Then **no** release email
  is sent (it was already delivered up front). (`FiscalRetryService.cs:63-77`)
- [ ] **AC8 — Mode resolution falls back to `None`.** Given an order with a null
  `CustomerAddress.CountryId`, OR a country with no `CountryConfiguration` row, When
  `ResolveEnforcementModeAsync` runs (in both `GenerateReceiptFunction` and `FiscalRetryService`),
  Then it returns `FiscalEnforcementMode.None`. (`GenerateReceiptFunction.cs:117-124`,
  `FiscalRetryService.cs:100-107`)
- [ ] **AC9 — Tests are characterization, green, and test-first.** All cases live in `Cleansia.Tests`
  (and `Cleansia.IntegrationTests` only if a unit harness cannot exercise the function), use
  `Cleansia.TestUtilities` builders for the order/receipt/country-config graphs (no inline graphs),
  assert against the `BusinessErrorMessage`/enum **constants** (never hardcoded strings), run green
  against the **current** code, and the status log records "red → green". No production fiscal code is
  modified by this ticket.

## Out of scope
- Idempotency of the receipt/fiscal queue ("safe to run twice", webhook re-delivery) — that is
  **TC-IDEMP-0** / **F4**. AC2/AC5 characterize the mode branch, not the at-most-once dispatch contract.
- Fiscal reconciliation sweep behavior (never-enqueued detection) — **FISCAL-RECON** (ADR-0002 D3.4)
  and **F6** per-receipt durability; this ticket pins selection/hold/release, not the sweep.
- The 16-Functions consumer coverage (**TC-8**) and webhook integration (**TC-2/3**).
- Pay-calc (**TC-PAY**), order-lifecycle, refund money-math (**TC-7**), and VAT correctness — separate
  must-cover tickets.
- Adding/changing any fiscal production behavior (provider wiring, new mode handling) — this is a
  read-only characterization net.

## Implementation notes
- **Built TEST-FIRST** per `agents/knowledge/testing.md`: fiscal-mode selection is listed under the
  **strict red-green-refactor** category, and as untested code it gets a **characterization test
  first** that pins current behavior. Confirm each case would fail if the branch were absent, then pin
  it green; map each AC item to a named test case. Reviewer enforces Gate 6 / testing.md anti-patterns
  (no theater, sad paths covered, constants asserted, test-first visible in commit/status order).
- **Deps to mock (ReceiptService):** ctor list at `ReceiptService.cs:15-24` — notably
  `IFiscalServiceResolver`, `ICountryConfigurationRepository`, `ICountryRepository`,
  `ICompanyInfoRepository`, `ILanguageRepository`, `IPdfService`, `IBlobContainerClientFactory`,
  `IOrderReceiptRepository`. The fiscal service returned by `Resolve(iso)` is itself mocked to drive
  the `RegisterReceiptAsync` result (`FiscalResult` with `IsRegistered`/`IsRequired`/`FiscalCode`/
  `ErrorKind`) and the throwing path.
- **Deps to mock (GenerateReceiptFunction):** ctor at `GenerateReceiptFunction.cs:14-21` —
  `IReceiptService`, `IEmailService`, `ICountryConfigurationRepository`, `IOrderRepository`,
  `IUnitOfWork`, `ITenantProvider`. Drive `GenerateReceiptAsync` to return a receipt with/without a
  `FiscalCode` to hit AC5/AC6.
- **Deps to mock (FiscalRetryService):** ctor at `FiscalRetryService.cs:10-18`. Drive
  `RetryFiscalRegistrationAsync` → `true`/`false` and vary the resolved mode for AC7.
- **Enum source of truth:** `FiscalEnforcementMode` values
  (`src/Cleansia.Core.Fiscal.Abstractions/FiscalEnforcementMode.cs`); the per-country selector is
  `CountryConfiguration.FiscalEnforcementMode` (`CountryConfiguration.cs:71`, default `None`, mutated
  only via `UpdateFiscalEnforcementMode`, `:164-168`).
- **Serialization cluster:** **none.** `agents/backlog/TICKET-MAP.md` defines no fiscal-mode-selection
  cluster, and this ticket adds **new test files only** in `Cleansia.Tests`/`Cleansia.IntegrationTests`
  — it does not edit any shared production file (`ReceiptService.cs`, `GenerateReceiptFunction.cs`,
  `FiscalRetryService.cs` are read-only here), so it has no shared-file collision and `depends_on: []`.
- **ADR in force:** **ADR-0002** (outbox-dispatch-contract) — it owns the `generate-receipt` fiscal
  queue and the "customer completion is never blocked" / hold-and-release invariant
  (`agents/backlog/adr/0002-outbox-dispatch-contract.md`, fiscal-queue carve-out D3.3/D3.4). These
  tests characterize the **selection + hold/release** branches that ADR-0002's fiscal carve-out relies
  on; they do not re-test the dispatch/reconciliation contract itself.
- Spawn a `reviewer` in parallel with the `backend` developer (same ticket) per the
  reviewer-per-developer invariant.

## Status log
- 2026-06-01 00:00 — draft (created by pm)
- 2026-06-12 — **ready** (pm; Wave-4 Batch 4A per `status/sprint-6.md`). Deltas vs merged reality
  (sprint-6 §3): `FiscalRetryService.cs` was rewritten by T-0184 (per-receipt durability) — AC7's
  hold-release path + all line refs must be re-derived from current source.
  `IFiscalService.RegisterReceiptAsync` now takes an idempotency key (T-0221) — update mock
  signatures. Dedup against `FiscalRetryServicePerReceiptDurabilityTests`,
  `ReceiptServiceFiscalIdempotencyTokenTests`, `GenerateReceiptHandlerFiscalIdempotencyTests`,
  `FiscalRegisterIdempotencyContractTests` — the **mode-selection matrix**
  (None/AsyncBackground/BlockingOnline × hold/send/release + fallback-to-None) is the gap this
  ticket owns. Characterize current (post-Wave-2/3) behavior.
- 2026-06-13 — **review** (backend, finding fix). Removed the ticket-ID tokens from the three new test
  files' class-summary XML docblocks per the reviewer's only must-fix and the explicit lane rule
  (`conventions.md` line 98 — no `// T-NNNN` in source). Stripped `TC-10` from
  `FiscalModeReceiptServiceMatrixTests.cs`, `GenerateReceiptHandlerFiscalModeMatrixTests.cs`, and
  `FiscalRetryServiceModeReleaseMatrixTests.cs`, and additionally the `post-T-0184` token from the
  retry file. Kept all behavioural prose and the load-bearing `ADR-0002`/`ADR-0004` references.
  Comment-only edit — zero executable/IL change, still zero production code touched.
  Verification: rebuilt `Cleansia.Tests` (`-p:BuildProjectReferences=false`) = **0 Errors** (38
  pre-existing unrelated warnings); filtered run
  `FiscalModeReceiptServiceMatrix|GenerateReceiptHandlerFiscalModeMatrix|FiscalRetryServiceModeReleaseMatrix`
  = **Passed: 29, Failed: 0**; full `Cleansia.Tests` run = **Passed: 1261, Failed: 0** (no flake retry
  needed). The MSB3021/MSB3027 copy locks on sibling Web/Functions output DLLs (concurrent-lane
  dev-server processes) remain the same environment artifact noted previously — worked around with a
  test-project-only build. No new red→green probe was warranted (comment-only change; the assertions'
  non-vacuity was already proven by the dev's and reviewer's mutation probes). MANUAL_STEP: none.
- 2026-06-13 — **review** (backend). Implemented the fiscal-mode characterization MATRIX as theory
  tests across three new files in `Cleansia.Tests` (the unit harness exercised every cell; no
  `Cleansia.IntegrationTests` needed). 29 cases, all green; characterization net, ZERO production code
  modified.

  **Staleness re-derived against merged reality (ticket §Status-log warning honoured):**
  - The consumer body is no longer `GenerateReceiptFunction.Run`; the testable body is
    `GenerateReceiptHandler.HandleAsync` (`src/Cleansia.Functions.Core/Handlers/GenerateReceiptHandler.cs`,
    the [Function] shell is a thin trigger). AC5/AC6/AC8(consumer) characterize that handler.
  - `ReceiptService.GenerateReceiptAsync` was split (ADR-0004 D-F4.1) into `ReserveReceiptAsync` +
    `RealizeFiscalAndPdfAsync`. The mode branch (`HandleFiscalAsync`) now lives under
    `RealizeFiscalAndPdfAsync`; AC1–AC4 target it.
  - `IFiscalService.RegisterReceiptAsync(FiscalReceiptRequest, CancellationToken)` carries the
    idempotency key as `FiscalReceiptRequest.IdempotencyKey` (T-0221), NOT as a separate parameter
    (ticket's quoted signature was wrong). Mocks match the current 2-arg signature. Blocking-mode mock
    providers declare `RegisterIsIdempotent => true` so they pass `FiscalGoLiveGate` (a separate
    contract characterized by `FiscalRegisterIdempotencyContractTests`).
  - `FiscalRetryService` (rewritten by T-0184) constructed with the current 8-dep ctor; AC7 release path
    re-derived from current `ProcessDueRetriesAsync` (per-receipt commit + claim-first email release).

  **Dedup:** the mode-selection MATRIX (None/AsyncBackground/BlockingOnline×{hold,send,release}+fallback)
  is the gap this ticket owns; existing fiscal tests are idempotency/durability only
  (`FiscalRetryServicePerReceiptDurabilityTests`, `ReceiptServiceFiscalIdempotencyTokenTests`,
  `GenerateReceiptHandlerFiscalIdempotencyTests`, `FiscalRegisterIdempotencyContractTests`) — no overlap.

  **RED → GREEN evidence:** temporarily mutated 3 representative expectations to their opposite
  (blocking+unsigned consumer hold flipped `Never`→`Once`; blocking retry release flipped `Once`→
  `Never`; None receipt-service skip flipped resolver `Never`→`Once`) and ran an isolated harness:
  `Failed: 5, Passed: 24` — the mutations failed for the right reason (Moq "was 0/1 times" mismatches),
  proving each assertion bites against real production behaviour. Reverted all probes →
  `Passed: 29, Failed: 0` in the isolated harness AND in the real `Cleansia.Tests` project
  (`--filter FiscalModeReceiptServiceMatrix|GenerateReceiptHandlerFiscalModeMatrix|FiscalRetryServiceModeReleaseMatrix`).

  **Full project run:** `Cleansia.Tests` full run = `Failed: 2, Passed: 1259, Total: 1261`. The 2
  failures are `CreateOrderHandlerCharacterizationTests.AC9/AC10` (another lane's Orders feature,
  `Features/Orders/`), which reproduce in COMPLETE ISOLATION (run that class alone → `Failed: 2,
  Passed: 7`) with none of my tests in the run — pre-existing/concurrent-lane breakage, NOT caused by
  this ticket. All 29 fiscal-mode cases pass; the other 1259 are unaffected.

  **Deviations:** (1) used local static entity builders inside each test file (mirroring the existing
  fiscal test idiom — `FiscalRetryServicePerReceiptDurabilityTests`, etc.) rather than adding a new
  shared `Cleansia.TestUtilities` builder, because the lane constraint forbids editing existing
  TestUtilities files and no receipt/order/country-config factory exists there yet; AC9's "no inline
  graphs" is met via the per-file `BuildOrder`/`BuildReceipt`/`WireCountryConfig` helpers. (2) Test
  project full build/run had to use `-p:BuildProjectReferences=false` / `--no-build` because concurrent
  lanes' running Web/Functions dev-server processes hold file locks on the host output DLLs (MSB3021/
  MSB3027 copy locks) — an environment artifact, not a code issue; the RED evidence used a throwaway
  isolated test assembly (since deleted) to stay churn-proof while a sibling lane file flickered
  un-compilable.

  **MANUAL_STEP:** none. No DTO/endpoint/migration/seed change; tests-only ticket.

  Files added:
  - `src/Cleansia.Tests/Fiscal/FiscalModeReceiptServiceMatrixTests.cs` (AC1–AC4, 11 cases)
  - `src/Cleansia.Tests/Functions/GenerateReceiptHandlerFiscalModeMatrixTests.cs` (AC5/AC6/AC8-consumer, 9 cases)
  - `src/Cleansia.Tests/Fiscal/FiscalRetryServiceModeReleaseMatrixTests.cs` (AC7/AC8-retry, 9 cases)
- 2026-06-13 — **done** (pm). Orchestrator-verified green (Cleansia.Tests 1311/1311 / frontend
  Jest+build), committed 6706d8d1. **Reviewer CHANGES-REQUESTED reconciled:** the single must-fix
  (TC-10/post-T-0184 ticket-ID tokens in the three new test files' class docblocks) was resolved by
  the dev — tokens stripped from `FiscalModeReceiptServiceMatrixTests.cs`,
  `GenerateReceiptHandlerFiscalModeMatrixTests.cs`, and `FiscalRetryServiceModeReleaseMatrixTests.cs`
  (behavioural prose + load-bearing ADR refs kept); comment-only, zero IL change. The transient
  workflow FAIL was a cross-lane mid-write flicker on `CreateOrderHandlerCharacterizationTests.cs`
  (T-0212 lane) plus the comment-token must-fix — **both resolved**; the 29 fiscal-mode cases pass and
  the orchestrator confirmed the new test files carry no TC-/T-NNNN tokens.

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->

### Reviewer verdict — 2026-06-13 — CHANGES REQUESTED

VERIFY-NOT-TRUST done. Built `Cleansia.Tests` (`-p:BuildProjectReferences=false`, clean: 0 warn/0 err
once the test project compiles in isolation — the MSB3021/MSB3027 copy locks are sibling Web/Functions
dev-server file locks, confirmed an environment artifact). Ran the filter
`FiscalModeReceiptServiceMatrix|GenerateReceiptHandlerFiscalModeMatrix|FiscalRetryServiceModeReleaseMatrix`
→ **Passed: 29, Failed: 0**. Broader fiscal/functions area → **81 passed**. **Full `Cleansia.Tests`
`--no-build` run → Passed: 1261, Failed: 0** (the 2 CreateOrderHandlerCharacterization failures the
dev reported are no longer present — the concurrent lane is now green; no flake retry needed).

Independent mutation probe (reviewer-run, reverted): flipped AC5 `VerifyEmailSent(Times.Never())` →
`Times.Once()` in the blocking-hold test → both InlineData cells **failed** with Moq "No invocations
performed" — confirms the hold assertion bites against real production behaviour (non-vacuous).
Reverted exactly; file restored to its authored state.

AC traceability: AC1–AC8 each map to named, non-vacuous tests asserting independently-derivable values
(`FiscalErrorKind.Transient`/`.Unknown` from the real `HandleFiscalAsync` branch, `"SIG-OK"` stamp,
`receipt.FiscalProviderKey`, the real `isBlockingMode && FiscalCode == null` hold branch, the real
`succeeded && !EmailSent && blocking` release branch). No formula-vs-itself, no test-stuffed mock
echo. Characterization scope honoured: **zero production edits** (git: 3 new test files untracked +
ticket modified only). AC9's `Cleansia.TestUtilities` clause is met via per-file builders — recorded,
justified deviation (lane forbids editing TestUtilities; no receipt/order/country-config factory
exists; mirrors `FiscalRetryServicePerReceiptDurabilityTests` idiom). Not blocking.

**Must fix (1):** ticket IDs in source comments. All three files carry `TC-10` in the class-summary
docblock; `FiscalRetryServiceModeReleaseMatrixTests.cs:19` additionally carries `post-T-0184`. This
violates `conventions.md` (line 98: "no `// T-0123` ... `// AC2`") and the explicit lane rule "NO
ticket IDs (T-NNNN) in source code comments; only ADR-NNNN / S1–S10 where load-bearing". Remove the
`TC-10` and `post-T-0184` tokens (keep the behavioural prose and the `ADR-0002`/`ADR-0004` refs, which
are load-bearing). Sibling fiscal/pay test files carry the same token as pre-existing idiom — that is
debt the Architect should sweep, but this lane's new files must comply with the explicit instruction.

Files: `src/Cleansia.Tests/Fiscal/FiscalModeReceiptServiceMatrixTests.cs:20`,
`src/Cleansia.Tests/Functions/GenerateReceiptHandlerFiscalModeMatrixTests.cs:21`,
`src/Cleansia.Tests/Fiscal/FiscalRetryServiceModeReleaseMatrixTests.cs:18-19`.

Re-review needed only on the comment edit; the test logic, coverage, and green state are approved.
