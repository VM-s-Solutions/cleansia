---
id: T-0218
title: Accessibility pass on cleansia-* library + order wizard (3 apps)
status: review
size: M
owner: —
created: 2026-06-01
updated: 2026-06-12
depends_on: []
blocks: []
stories: []
adrs: []
layers: [frontend]
security_touching: false
manual_steps: []
sprint: 6
source: blindspots §D8
---

## Context
The blindspots audit names **accessibility as "entirely absent across the 3 Angular apps"**
(`agents/backlog/audits/AUDIT-2026-06-01-blindspots.md` §B, line 56) and prioritizes it as
follow-up pass **§D item 8**: "Accessibility pass — the `cleansia-*` library + the order wizard
across all 3 apps" (line 81). This ticket closes the highest-impact holes on the two surfaces the
audit calls out: the shared `cleansia-*` component primitives (reused by all three apps) and the
customer order wizard (the conversion-critical, keyboard-hostile path).

Grounded gaps in real code:
- **Clickable `<div>`/non-button elements with no keyboard affordance or ARIA state** in the wizard:
  stepper items (`order-wizard.component.html:29-45`), service/package selection cards
  (`:119-147`, `:218-251`), saved-address radio rows (`:335-364`), payment-method cards
  (`:546-586`), and the mobile price-bar header (`:732-745`) all carry `(click)` but have no
  `role`, `tabindex`, keyboard handler, or `aria-pressed`/`aria-selected`/`aria-current`. They are
  unreachable and unoperable by keyboard and unannounced by screen readers.
- **Form labels not programmatically associated** with their inputs — contact fields use bare
  `<label>` next to `pInputText` with no `for`/`id` pairing (`:267-322`), and the saved-address
  label field (`:415-422`).
- **Inline field errors not linked** to their inputs — `<small class="…__field-error">` is rendered
  (`:277-279` etc.) but inputs carry no `aria-invalid` / `aria-describedby`, so the error text is
  never announced.
- **Icon-only buttons with no accessible name** — the room/bathroom counter +/- buttons
  (`:165-203`) and the shared `cleansia-button` icon-only mode
  (`cleansia-button.component.html`) render an icon with no `aria-label`. The convention already
  exists in the library: `cleansia-scroll-top` is invoked with `[ariaLabel]` (`:805`), so the fix
  is to extend the same pattern.

## Acceptance criteria
- [ ] **AC1 — keyboard operability of click-only controls.** Given a keyboard-only user in the
  order wizard, When they Tab through step 0–3, Then every interactive element that currently has
  `(click)` only — stepper items, service cards, package cards, saved-address rows, payment cards,
  mobile price header — is focusable (`tabindex`/native control), shows a visible focus ring, and
  activates on **Enter and Space** with the same effect as a mouse click.
- [ ] **AC2 — ARIA state on selectable controls.** Given a screen reader, When focus lands on a
  selection card / payment card / saved-address row / stepper item, Then its selected/current state
  is exposed (`aria-pressed`, `aria-selected`, or `aria-current="step"` as appropriate) and its
  accessible name is the visible text — verified by the spec asserting the rendered attributes.
- [ ] **AC3 — label association.** Given the contact step (`:267-322`) and the save-address label
  field (`:415-422`), When rendered, Then every `<label>` is tied to its control via `for`/`id`
  (or wrapping), so clicking the label focuses the input and the input has an accessible name.
- [ ] **AC4 — error announcement.** Given a wizard field fails validation, When the error `<small>`
  renders, Then the input gets `aria-invalid="true"` and `aria-describedby` pointing at the error
  element's `id`, so the message is announced. Given the field becomes valid, Then `aria-invalid`
  clears.
- [ ] **AC5 — icon-only buttons have names.** Given the room/bathroom counter buttons (`:165-203`)
  and any icon-only `cleansia-button`, When rendered, Then each exposes an `aria-label` (sourced
  from a translation key present in all 5 locales). `cleansia-button` gains an `ariaLabel` input and
  emits `aria-label` on the underlying `p-button` when icon-only — mirroring the existing
  `cleansia-scroll-top [ariaLabel]` convention.
- [ ] **AC6 — tests prove the holes are closed.** Jest component specs assert the above on the
  wizard step components and on the touched `cleansia-*` primitives (`cleansia-button` at minimum):
  focusability, Enter/Space activation, the ARIA attributes, label/error association. Specs were
  written **before** the fix and fail against current markup (red→green per `knowledge/testing.md`).

## Out of scope
- A full WCAG 2.1 AA audit of all 3 apps end to end — this pass is scoped by §D8 to the shared
  `cleansia-*` library and the order wizard only. Other features get their own a11y tickets later.
- Visual/color-contrast theme changes and the email-template contrast work (that is BUG-22).
- Admin/partner feature pages beyond the shared primitives they consume.
- Any backend, DTO, or i18n-namespace restructuring. New aria-label strings are added under
  existing namespaces; no `nswag-regen` and no migration.
- Automated axe-core CI integration (propose as a follow-up, do not build here).

## Implementation notes
- **Built TEST-FIRST per `agents/knowledge/testing.md`** — UI is "pragmatic test-alongside", but the
  observable a11y contract here (focusability, Enter/Space activation, ARIA attributes, label/error
  wiring) is assertable logic: write the Jest specs first, confirm they fail against current markup
  for the right reason, then make them pass. The status log must show red→green and each AC must map
  to a case.
- **No governing ADR.** The three accepted ADRs (0001 authorization, 0002 outbox, 0003 rate-limiting)
  do not touch frontend a11y; `adrs: []`. Follow `agents/knowledge/patterns-frontend.md` +
  `conventions.md` — reuse existing `cleansia-*` primitives, never raw controls, OnPush + signals,
  every new string via `TranslatePipe`/`TranslateService` with keys added to **all 5 locales**
  (`apps/<app>/src/assets/i18n/{en,cs,sk,uk,ru}.json`).
- **Serialization cluster:** `cleansia-button.component.{html,ts}` is a shared library primitive
  reused across all 3 apps and many features — this ticket is the sole concurrent editor of the
  touched `cleansia-*` primitives and the order-wizard files for this wave. TICKET-MAP.md lists no
  named cluster for these files; the PM must NOT spawn another frontend ticket editing
  `libs/shared/components/**` or `libs/cleansia-customer-features/order-wizard/**` in parallel —
  serialize any such ticket after this one to avoid a shared-file collision.
- **Files to touch (known):**
  - `libs/cleansia-customer-features/order-wizard/src/lib/order-wizard/order-wizard.component.{html,ts}`
  - `libs/cleansia-customer-features/order-wizard/src/lib/order-wizard/components/wizard-summary-step.component.{html,ts}`
  - `libs/shared/components/src/lib/cleansia-button/cleansia-button.component.{html,ts}`
  - i18n JSON for the customer app (new `aria.*`-style keys) in all 5 locales.
- **Convention to mirror:** `cleansia-scroll-top [ariaLabel]` (`order-wizard.component.html:805`) is
  the existing accessible-name pattern — extend `cleansia-button` the same way rather than inventing
  a new mechanism. Prefer converting click-only `<div>`s to native `<button type="button">` where
  layout allows (cleanest a11y) over bolting `role`/`tabindex`/keydown onto a `<div>`.
- **No owner-only steps:** `manual_steps: []`, `security_touching: false`. Flag none.

## Status log
- 2026-06-01 — draft (created by pm)
- 2026-06-12 — **ready** (pm; Wave-4 Batch 4B per `status/sprint-6.md` — **runs FIRST in 4B; T-0217
  is serialized behind it** on the 5 customer locale JSONs). Deltas: wizard HTML line refs may have
  drifted since 2026-06-01 — verify against current files. This ticket is the sole editor of
  `libs/shared/components/**` + `order-wizard/**` this wave. QA gate includes a keyboard/screen-reader
  walkthrough.
- 2026-06-13 — **review** (frontend). Built TEST-FIRST.
  **RED evidence:**
  - `cleansia-button.component.spec.ts` (4 cases) run RED first → failed `NG0303: Can't set value of
    the 'ariaLabel' input` (input did not exist) + 2 cases asserting the inner `<button>` carried no
    `aria-label`. Added `ariaLabel` input + `resolvedAriaLabel` computed (emits only when icon-only) →
    bound PrimeNG `[ariaLabel]` on `p-button` → GREEN 4/4.
  - `order-wizard.component.spec.ts` (12 cases) run RED first → all 12 failed for the right reasons:
    stepper/cards/payment/address/mobile-header rendered as `DIV` (expected `BUTTON`), no
    `aria-pressed`/`aria-current`/`aria-expanded`, no counter `aria-label`, no `for`/`id` label
    association, no `aria-invalid`/`aria-describedby`. Converted click-only `<div>`s to native
    `<button type="button">`, added ARIA state + labels (new `aria.*` i18n keys), wired
    label/error association → GREEN 12/12.
  **AC mapping:** AC1 → stepper/cards/payment/address/mobile-header focusable native buttons +
  `:focus-visible` ring + Enter/Space (native) + Space added to summary code rows; AC2 →
  `aria-current="step"` (stepper), `aria-pressed` (service/package/payment/address cards); AC3 →
  `for`/`id` on all contact labels + save-address label; AC4 → `aria-invalid`/`aria-describedby`
  toggling with `fieldError()`; AC5 → counter `aria-label`s + `cleansia-button` `ariaLabel` input;
  AC6 → 16 Jest cases (4 button + 12 wizard) written before the fix, red→green recorded above.
  **GREEN evidence:** `nx test components` 4/4; `nx test cleansia-customer-order-wizard` 12/12;
  `nx build cleansia.app --configuration=production` succeeded (only pre-existing NG8102 warning on
  `wizard-summary-step.component.html:117` `extra.price ?? 0`, untouched by this ticket).
  **i18n:** one `aria.*` block (11 keys) added to all 5 customer locales
  (`en,cs,sk,uk,ru`) — kept clean/isolated so T-0217 can append behind it.
  **Deviations / notes (see Review):**
  - Fixed a latent harness bug in the order-wizard lib: `jest.config.ts` (`preset`/`setupFilesAfterEnv`)
    and `tsconfig.json` `extends` were one path level too deep (`../../../../` vs `../../../`),
    which is why this lib had never had a spec run. Corrected to match working sibling libs
    (`disputes`, `profile`, `register`). No production code in those config files.
  - `components:lint` fails on **pre-existing** baseline errors (module-boundaries, dependency-checks,
    the legacy `onClick` output `no-output-on-prefix`, empty base-form methods) across the whole lib —
    NOT introduced here; `order-wizard:lint` passes clean. No new lint errors from this ticket.
  - Customer app project name is `cleansia.app` (ticket said `cleansia-app`).

## Review
<!-- reviewer / security / optimizer write verdicts here; PM reconciles before advancing state -->

### Reviewer verdict — 2026-06-13 (reviewer)
**APPROVED (PASS-WITH-NOTES).** All 6 AC have real tests; verified by running them, not trusting the report.

**VERIFY-NOT-TRUST (ran myself):**
- `nx test components` → 4/4 GREEN (cleansia-button a11y; it is the only spec in the lib — full run clean).
- `nx test cleansia-customer-order-wizard` → 12/12 GREEN (full project run; one spec in lib).
- `nx lint cleansia-customer-order-wizard` → clean (div→button conversions satisfy
  `click-events-have-key-events` / `interactive-supports-focus`).
- Counts match the developer report exactly (16 = 4 + 12).

**Production-edit scope:** within ticket allowance. Diff is HTML/SCSS/i18n + new specs + harness config
only. `order-wizard.component.ts`, `.facade.ts`, `.models.ts`, `wizard-summary-step.component.ts` are
UNMODIFIED — `markTouched`/`fieldError`/toggle methods the specs drive already existed; the a11y work is
template + style + i18n, which is the correct surface for this ticket. No accidental logic change.

**AC traceability (each maps to a real, biting test):**
- AC1 focusable+activation: stepper/cards/payment/address/mobile-header are native `<button type="button">`
  (Enter/Space free); `:focus-visible` ring added in SCSS on every converted control; summary code-rows
  keep `role=button tabindex=0` + added `keydown.space`+preventDefault (matches catalog a11y-div variant).
- AC2 ARIA state: `aria-current="step"` (stepper), `aria-pressed` (service/package/payment/address) — specs
  assert false→true through the REAL `toggleService`/`isAddressSelected`/etc., not stubs.
- AC3 label assoc: `for`/`id` on all 4 contact labels + save-address label; spec renders step 1 and finds
  `#wizard-first-name` + `label[for=...]` (not a null-vs-null pass).
- AC4 error announce: `aria-invalid`/`aria-describedby` toggle with `fieldError()`; spec proves both the
  set ('true' + describedby resolves to the `<small id>`) and the clear (absent when valid) directions.
- AC5 icon-only names: counter `aria-label`s + `cleansia-button` `ariaLabel` input → `resolvedAriaLabel`
  emits only when icon-only; spec proves it is null for text buttons and when unset.
- AC6 test-first: RED claim is credible — `git show HEAD` confirms originals were `<div>`, so the
  `tagName==='BUTTON'` assertions fail red against prior markup; button spec red on `NG0303` (input absent).

**i18n (verified all 5 locales):** 11 `aria.*` keys present + non-empty + translated in en/cs/sk/uk/ru;
summary aria refs (`pages.order.referral.row_title`, `pages.order.promo.row_title`) exist in all 5.
Diff is purely additive (+13 lines each), root-level `aria` block kept isolated so T-0217 appends behind.

**No vacuous tests:** assertions read rendered DOM driven through real component methods; expected values
are independently derivable from setup. No mock-echo, no formula-vs-itself.

**Harness fix (justified, no production code):** `jest.config.ts` (`setupFilesAfterSetup`→`setupFilesAfterEnv`,
preset depth) and `tsconfig.json` extends depth corrected to match working siblings (`profile`, `disputes`).
This is why the lib never ran a spec before. Confirmed against siblings.

**Non-blocking notes (do not block; address opportunistically):**
1. `order-wizard.component.spec.ts` carries decorative `// ─── AC.. — section ───` banner comments that
   duplicate the `describe(... (AC..))` line directly below them — `conventions.md` flags section-divider
   banners + `// AC2`-style labels as comment noise. The AC labels inside `describe`/`it` are the sanctioned
   AC→test mapping and may stay; remove the redundant banner comments above each describe.
2. Payment cards use `aria-pressed` (toggle) though they are mutually exclusive (radio-like). `aria-pressed`
   is acceptable; `role=radiogroup`/`role=radio`+`aria-checked` would be marginally more semantic. Optional.
3. `coverageDirectory` in `jest.config.ts` still uses `../../../../` (one level too deep) — only affects the
   coverage output path, harmless, but tidy it while the file is open.

**Residual for owner (cannot be done here):** a runtime screen-reader walkthrough (NVDA/VoiceOver) and an
axe-core pass are out of scope per the ticket and not executable in this environment — owner/QA should do a
manual SR pass on the wizard before PROD. Automated axe-core CI is the proposed follow-up.
