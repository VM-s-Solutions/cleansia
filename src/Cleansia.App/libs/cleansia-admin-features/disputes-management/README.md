# disputes-management

Admin dispute-management feature: a paged disputes list and a detail/resolution
panel (resolve + issue refund via Stripe, guarded status transitions, staff
messages) over the generated `AdminDisputeClient`.

## Running unit tests

Run `nx test disputes-management` to execute the unit tests via
[Jest](https://jestjs.io).
