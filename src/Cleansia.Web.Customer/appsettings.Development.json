{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "CorsOrigins": [
    "http://localhost:4000",
    "http://localhost:4200",
    "http://localhost:4202"
  ],
  "Google": {
    // Local only. The same web client the browser uses (environment.ts), which lists
    // http://localhost:4202 as an authorised origin. Deployed hosts get theirs from the
    // Bicep-managed Google__ClientId app setting instead — see appsettings.json.
    "ClientId": "354682423254-boe1nlnb1dbd3m6a013d3nkpo2e9bgiq.apps.googleusercontent.com"
  }
}
