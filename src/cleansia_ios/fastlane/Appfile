# app_identifier is passed per lane (two apps share this config), so it is not
# set here. The signing team comes from the environment — never hardcoded — and
# overrides the DEVELOPMENT_TEAM in the gitignored Config/Local.xcconfig.
team_id(ENV["ASC_TEAM_ID"]) if ENV["ASC_TEAM_ID"]
